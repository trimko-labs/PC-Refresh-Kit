# v2.5 Confiance visuelle - Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** L'aide du cockpit devient visuelle et le kit se justifie d'un coup d'oeil : ancre sur l'élément dont l'aide est affichée, en-tête contextuel avec badges normalisés, filet de sauvegarde visible en permanence, vérité annoncée avant tout lancement réel.

**Architecture:** Fonctions pures dans lib/Help.ps1 et lib/Theme.ps1 (testées Pester), rendu et câblage dans Run-GUI.ps1, données dans config/help.fr.json (champ optionnel `badges`, fail-safe). Détection des volumes de backup extraite de modules/01-Backup.ps1 vers lib/Common.ps1 (DRY module + GUI).

**Tech Stack:** PowerShell 5.1, WinForms, Pester 5 (via pwsh 7 uniquement), StrictMode Latest.

**Spec:** `docs/superpowers/specs/2026-08-28-v2.5-confiance-visuelle-design.md`

## Global Constraints

- Branche de travail : `feat/v2.5-confiance-visuelle` (locale, JAMAIS poussée).
- Messages de commit en ASCII pur (pas d'accent, pas d'apostrophe typographique), format `type: description`.
- Fichiers `.ps1`/`.md` suivis : BOM UTF-8, zéro U+2014/U+2013, newline finale. `config/*.json` : JAMAIS de BOM.
- Textes visibles (UI, aide, JSON) : français avec TOUS les accents corrects, séparateur ` - ` (jamais de tiret long), pas d'émojis.
- Pester UNIQUEMENT via `pwsh -NoProfile -Command "Invoke-Pester -Path tests -CI"` (jamais powershell 5.1).
- SelfTest UNIQUEMENT via `powershell -ExecutionPolicy Bypass -File .\Run-GUI.ps1 -SelfTest` (5.1), exit code 0 attendu.
- `vendor/TelemetryGuard/` intouchable.
- StrictMode Latest partout : accès défensifs (`PSObject.Properties['x']` avant `.x` sur objets JSON, `@()` autour des collections).
- Convention de retour des collections (décision Task 1, preuve au banc) : les fonctions retournent les tableaux SANS virgule unaire, et TOUT appelant enveloppe l'appel dans `@(...)` - contrat (a) documenté dans `modules/02-Antivirus.ps1:138`. La combinaison `return ,$arr` + `@(f)` à l'appel imbrique le tableau et `Count` vaut 1 même à vide.
- Après CHAQUE tâche, jeu de portes complet vert :
  `pwsh -NoProfile -File .\tests\Test-KitParse.ps1 -CI` ; `pwsh -NoProfile -File .\tests\Test-KitEncoding.ps1 -CI` ; `pwsh -NoProfile -File .\tests\Test-KitAnonymity.ps1 -CI` ; `pwsh -NoProfile -Command "Invoke-Pester -Path tests -CI"` ; `pwsh -NoProfile -File .\tests\Invoke-SmokeTest.ps1 -CI` ; `powershell -ExecutionPolicy Bypass -File .\Run-GUI.ps1 -SelfTest`.
- Les numéros de ligne cités sont ceux de l'état de départ (commit `1d0f8fd`) : après édition, retrouver les points d'insertion par leur CONTENU, pas par le numéro.

---

### Task 1: Fonctions pures d'en-tête d'aide (Get-HelpHeaderInfo, Get-HelpBadges)

**Files:**
- Modify: `lib/Help.ps1` (ajouter les deux fonctions à la fin du fichier)
- Test: `tests/Help.Tests.ps1` (ajouter un Describe, suivre le pattern de chargement déjà présent dans ce fichier : il dot-source `lib/Help.ps1`)

**Interfaces:**
- Produces: `Get-HelpHeaderInfo -Key <string>` -> PSCustomObject `@{ Kind; KindText; Breadcrumb }` (jamais null, repli `general`/`AIDE`/`Général`).
- Produces: `Get-HelpBadges -Entry <object>` -> tableau (possiblement vide) de PSCustomObject `@{ Text; Level }`, Level dans `Ok|Accent|Warn|Err|Neutral`. Ordre : reversible, data, duration.

- [ ] **Step 1: Écrire les tests (échec attendu)**

Ajouter à la fin de `tests/Help.Tests.ps1` :

```powershell
Describe 'Get-HelpHeaderInfo' {
    It 'classe une cle module' {
        $i = Get-HelpHeaderInfo -Key 'module.03'
        $i.Kind | Should -Be 'module'
        $i.KindText | Should -Be 'ÉTAPE'
        $i.Breadcrumb | Should -Be 'Intervention > Étapes'
    }
    It 'classe profil, debloatage, comptes, options, actions, etat' {
        (Get-HelpHeaderInfo -Key 'profile.gamer').KindText   | Should -Be 'PROFIL'
        (Get-HelpHeaderInfo -Key 'debloat.standard').Breadcrumb | Should -Be 'Réglages > Débloatage'
        (Get-HelpHeaderInfo -Key 'account.standard').Breadcrumb | Should -Be 'Réglages > Comptes'
        (Get-HelpHeaderInfo -Key 'option.recycle').KindText  | Should -Be 'RÉGLAGE'
        (Get-HelpHeaderInfo -Key 'action.run').KindText      | Should -Be 'ACTION'
        (Get-HelpHeaderInfo -Key 'status.backupnet').KindText | Should -Be 'ÉTAT'
    }
    It 'replie toute cle inconnue sur AIDE / General' {
        $i = Get-HelpHeaderInfo -Key 'welcome'
        $i.Kind | Should -Be 'general'
        $i.KindText | Should -Be 'AIDE'
        $i.Breadcrumb | Should -Be 'Général'
    }
}

Describe 'Get-HelpBadges' {
    It 'rend les trois badges dans l ordre reversible, data, duration' {
        $e = [PSCustomObject]@{ title = 't'; badges = [PSCustomObject]@{ reversible = 'store'; data = 'untouched'; duration = '5-15 min' } }
        $b = @(Get-HelpBadges -Entry $e)
        $b.Count | Should -Be 3
        $b[0].Text | Should -Be 'RÉVERSIBLE VIA STORE'
        $b[0].Level | Should -Be 'Accent'
        $b[1].Text | Should -Be 'DONNÉES PERSO : INTACTES'
        $b[1].Level | Should -Be 'Ok'
        $b[2].Text | Should -Be 'DURÉE : 5-15 min'
        $b[2].Level | Should -Be 'Neutral'
    }
    It 'mappe chaque niveau de reversibilite' {
        foreach ($case in @(
            @{ v = 'yes';      t = 'RÉVERSIBLE';               l = 'Ok' }
            @{ v = 'partial';  t = 'PARTIELLEMENT RÉVERSIBLE'; l = 'Warn' }
            @{ v = 'no';       t = 'IRRÉVERSIBLE';             l = 'Err' }
            @{ v = 'readonly'; t = 'LECTURE SEULE';            l = 'Ok' }
        )) {
            $e = [PSCustomObject]@{ badges = [PSCustomObject]@{ reversible = $case.v } }
            $b = @(Get-HelpBadges -Entry $e)
            $b[0].Text | Should -Be $case.t
            $b[0].Level | Should -Be $case.l
        }
    }
    It 'mappe copyonly et optin' {
        $e = [PSCustomObject]@{ badges = [PSCustomObject]@{ data = 'copyonly' } }
        (@(Get-HelpBadges -Entry $e))[0].Text | Should -Be 'DONNÉES PERSO : COPIE SEULE'
        $e = [PSCustomObject]@{ badges = [PSCustomObject]@{ data = 'optin' } }
        (@(Get-HelpBadges -Entry $e))[0].Level | Should -Be 'Warn'
    }
    It 'ignore une valeur inconnue et un champ vide sans erreur' {
        $e = [PSCustomObject]@{ badges = [PSCustomObject]@{ reversible = 'nimporte'; duration = '   ' } }
        @(Get-HelpBadges -Entry $e).Count | Should -Be 0
    }
    It 'rend une liste vide sans champ badges' {
        $e = [PSCustomObject]@{ title = 'sans badges' }
        @(Get-HelpBadges -Entry $e).Count | Should -Be 0
    }
    It 'rend une liste vide si badges est null' {
        $e = [PSCustomObject]@{ badges = $null }
        @(Get-HelpBadges -Entry $e).Count | Should -Be 0
    }
}
```

- [ ] **Step 2: Vérifier l'échec** - `pwsh -NoProfile -Command "Invoke-Pester -Path tests/Help.Tests.ps1 -CI"` doit échouer (fonctions absentes).

- [ ] **Step 3: Implémenter dans `lib/Help.ps1`** (à la fin du fichier, avant la newline finale) :

```powershell
# ---------------------------------------------------------------------------
# Get-HelpHeaderInfo : type et fil d'Ariane d'une rubrique, dérivés de sa clé.
# PURE. Aucune donnée de catalogue : la clé suffit, toute clé inconnue tombe
# sur le repli générique (jamais d'erreur).
# ---------------------------------------------------------------------------
function Get-HelpHeaderInfo {
    [CmdletBinding()]
    param([Parameter(Mandatory)][string]$Key)
    $map = @(
        @{ Prefix = 'module.';  Kind = 'module';  KindText = 'ÉTAPE';   Breadcrumb = 'Intervention > Étapes' }
        @{ Prefix = 'profile.'; Kind = 'profile'; KindText = 'PROFIL';  Breadcrumb = 'Intervention > Profil' }
        @{ Prefix = 'debloat.'; Kind = 'setting'; KindText = 'RÉGLAGE'; Breadcrumb = 'Réglages > Débloatage' }
        @{ Prefix = 'account.'; Kind = 'setting'; KindText = 'RÉGLAGE'; Breadcrumb = 'Réglages > Comptes' }
        @{ Prefix = 'option.';  Kind = 'setting'; KindText = 'RÉGLAGE'; Breadcrumb = 'Réglages' }
        @{ Prefix = 'status.';  Kind = 'status';  KindText = 'ÉTAT';    Breadcrumb = "Barre d'action" }
        @{ Prefix = 'action.';  Kind = 'action';  KindText = 'ACTION';  Breadcrumb = 'Actions' }
    )
    foreach ($m in $map) {
        if ($Key.StartsWith($m.Prefix)) {
            return [PSCustomObject]@{ Kind = $m.Kind; KindText = $m.KindText; Breadcrumb = $m.Breadcrumb }
        }
    }
    return [PSCustomObject]@{ Kind = 'general'; KindText = 'AIDE'; Breadcrumb = 'Général' }
}

# ---------------------------------------------------------------------------
# Get-HelpBadges : badges normalisés d'une rubrique, depuis son champ optionnel
# badges { reversible, data, duration }. PURE, fail-safe : champ absent,
# valeur inconnue ou vide = badge omis, jamais d'erreur (même contrat que le
# reste du catalogue). Ordre fixe : réversibilité, données, durée.
# ---------------------------------------------------------------------------
function Get-HelpBadges {
    [CmdletBinding()]
    param([Parameter(Mandatory)][object]$Entry)
    $badges = @()
    if (-not $Entry.PSObject.Properties['badges'] -or $null -eq $Entry.badges) { return $badges }
    $b = $Entry.badges
    $revMap = @{
        'yes'      = @{ Text = 'RÉVERSIBLE';               Level = 'Ok' }
        'store'    = @{ Text = 'RÉVERSIBLE VIA STORE';     Level = 'Accent' }
        'partial'  = @{ Text = 'PARTIELLEMENT RÉVERSIBLE'; Level = 'Warn' }
        'no'       = @{ Text = 'IRRÉVERSIBLE';             Level = 'Err' }
        'readonly' = @{ Text = 'LECTURE SEULE';            Level = 'Ok' }
    }
    $dataMap = @{
        'untouched' = @{ Text = 'DONNÉES PERSO : INTACTES';      Level = 'Ok' }
        'copyonly'  = @{ Text = 'DONNÉES PERSO : COPIE SEULE';   Level = 'Accent' }
        'optin'     = @{ Text = 'DONNÉES : SELON CASES COCHÉES'; Level = 'Warn' }
    }
    if ($b.PSObject.Properties['reversible']) {
        $v = [string]$b.reversible
        if ($revMap.ContainsKey($v)) {
            $badges += [PSCustomObject]@{ Text = $revMap[$v].Text; Level = $revMap[$v].Level }
        }
    }
    if ($b.PSObject.Properties['data']) {
        $v = [string]$b.data
        if ($dataMap.ContainsKey($v)) {
            $badges += [PSCustomObject]@{ Text = $dataMap[$v].Text; Level = $dataMap[$v].Level }
        }
    }
    if ($b.PSObject.Properties['duration']) {
        $v = [string]$b.duration
        if (-not [string]::IsNullOrWhiteSpace($v)) {
            $badges += [PSCustomObject]@{ Text = "DURÉE : $v"; Level = 'Neutral' }
        }
    }
    return $badges
}
```

- [ ] **Step 4: Vérifier le vert** - Pester ciblé puis jeu de portes complet (Global Constraints).
- [ ] **Step 5: Commit** - `git add lib/Help.ps1 tests/Help.Tests.ps1` puis `git commit -m "feat: fonctions pures d en-tete d aide (kind, breadcrumb, badges)"`.

---

### Task 2: En-tête contextuel dans le panneau d'aide (GUI)

**Files:**
- Modify: `Run-GUI.ps1` (création du panneau vers L439-440, fonction de rendu près de Show-KitHelp L753, branchements, zones de gel L468, SelfTest L2008+)
- Modify: `lib/Theme.ps1` (fonction pure `Get-KitBadgeLevelColors`)
- Test: `tests/Theme.Tests.ps1` (Describe couleurs), SelfTest (assertions en-tête)

**Interfaces:**
- Consumes: `Get-HelpHeaderInfo`, `Get-HelpBadges` (Task 1).
- Produces: `Get-KitBadgeLevelColors -Level <string>` -> `@{ Back; Fore }` (hex). `Update-KitHelpHeader -Key <string> -Entry <object>` (GUI, jamais d'exception). Contrôles `$script:HelpHeaderPanel`, `$script:HelpKindLabel`, `$script:HelpBreadcrumbLabel`, `$script:HelpBadgeLabels` (tableau de 3 Labels).

- [ ] **Step 1: Test pur (échec attendu)** - ajouter à `tests/Theme.Tests.ps1` :

```powershell
Describe 'Get-KitBadgeLevelColors' {
    It 'mappe chaque niveau sur des couleurs de la palette' {
        $p = Get-KitPalette
        (Get-KitBadgeLevelColors -Level 'Ok').Back     | Should -Be $p.Ok
        (Get-KitBadgeLevelColors -Level 'Accent').Back | Should -Be $p.Accent
        (Get-KitBadgeLevelColors -Level 'Warn').Back   | Should -Be $p.Warn
        (Get-KitBadgeLevelColors -Level 'Err').Back    | Should -Be $p.Err
    }
    It 'replie un niveau inconnu sur le badge neutre' {
        $p = Get-KitPalette
        $c = Get-KitBadgeLevelColors -Level 'Neutral'
        $c.Back | Should -Be $p.Line
        $c.Fore | Should -Be $p.InkSoft
        (Get-KitBadgeLevelColors -Level 'zzz').Back | Should -Be $p.Line
    }
}
```

- [ ] **Step 2: Implémenter `Get-KitBadgeLevelColors`** dans `lib/Theme.ps1` (après `Get-KitPalette`) :

```powershell
# Couleurs d'un badge d'aide selon son niveau. Blanc sur couleur pleine pour
# les niveaux signifiants, neutre discret pour le reste (durée).
function Get-KitBadgeLevelColors {
    [CmdletBinding()]
    param([Parameter(Mandatory)][string]$Level)
    $p = Get-KitPalette
    switch ($Level) {
        'Ok'     { return @{ Back = $p.Ok;     Fore = '#ffffff' } }
        'Accent' { return @{ Back = $p.Accent; Fore = '#ffffff' } }
        'Warn'   { return @{ Back = $p.Warn;   Fore = '#ffffff' } }
        'Err'    { return @{ Back = $p.Err;    Fore = '#ffffff' } }
        default  { return @{ Back = $p.Line;   Fore = $p.InkSoft } }
    }
}
```

- [ ] **Step 3: Créer le panneau d'en-tête** dans `Run-GUI.ps1`. Point d'insertion : juste APRÈS `$script:TabHelp.Controls.Add($script:HelpPinBar)` (L439) et AVANT `$txtHelp.BringToFront()` (L440) - cet ordre d'ajout place l'en-tête SOUS la barre d'épinglage, et le BringToFront existant garde le RichTextBox en Fill :

```powershell
# En-tête contextuel (v2.5) : type de l'élément, fil d'Ariane, badges. Ajouté
# après la barre d'épinglage, il se docke SOUS elle ; le BringToFront du
# RichTextBox ci-dessous lui laisse le reste (même logique de docking).
$script:HelpHeaderPanel = New-Object System.Windows.Forms.Panel
$script:HelpHeaderPanel.Dock = 'Top'
$script:HelpHeaderPanel.Height = 46
$script:HelpHeaderPanel.BackColor = ConvertTo-KitColor $script:Palette.Card

$script:HelpKindLabel = New-Object System.Windows.Forms.Label
$script:HelpKindLabel.AutoSize = $true
$script:HelpKindLabel.Font = New-Object System.Drawing.Font('Segoe UI', 7.5, [System.Drawing.FontStyle]::Bold)
$script:HelpKindLabel.ForeColor = [System.Drawing.Color]::White
$script:HelpKindLabel.BackColor = ConvertTo-KitColor $script:Palette.AccentDark
$script:HelpKindLabel.Padding = New-Object System.Windows.Forms.Padding(4, 2, 4, 2)
$script:HelpKindLabel.Location = New-Object System.Drawing.Point(8, 4)
$script:HelpHeaderPanel.Controls.Add($script:HelpKindLabel)

$script:HelpBreadcrumbLabel = New-Object System.Windows.Forms.Label
$script:HelpBreadcrumbLabel.AutoSize = $true
$script:HelpBreadcrumbLabel.Font = New-Object System.Drawing.Font('Segoe UI', 8.25)
$script:HelpBreadcrumbLabel.ForeColor = ConvertTo-KitColor $script:Palette.InkMuted
$script:HelpBreadcrumbLabel.Location = New-Object System.Drawing.Point(64, 6)   # recalé à chaque rendu
$script:HelpHeaderPanel.Controls.Add($script:HelpBreadcrumbLabel)

# Trois labels de badge recyclés à chaque rendu : jamais de création de
# contrôle au survol.
$script:HelpBadgeLabels = @()
for ($i = 0; $i -lt 3; $i++) {
    $bl = New-Object System.Windows.Forms.Label
    $bl.AutoSize = $true
    $bl.Visible = $false
    $bl.Font = New-Object System.Drawing.Font('Segoe UI', 7.5, [System.Drawing.FontStyle]::Bold)
    $bl.Padding = New-Object System.Windows.Forms.Padding(4, 2, 4, 2)
    $bl.Location = New-Object System.Drawing.Point(8, 24)
    $script:HelpBadgeLabels += $bl
    $script:HelpHeaderPanel.Controls.Add($bl)
}
$script:TabHelp.Controls.Add($script:HelpHeaderPanel)
```

- [ ] **Step 4: Étendre les zones de gel** - remplacer la liste de la boucle L468 :

```powershell
foreach ($helpZone in (@($script:TabHelp, $txtHelp, $script:HelpPinBar, $script:BtnHelpPin,
                         $script:HelpHeaderPanel, $script:HelpKindLabel, $script:HelpBreadcrumbLabel) + $script:HelpBadgeLabels)) {
    $helpZone.Add_MouseEnter({ $script:HelpHoverTimer.Stop() })
}
```

- [ ] **Step 5: Fonction de rendu + branchements.** Insérer AVANT `function Show-KitHelp` (L753) :

```powershell
# Update-KitHelpHeader : synchronise l'en-tête contextuel avec la rubrique
# affichée. Clé vide = retour à l'invite d'accueil (en-tête générique, aucun
# badge). Ne lève jamais : l'aide ne casse pas la GUI.
function Update-KitHelpHeader {
    [CmdletBinding()]
    param(
        [AllowEmptyString()][string]$Key = '',
        [AllowNull()][object]$Entry = $null
    )
    try {
        $lookup = if ([string]::IsNullOrWhiteSpace($Key)) { 'general' } else { $Key }
        $info = Get-HelpHeaderInfo -Key $lookup
        $script:HelpKindLabel.Text = $info.KindText
        $script:HelpBreadcrumbLabel.Text = $info.Breadcrumb
        $script:HelpBreadcrumbLabel.Location = New-Object System.Drawing.Point(($script:HelpKindLabel.Right + 8), 6)
        $badges = @()
        if ($null -ne $Entry) { $badges = @(Get-HelpBadges -Entry $Entry) }
        $x = 8
        for ($i = 0; $i -lt $script:HelpBadgeLabels.Count; $i++) {
            $bl = $script:HelpBadgeLabels[$i]
            if ($i -lt $badges.Count) {
                $bl.Text = $badges[$i].Text
                $colors = Get-KitBadgeLevelColors -Level $badges[$i].Level
                $bl.BackColor = ConvertTo-KitColor $colors.Back
                $bl.ForeColor = ConvertTo-KitColor $colors.Fore
                $bl.Location = New-Object System.Drawing.Point($x, 24)
                $bl.Visible = $true
                $x = $bl.Right + 6
            }
            else { $bl.Visible = $false }
        }
    }
    catch { }
}
```

Puis modifier `Show-KitHelp` (le try existant, L758-761) pour appeler le rendu :

```powershell
    try {
        $entry = Get-HelpEntry -Catalog $script:HelpCatalog -Key $Key
        $txtHelp.Text = Format-HelpPanel -Entry $entry
        Update-KitHelpHeader -Key $Key -Entry $entry
    }
```

Et `Show-KitProfileHelpCore` : dans le cas Description JSON (après le `$txtHelp.Text = Format-HelpPanel ...` du bloc `$desc`, L784-785), ajouter `Update-KitHelpHeader -Key "profile.$Name"` ; dans le cas repli final (`$txtHelp.Text = $script:HelpPrompt`, L788), ajouter `Update-KitHelpHeader -Key ''`. Enfin, juste après le bloc de création du panneau (Step 3), initialiser l'état d'accueil : `Update-KitHelpHeader -Key ''` - à placer APRÈS la définition de la fonction, donc en pratique : ajouter l'appel d'initialisation immédiatement après la définition de `Update-KitHelpHeader`.

- [ ] **Step 6: SelfTest** - à la fin du bloc SelfTest existant (après l'assertion `'aide : 16 titres Etape N conformes'`, L2008), ajouter :

```powershell
    # --- v2.5 : en-tete contextuel ---
    Show-KitHelp -Key 'module.03'
    Assert-KitSelfTest 'aide v2.5 : kind ETAPE' ($script:HelpKindLabel.Text -eq 'ÉTAPE')
    Assert-KitSelfTest 'aide v2.5 : breadcrumb Intervention > Etapes' ($script:HelpBreadcrumbLabel.Text -eq 'Intervention > Étapes')
    Show-KitHelp -Key 'action.run'
    Assert-KitSelfTest 'aide v2.5 : kind ACTION' ($script:HelpKindLabel.Text -eq 'ACTION')
    Update-KitHelpHeader -Key ''
    Assert-KitSelfTest 'aide v2.5 : repli accueil AIDE' ($script:HelpKindLabel.Text -eq 'AIDE')
```

(Les badges visibles sont testés en Task 4, une fois le catalogue doté du champ `badges`.)

- [ ] **Step 7: Vérifier** - jeu de portes complet, dont SelfTest 5.1 exit 0.
- [ ] **Step 8: Commit** - `git commit -m "feat: en-tete contextuel du panneau d aide (kind, fil d Ariane, badges)"`.

---

### Task 3: Ancre visuelle sur la timeline

**Files:**
- Modify: `lib/Theme.ps1` (`New-KitModuleRow` L423-487 : contrôle `AnchorStripe` ; nouvelle fonction `Set-KitRowHelpAnchor`)
- Modify: `Run-GUI.ps1` (`$script:HelpAnchorId`, fonction `Set-KitHelpAnchorByKey`, appels dans `Show-KitHelp` et `Show-KitProfileHelpCore`, SelfTest)
- Test: `tests/Theme.Tests.ps1`

**Interfaces:**
- Produces: `New-KitModuleRow` retourne en plus `AnchorStripe` (Panel 3 px, invisible par défaut). `Set-KitRowHelpAnchor -Row <row> -On <bool>`. GUI : `Set-KitHelpAnchorByKey -Key <string>` (clé non-module ou vide = tout retirer).

- [ ] **Step 1: Tests (échec attendu)** - ajouter à `tests/Theme.Tests.ps1` :

```powershell
Describe 'Ancre d aide de la timeline' {
    It 'New-KitModuleRow expose un AnchorStripe invisible par defaut' {
        $row = New-KitModuleRow -Index '5' -Name 'Test' -Mdl2Available $false
        $row.PSObject.Properties['AnchorStripe'] | Should -Not -BeNullOrEmpty
        $row.AnchorStripe.Visible | Should -BeFalse
        $row.AnchorStripe.Width | Should -Be 3
    }
    It 'Set-KitRowHelpAnchor pose et retire le lisere' {
        $row = New-KitModuleRow -Index '5' -Name 'Test' -Mdl2Available $false
        Set-KitRowHelpAnchor -Row $row -On $true
        $row.AnchorStripe.Visible | Should -BeTrue
        Set-KitRowHelpAnchor -Row $row -On $false
        $row.AnchorStripe.Visible | Should -BeFalse
    }
    It 'les etats d execution ne touchent pas l ancre' {
        $row = New-KitModuleRow -Index '5' -Name 'Test' -Mdl2Available $false
        Set-KitRowHelpAnchor -Row $row -On $true
        Set-KitModuleRowState -Row $row -State Running -Detail 'en cours' -Mdl2Available $false
        $row.AnchorStripe.Visible | Should -BeTrue
        Set-KitModuleRowState -Row $row -State Pending -Detail '' -Mdl2Available $false
        $row.AnchorStripe.Visible | Should -BeTrue
    }
}
```

- [ ] **Step 2: Implémenter dans `lib/Theme.ps1`.** Dans `New-KitModuleRow`, après la création de `$detail` (L469-478) et avant le `AddRange` (L480), créer le liseré ; l'ajouter à la liste puis l'envoyer au fond (il passe SOUS la case à cocher, dont le x=2 le chevauche d'un pixel) ; l'exposer dans l'objet retourné :

```powershell
    # Liseré d'ancrage (v2.5) : visible quand l'aide affichée est celle de
    # cette ligne. Sous les autres contrôles, jamais touché par les états.
    $stripe = New-Object System.Windows.Forms.Panel
    $stripe.Width = 3
    $stripe.Height = $panel.Height
    $stripe.Location = New-Object System.Drawing.Point(0, 0)
    $stripe.BackColor = ConvertTo-KitColor $p.Accent
    $stripe.Visible = $false

    $panel.Controls.AddRange(@($cbx, $glyph, $idLbl, $nameLbl, $detail, $stripe))
    $stripe.SendToBack()
```

(Le `AddRange` existant est REMPLACÉ par cette version à 6 contrôles.) Retour de la fonction : ajouter `AnchorStripe = $stripe` dans le PSCustomObject (L483-486). Puis, après `Set-KitModuleRowState` (fin de la fonction, vers L520+), ajouter :

```powershell
# Set-KitRowHelpAnchor : montre ou cache le liseré d'ancrage d'une ligne.
# Orthogonal aux états d'exécution : aucun Set-KitModuleRowState n'y touche.
function Set-KitRowHelpAnchor {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][object]$Row,
        [Parameter(Mandatory)][bool]$On
    )
    $Row.AnchorStripe.Visible = $On
}
```

- [ ] **Step 3: Câbler dans `Run-GUI.ps1`.** Près des variables d'état de l'aide (vers L93-98), ajouter `$script:HelpAnchorId = ''`. Insérer AVANT `Update-KitHelpHeader` (Task 2) :

```powershell
# Set-KitHelpAnchorByKey : pose le liseré sur la ligne de timeline dont l'aide
# est affichée, et le retire partout ailleurs. Une seule ancre à la fois ;
# clé non-module ou vide = aucune ligne marquée (l'en-tête identifie déjà
# l'élément). Épinglage et gel : l'aide ne change pas, l'ancre non plus.
function Set-KitHelpAnchorByKey {
    [CmdletBinding()]
    param([AllowEmptyString()][string]$Key = '')
    try {
        $newId = ''
        if ($Key -match '^module\.([0-9]{2})$' -and $script:ModuleRows.ContainsKey($Matches[1])) {
            $newId = $Matches[1]
        }
        if ($script:HelpAnchorId -eq $newId) { return }
        if ($script:HelpAnchorId -and $script:ModuleRows.ContainsKey($script:HelpAnchorId)) {
            Set-KitRowHelpAnchor -Row $script:ModuleRows[$script:HelpAnchorId] -On $false
        }
        if ($newId) { Set-KitRowHelpAnchor -Row $script:ModuleRows[$newId] -On $true }
        $script:HelpAnchorId = $newId
    }
    catch { }
}
```

Appels : dans `Show-KitHelp`, après `Update-KitHelpHeader ...` : `Set-KitHelpAnchorByKey -Key $Key`. Dans `Show-KitProfileHelpCore` : `Set-KitHelpAnchorByKey -Key ''` dans les deux cas d'écriture directe (Description et repli).

- [ ] **Step 4: SelfTest** - après les assertions Task 2 :

```powershell
    # --- v2.5 : ancre visuelle ---
    Show-KitHelp -Key 'module.03'
    Assert-KitSelfTest 'ancre v2.5 : posee sur la ligne 03' ($script:ModuleRows['03'].AnchorStripe.Visible)
    Assert-KitSelfTest 'ancre v2.5 : unique' (@($script:Modules | Where-Object { $script:ModuleRows[$_.Id].AnchorStripe.Visible }).Count -eq 1)
    Show-KitHelp -Key 'module.05'
    Assert-KitSelfTest 'ancre v2.5 : deplacee sur 05' ($script:ModuleRows['05'].AnchorStripe.Visible -and -not $script:ModuleRows['03'].AnchorStripe.Visible)
    Show-KitHelp -Key 'action.run'
    Assert-KitSelfTest 'ancre v2.5 : retiree sur une cle non module' (@($script:Modules | Where-Object { $script:ModuleRows[$_.Id].AnchorStripe.Visible }).Count -eq 0)
```

- [ ] **Step 5: Vérifier** - jeu de portes complet.
- [ ] **Step 6: Commit** - `git commit -m "feat: ancre visuelle - lisere sur la ligne dont l aide est affichee"`.

---

### Task 4: Badges dans le catalogue d'aide

**Files:**
- Modify: `config/help.fr.json` (insertion d'une ligne `"badges": {...},` juste APRÈS la ligne `"title"` de chaque entrée listée - édition TEXTE directe, PAS de round-trip ConvertTo-Json qui casserait la mise en forme)
- Test: `tests/Help.Tests.ps1` (validation du catalogue réel), SelfTest (badges visibles)

**Interfaces:**
- Consumes: le schéma `badges` de Task 1 (enums exacts).

- [ ] **Step 1: Test de validation du catalogue (échec attendu au 2e assert)** - ajouter à `tests/Help.Tests.ps1` :

```powershell
Describe 'Catalogue reel : badges valides' {
    BeforeAll {
        $script:cheminCatalogue = Join-Path $PSScriptRoot '..\config\help.fr.json'
        $script:catalogue = Get-HelpCatalog -Path $script:cheminCatalogue
    }
    It 'toutes les valeurs badges du catalogue sont des enums connus' {
        $revOk  = @('yes', 'store', 'partial', 'no', 'readonly')
        $dataOk = @('untouched', 'copyonly', 'optin')
        foreach ($key in $script:catalogue.Keys) {
            $e = $script:catalogue[$key]
            if (-not $e.PSObject.Properties['badges'] -or $null -eq $e.badges) { continue }
            $b = $e.badges
            if ($b.PSObject.Properties['reversible']) { [string]$b.reversible | Should -BeIn $revOk -Because "$key" }
            if ($b.PSObject.Properties['data'])       { [string]$b.data       | Should -BeIn $dataOk -Because "$key" }
        }
    }
    It 'chaque rubrique module porte des badges' {
        foreach ($id in @('00','01','16','02','03','04','05','06','07','08','09','11','12','13','15','10','14')) {
            $e = $script:catalogue["module.$id"]
            $e.PSObject.Properties['badges'] | Should -Not -BeNullOrEmpty -Because "module.$id"
        }
    }
    It 'le fichier catalogue n a pas de BOM' {
        $octets = [System.IO.File]::ReadAllBytes($script:cheminCatalogue)
        ($octets[0] -eq 0xEF -and $octets[1] -eq 0xBB) | Should -BeFalse
    }
}
```

- [ ] **Step 2: Insérer les lignes badges.** Pour chaque clé ci-dessous, insérer la ligne indiquée immédiatement après la ligne `"title": ...,` de l'entrée (indentation : 6 espaces, comme les autres champs). Valeurs dérivées des textes `reversible`/`protects` existants - ne PAS improviser :

```text
module.00  -> "badges": { "reversible": "readonly", "data": "untouched", "duration": "1-3 min" },
module.01  -> "badges": { "data": "copyonly", "duration": "10-30 min" },
module.16  -> "badges": { "data": "untouched", "duration": "2-5 min" },
module.02  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "5-20 min" },
module.03  -> "badges": { "reversible": "store", "data": "untouched", "duration": "5-15 min" },
module.04  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "2-5 min" },
module.05  -> "badges": { "reversible": "partial", "data": "untouched", "duration": "20-60+ min" },
module.06  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "10-20 min" },
module.07  -> "badges": { "reversible": "no", "data": "optin", "duration": "30-60 min" },
module.08  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "2 min" },
module.09  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "1-2 min" },
module.10  -> "badges": { "data": "untouched", "duration": "1 min" },
module.11  -> "badges": { "reversible": "partial", "data": "untouched", "duration": "2-5 min" },
module.12  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "1-3 min" },
module.13  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "1-2 min" },
module.14  -> "badges": { "data": "untouched", "duration": "1 min" },
module.15  -> "badges": { "reversible": "no", "data": "untouched", "duration": "1 min + redémarrage" },
option.backupdata    -> "badges": { "data": "copyonly", "duration": "10-30 min" },
option.scandefender  -> "badges": { "data": "untouched", "duration": "5-15 min" },
option.recycle       -> "badges": { "reversible": "no", "data": "optin", "duration": "secondes" },
option.winold        -> "badges": { "reversible": "no", "data": "untouched", "duration": "2-10 min" },
option.cache         -> "badges": { "reversible": "partial", "data": "untouched", "duration": "1-3 min" },
option.onedrive      -> "badges": { "reversible": "yes", "data": "untouched", "duration": "2 min" },
option.oem           -> "badges": { "reversible": "yes", "data": "untouched", "duration": "5-10 min" },
option.netreset      -> "badges": { "reversible": "no", "data": "untouched", "duration": "1 min + redémarrage" },
option.bitlockerkey  -> "badges": { "reversible": "yes", "data": "untouched", "duration": "secondes" },
option.dryrun        -> "badges": { "reversible": "readonly", "data": "untouched" },
action.delfiche      -> "badges": { "reversible": "no" },
```

(Aucun badge sur `debloat.*`, `account.*`, `profile.*` ni les autres `action.*` : réglages et boutons sans enjeu de réversibilité propre. Les rationnels non évidents : module.00 `readonly` = « lit uniquement » ; module.01 sans `reversible` = c'est le filet lui-même ; module.07 `no`+`optin` = temp irréversibles et corbeille/Windows.old selon cases ; module.11 `partial` = « un raccourci se recrée » ; option.cache `partial` = « se reconstitue seul ».)

- [ ] **Step 3: Vérifier l'absence de BOM et le parse** - `pwsh -NoProfile -Command "(Get-Content config/help.fr.json -Raw -Encoding UTF8 | ConvertFrom-Json).entries.'module.03'.badges.reversible"` doit afficher `store`.
- [ ] **Step 4: SelfTest badges** - après les assertions Task 3 :

```powershell
    Show-KitHelp -Key 'module.03'
    Assert-KitSelfTest 'badges v2.5 : trois badges visibles pour module.03' (@($script:HelpBadgeLabels | Where-Object { $_.Visible }).Count -eq 3)
    Assert-KitSelfTest 'badges v2.5 : premier badge = reversible via store' ($script:HelpBadgeLabels[0].Text -eq 'RÉVERSIBLE VIA STORE')
```

- [ ] **Step 5: Vérifier** - jeu de portes complet (le test « chaque module porte des badges » passe au vert).
- [ ] **Step 6: Commit** - `git commit -m "feat: badges de reversibilite, donnees et duree dans le catalogue d aide"`.

---

### Task 5: Filet de sauvegarde visible (détection commune + indicateur)

**Files:**
- Modify: `lib/Common.ps1` (après `Get-BackupSourceFolders`, vers L576+ : `Select-KitBackupCandidates`, `Get-KitExternalBackupVolumes`)
- Modify: `modules/01-Backup.ps1` (L96-108 : remplacer le bloc de détection inline par l'appel commun)
- Modify: `lib/Theme.ps1` (`Get-KitBackupNetState`, `Get-KitBackupNetDisplay`)
- Modify: `Run-GUI.ps1` (label barre d'action, `Update-KitBackupNetIndicator`, câblages, phase, binding aide, SelfTest)
- Modify: `config/help.fr.json` (rubrique `status.backupnet`)
- Test: `tests/Common.Tests.ps1`, `tests/Theme.Tests.ps1`

**Interfaces:**
- Produces: `Select-KitBackupCandidates -Volumes <object[]> -SystemDriveLetter <string> -SystemPathLetters <string[]>` -> tableau trié (Removable d'abord, puis lettre). `Get-KitExternalBackupVolumes` (wrapper Get-Volume). `Get-KitBackupNetState -Module01Checked <bool> -BackupDataChecked <bool> -Candidates <object[]>` -> `'off'|'none'|'internal'|'external'`. `Get-KitBackupNetDisplay -State -Letter -FreeGB` -> `@{ Text; Level }` (Level `Ok|Warn`).
- Consumes: `Update-KitActionBarLayout` (L727), `Set-KitActionPhase` (L1086), `$script:HostAction`, `$script:ModuleRows`, `$cbBackupData`, `$form`.

- [ ] **Step 1: Tests purs (échec attendu).** Dans `tests/Common.Tests.ps1` :

```powershell
Describe 'Select-KitBackupCandidates' {
    BeforeAll {
        function New-FauxVolume([string]$Letter, [string]$Type, [long]$Free = 10GB) {
            [PSCustomObject]@{ DriveLetter = [char]$Letter; DriveType = $Type; SizeRemaining = $Free }
        }
    }
    It 'exclut le volume systeme et les partitions portant un Windows' {
        $vols = @(
            (New-FauxVolume 'C' 'Fixed'), (New-FauxVolume 'D' 'Fixed'), (New-FauxVolume 'E' 'Removable')
        )
        $r = @(Select-KitBackupCandidates -Volumes $vols -SystemDriveLetter 'C' -SystemPathLetters @('C', 'D'))
        @($r | ForEach-Object { "$($_.DriveLetter)" }) | Should -Be @('E')
    }
    It 'trie les amovibles avant les partitions fixes' {
        $vols = @((New-FauxVolume 'D' 'Fixed'), (New-FauxVolume 'F' 'Removable'))
        $r = @(Select-KitBackupCandidates -Volumes $vols -SystemDriveLetter 'C' -SystemPathLetters @('C'))
        @($r | ForEach-Object { "$($_.DriveLetter)" }) | Should -Be @('F', 'D')
    }
    It 'ignore les volumes sans lettre et les types non geres' {
        $vols = @(
            [PSCustomObject]@{ DriveLetter = $null; DriveType = 'Fixed'; SizeRemaining = 1GB },
            [PSCustomObject]@{ DriveLetter = [char]'G'; DriveType = 'CD-ROM'; SizeRemaining = 1GB }
        )
        @(Select-KitBackupCandidates -Volumes $vols -SystemDriveLetter 'C').Count | Should -Be 0
    }
    It 'rend une liste vide sans candidat' {
        @(Select-KitBackupCandidates -Volumes @() -SystemDriveLetter 'C').Count | Should -Be 0
    }
}
```

Dans `tests/Theme.Tests.ps1` :

```powershell
Describe 'Get-KitBackupNetState et affichage' {
    It 'decoche = off, quel que soit le disque' {
        Get-KitBackupNetState -Module01Checked $false -BackupDataChecked $true -Candidates @() | Should -Be 'off'
        $vol = [PSCustomObject]@{ DriveType = 'Removable' }
        Get-KitBackupNetState -Module01Checked $true -BackupDataChecked $false -Candidates @($vol) | Should -Be 'off'
    }
    It 'aucun candidat = none' {
        Get-KitBackupNetState -Module01Checked $true -BackupDataChecked $true -Candidates @() | Should -Be 'none'
    }
    It 'meilleur candidat amovible = external, fixe = internal' {
        $rem = [PSCustomObject]@{ DriveType = 'Removable' }
        $fix = [PSCustomObject]@{ DriveType = 'Fixed' }
        Get-KitBackupNetState -Module01Checked $true -BackupDataChecked $true -Candidates @($rem, $fix) | Should -Be 'external'
        Get-KitBackupNetState -Module01Checked $true -BackupDataChecked $true -Candidates @($fix) | Should -Be 'internal'
    }
    It 'affiche chaque etat avec le bon niveau' {
        $d = Get-KitBackupNetDisplay -State 'external' -Letter 'E' -FreeGB 120.4
        $d.Text | Should -Be 'Filet : disque E: (120 Go libres)'
        $d.Level | Should -Be 'Ok'
        (Get-KitBackupNetDisplay -State 'internal' -Letter 'D').Level | Should -Be 'Warn'
        (Get-KitBackupNetDisplay -State 'none').Text | Should -Be 'Sans disque externe : fichiers perso non copiés'
        (Get-KitBackupNetDisplay -State 'off').Text | Should -Be 'Aucune sauvegarde des données prévue : aucun filet'
    }
}
```

- [ ] **Step 2: Implémenter dans `lib/Common.ps1`** (après `Get-BackupSourceFolders`) :

```powershell
# ---------------------------------------------------------------------------
# Select-KitBackupCandidates : filtre et trie les volumes candidats au backup
# data. PURE (volumes et lettres à écarter passés en paramètres). Tri :
# amovible d'abord, puis lettre - le backup vise une clé ou un disque USB,
# jamais le disque physique qui porte les données. DriveLetter est un [char]
# (Get-Volume) : les comparaisons passent par "$(...)" des deux côtés.
# ---------------------------------------------------------------------------
function Select-KitBackupCandidates {
    [CmdletBinding()]
    param(
        [AllowEmptyCollection()][object[]]$Volumes = @(),
        [string]$SystemDriveLetter = $env:SystemDrive.TrimEnd(':'),
        [AllowEmptyCollection()][string[]]$SystemPathLetters = @()
    )
    $out = @($Volumes | Where-Object {
        ($_.DriveType -eq 'Removable' -or $_.DriveType -eq 'Fixed') -and
        $_.DriveLetter -and
        "$($_.DriveLetter)" -ne $SystemDriveLetter
    } | Where-Object {
        $_.DriveType -eq 'Removable' -or ("$($_.DriveLetter)" -notin $SystemPathLetters)
    } | Sort-Object @{ Expression = { if ($_.DriveType -eq 'Removable') { 0 } else { 1 } } }, 'DriveLetter')
    return $out
}

# Get-KitExternalBackupVolumes : wrapper fin autour de Get-Volume. Les lettres
# portant un \Windows\System32 sont écartées comme partitions système.
function Get-KitExternalBackupVolumes {
    [CmdletBinding()]
    param()
    $vols = @(Get-Volume -ErrorAction SilentlyContinue)
    $sysLetters = @($vols | Where-Object { $_.DriveLetter } |
                    Where-Object { Test-Path "$($_.DriveLetter):\Windows\System32" } |
                    ForEach-Object { "$($_.DriveLetter)" })
    return (Select-KitBackupCandidates -Volumes $vols -SystemPathLetters $sysLetters)
}
```

- [ ] **Step 3: Refactorer `modules/01-Backup.ps1`.** Remplacer TOUT le bloc L96-108 (de `$externalVolumes = @(Get-Volume | Where-Object {` à la ligne de commentaire `# pour ne pas écrire le backup sur le même disque physique que les données.` incluse) par :

```powershell
$externalVolumes = @(Get-KitExternalBackupVolumes)
# Tri : un disque amovible (clé USB) passe avant une partition fixe (D: interne)
# pour ne pas écrire le backup sur le même disque physique que les données.
# Détection partagée avec le cockpit (indicateur de filet) : lib/Common.ps1.
```

Comportement identique sur poste standard ; l'exclusion du disque système passe de `'C'` codé en dur à `$env:SystemDrive` (aligné v2.4.1).

- [ ] **Step 4: Implémenter dans `lib/Theme.ps1`** (après `Get-RunSummaryText`) :

```powershell
# ---------------------------------------------------------------------------
# Get-KitBackupNetState : état du filet de sauvegarde des données.
#   'off'      : étape Sauvegarde ou option données décochée - rien ne tournera
#   'none'     : aucun volume candidat détecté
#   'internal' : meilleur candidat = partition fixe (pas un support amovible)
#   'external' : disque amovible présent
# ---------------------------------------------------------------------------
function Get-KitBackupNetState {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][bool]$Module01Checked,
        [Parameter(Mandatory)][bool]$BackupDataChecked,
        [AllowEmptyCollection()][object[]]$Candidates = @()
    )
    if (-not $Module01Checked -or -not $BackupDataChecked) { return 'off' }
    if ($Candidates.Count -eq 0) { return 'none' }
    if ($Candidates[0].DriveType -eq 'Removable') { return 'external' }
    return 'internal'
}

# Texte et niveau de l'indicateur de filet, par état.
function Get-KitBackupNetDisplay {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][ValidateSet('off', 'none', 'internal', 'external')][string]$State,
        [AllowEmptyString()][string]$Letter = '',
        [double]$FreeGB = 0
    )
    switch ($State) {
        'external' { return @{ Text = "Filet : disque $Letter`: ($([math]::Round($FreeGB, 0)) Go libres)"; Level = 'Ok' } }
        'internal' { return @{ Text = "Filet : partition interne $Letter`: - vérifier le support"; Level = 'Warn' } }
        'none'     { return @{ Text = 'Sans disque externe : fichiers perso non copiés'; Level = 'Warn' } }
        'off'      { return @{ Text = 'Aucune sauvegarde des données prévue : aucun filet'; Level = 'Warn' } }
    }
}
```

- [ ] **Step 5: GUI.** (a) Créer le label juste après la création de `$script:ActionSummary` (L679-684) :

```powershell
# Indicateur du filet de sauvegarde (v2.5) : vert quand un disque externe est
# prêt à recevoir la copie des dossiers personnels, orange sinon. Rafraîchi à
# l'ouverture, au retour de focus et au lancement.
$script:BackupNetLabel = New-Object System.Windows.Forms.Label
$script:BackupNetLabel.AutoSize = $true
$script:BackupNetLabel.Location = New-Object System.Drawing.Point(14, 18)   # recalé par le layout
$script:BackupNetLabel.Font = New-Object System.Drawing.Font('Segoe UI', 9)
$script:BackupNetLabel.ForeColor = ConvertTo-KitColor $script:Palette.InkSoft
$script:HostAction.Controls.Add($script:BackupNetLabel)
```

(b) Dans `Update-KitActionBarLayout` (L727), remplacer la ligne `$left = $script:ActionSummary.Right + 20` par :

```powershell
    $script:BackupNetLabel.Location = New-Object System.Drawing.Point(($script:ActionSummary.Right + 20), 18)
    $left = if ($script:BackupNetLabel.Visible) { $script:BackupNetLabel.Right + 20 } else { $script:ActionSummary.Right + 20 }
```

(c) Près des variables d'état (L93-98) : `$script:BackupNetState = 'none'`, `$script:BackupNetLastRefresh = [DateTime]::MinValue`. (d) Définir après `Update-KitActionSummary` (L1071-1084) :

```powershell
# Update-KitBackupNetIndicator : rafraîchit l'indicateur de filet. Garde
# anti-rafale de 2 s (Form.Activated se déclenche à chaque retour de focus) ;
# -Force la contourne pour les changements de cases et le lancement.
function Update-KitBackupNetIndicator {
    [CmdletBinding()]
    param([switch]$Force)
    try {
        if (-not $Force -and ((Get-Date) - $script:BackupNetLastRefresh).TotalSeconds -lt 2) { return }
        $script:BackupNetLastRefresh = Get-Date
        $candidates = @(Get-KitExternalBackupVolumes)
        $m01 = $script:ModuleRows.ContainsKey('01') -and $script:ModuleRows['01'].CheckBox.Checked
        $state = Get-KitBackupNetState -Module01Checked $m01 -BackupDataChecked $cbBackupData.Checked -Candidates $candidates
        $script:BackupNetState = $state
        $letter = ''
        $free = 0.0
        if ($candidates.Count -gt 0) {
            $letter = "$($candidates[0].DriveLetter)"
            $free = [double]($candidates[0].SizeRemaining) / 1GB
        }
        $d = Get-KitBackupNetDisplay -State $state -Letter $letter -FreeGB $free
        $script:BackupNetLabel.Text = $d.Text
        $couleur = if ($d.Level -eq 'Ok') { $script:Palette.Ok } else { $script:Palette.Warn }
        $script:BackupNetLabel.ForeColor = ConvertTo-KitColor $couleur
        Update-KitActionBarLayout
    }
    catch { }
}
```

(e) Câblages et détection initiale, à placer IMMÉDIATEMENT APRÈS la définition de `Update-KitBackupNetIndicator` (jamais avant : l'appel initial s'exécute au chargement du fichier, la fonction doit déjà exister ; à ce point, `$form`, `$toolTip`, `Request-KitHelp`, `$script:ModuleRows` et les contrôles existent tous) :

```powershell
# Filet de sauvegarde : détection au chargement, au retour de focus (garde
# anti-rafale) et quand la sauvegarde se coche ou se décoche.
$form.Add_Activated({ Update-KitBackupNetIndicator })
$cbBackupData.Add_CheckedChanged({ Update-KitBackupNetIndicator -Force })
$script:ModuleRows['01'].CheckBox.Add_CheckedChanged({ Update-KitBackupNetIndicator -Force })
$script:BackupNetLabel.Add_MouseEnter({ Request-KitHelp -Key 'status.backupnet' })
$toolTip.SetToolTip($script:BackupNetLabel, (Format-HelpTooltip -Entry (Get-HelpEntry -Catalog $script:HelpCatalog -Key 'status.backupnet') -Width 90))
Update-KitBackupNetIndicator -Force
```

(f) Dans `Set-KitActionPhase` : phase `Prepare` -> `$script:BackupNetLabel.Visible = $true` ; phases `Running` et `Done` -> `$script:BackupNetLabel.Visible = $false` (le layout suit via les appels existants).

- [ ] **Step 6: Rubrique catalogue.** Ajouter à `config/help.fr.json`, après l'entrée `option.dryrun` :

```json
    "status.backupnet": {
      "title": "Filet de sauvegarde des données",
      "short": "Indique si les dossiers personnels seront copiés avant l'intervention, et vers quel disque.",
      "what": "Le point de restauration Windows ne protège pas les fichiers personnels : seul un disque externe branché permet d'en garder une copie qui survivrait à une panne pendant l'intervention. Cet indicateur se met à jour à l'ouverture du cockpit, quand la fenêtre reprend le focus et quand les cases de sauvegarde changent. Il passe à l'orange si aucun support externe n'est détecté ou si la sauvegarde est décochée.",
      "protects": "La copie est purement additive : rien n'est déplacé ni supprimé des dossiers d'origine.",
      "reversible": "Sans objet, il s'agit d'un indicateur.",
      "duration": "Sans objet",
      "whenNot": "Sans objet."
    },
```

- [ ] **Step 7: SelfTest** - après les assertions Task 4 :

```powershell
    # --- v2.5 : indicateur de filet ---
    Update-KitBackupNetIndicator -Force   # la fenetre n'est pas forcement affichee en SelfTest : detection explicite
    Assert-KitSelfTest 'filet v2.5 : indicateur renseigne' (-not [string]::IsNullOrWhiteSpace($script:BackupNetLabel.Text))
    Assert-KitSelfTest 'filet v2.5 : etat connu' ($script:BackupNetState -in @('off', 'none', 'internal', 'external'))
    $etatFiletAvant = $script:BackupNetState
    $cbBackupData.Checked = $false
    Assert-KitSelfTest 'filet v2.5 : decocher bascule sur off' ($script:BackupNetState -eq 'off')
    $cbBackupData.Checked = $true
    Assert-KitSelfTest 'filet v2.5 : recocher restaure la detection' ($script:BackupNetState -eq $etatFiletAvant)
```

- [ ] **Step 8: Vérifier** - jeu de portes complet (dont Pester Common/Theme et SelfTest 5.1).
- [ ] **Step 9: Commit** - `git commit -m "feat: filet de sauvegarde visible - detection commune et indicateur de barre d action"`.

---

### Task 6: Vérité avant le Go + micro-copie de la politique de débloatage

**Files:**
- Modify: `lib/Theme.ps1` (`Get-DebloatPolicyHint`, `Get-KitPreRunWarnings`)
- Modify: `Run-GUI.ps1` (hint sous la ComboBox avec réagencement de la carte Réglages L255-328, confirmation conditionnelle dans `$btnRun.Add_Click` L1393+, SelfTest)
- Modify: `config/help.fr.json` (`debloat.standard` réécrit)
- Test: `tests/Theme.Tests.ps1`

**Interfaces:**
- Produces: `Get-DebloatPolicyHint -PolicyFr <string>` -> texte court (`''` si inconnu). `Get-KitPreRunWarnings -BackupState <string> -Module03Checked <bool> -Policy <string> -IsDryRun <bool>` -> tableau de lignes (vide = pas de confirmation).
- Consumes: `$script:BackupNetState` (Task 5), `$cmbDebloat`, `$script:ModuleRows`, `$cbDryRun`.

- [ ] **Step 1: Tests purs (échec attendu)** - ajouter à `tests/Theme.Tests.ps1` :

```powershell
Describe 'Get-DebloatPolicyHint' {
    It 'donne une consequence courte par politique' {
        Get-DebloatPolicyHint -PolicyFr 'Conservateur' | Should -Be 'Aucune app douteuse supprimée'
        Get-DebloatPolicyHint -PolicyFr 'Standard'     | Should -Be 'Non utilisée depuis 90 j = retirée sans question'
        Get-DebloatPolicyHint -PolicyFr 'Agressif'     | Should -Be 'Les apps douteuses sont retirées, même utilisées'
    }
    It 'rend une chaine vide pour une politique inconnue' {
        Get-DebloatPolicyHint -PolicyFr 'zzz' | Should -Be ''
        Get-DebloatPolicyHint -PolicyFr ''    | Should -Be ''
    }
}

Describe 'Get-KitPreRunWarnings' {
    It 'ne dit rien en simulation, meme avec tout a signaler' {
        @(Get-KitPreRunWarnings -BackupState 'none' -Module03Checked $true -Policy 'Aggressive' -IsDryRun $true).Count | Should -Be 0
    }
    It 'ne dit rien quand filet externe et debloatage conservateur' {
        @(Get-KitPreRunWarnings -BackupState 'external' -Module03Checked $true -Policy 'Conservative' -IsDryRun $false).Count | Should -Be 0
    }
    It 'signale chaque etat de filet degrade' {
        foreach ($etat in @('none', 'internal', 'off')) {
            $w = @(Get-KitPreRunWarnings -BackupState $etat -Module03Checked $false -Policy 'Standard' -IsDryRun $false)
            $w.Count | Should -Be 1 -Because $etat
        }
    }
    It 'signale la suppression silencieuse en Standard et l agressif, pas sans module 03' {
        $w = @(Get-KitPreRunWarnings -BackupState 'external' -Module03Checked $true -Policy 'Standard' -IsDryRun $false)
        $w.Count | Should -Be 1
        $w[0] | Should -Match '90 j'
        $w = @(Get-KitPreRunWarnings -BackupState 'external' -Module03Checked $true -Policy 'Aggressive' -IsDryRun $false)
        $w[0] | Should -Match 'même utilisées'
        @(Get-KitPreRunWarnings -BackupState 'external' -Module03Checked $false -Policy 'Aggressive' -IsDryRun $false).Count | Should -Be 0
    }
    It 'cumule filet et debloatage' {
        @(Get-KitPreRunWarnings -BackupState 'none' -Module03Checked $true -Policy 'Standard' -IsDryRun $false).Count | Should -Be 2
    }
}
```

- [ ] **Step 2: Implémenter dans `lib/Theme.ps1`** (après `Get-KitBackupNetDisplay`) :

```powershell
# Conséquence courte de la politique de débloatage, affichée sous la liste.
# Textes courts : la carte Réglages fait 301 px utiles à la taille mini.
function Get-DebloatPolicyHint {
    [CmdletBinding()]
    param([AllowEmptyString()][string]$PolicyFr = '')
    switch ($PolicyFr) {
        'Conservateur' { return 'Aucune app douteuse supprimée' }
        'Standard'     { return 'Non utilisée depuis 90 j = retirée sans question' }
        'Agressif'     { return 'Les apps douteuses sont retirées, même utilisées' }
        default        { return '' }
    }
}

# ---------------------------------------------------------------------------
# Get-KitPreRunWarnings : lignes de la confirmation avant lancement RÉEL.
# Liste vide = aucune confirmation (rien à signaler = aucune friction).
# En simulation, jamais de confirmation. PURE.
# ---------------------------------------------------------------------------
function Get-KitPreRunWarnings {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][ValidateSet('off', 'none', 'internal', 'external')][string]$BackupState,
        [Parameter(Mandatory)][bool]$Module03Checked,
        [AllowEmptyString()][string]$Policy = 'Standard',
        [Parameter(Mandatory)][bool]$IsDryRun
    )
    if ($IsDryRun) { return @() }
    $w = @()
    switch ($BackupState) {
        'none'     { $w += 'Aucun disque externe branché : les fichiers personnels ne seront pas copiés (le point de restauration ne les couvre pas).' }
        'internal' { $w += 'La sauvegarde partirait sur une partition interne : brancher un vrai disque externe est plus sûr.' }
        'off'      { $w += 'La sauvegarde des données est décochée : aucun filet pour les fichiers personnels.' }
    }
    if ($Module03Checked) {
        if ($Policy -eq 'Standard') {
            $w += 'Débloatage standard : les apps douteuses non utilisées depuis 90 j seront supprimées sans question (réinstallables via le Store).'
        }
        elseif ($Policy -eq 'Aggressive') {
            $w += 'Débloatage agressif : les apps douteuses seront supprimées même utilisées (sauf Game Pass).'
        }
    }
    return $w
}
```

- [ ] **Step 3: Hint sous la ComboBox + réagencement de la carte Réglages** (`Run-GUI.ps1` L255-328). Ajouter après le bloc `$cmbDebloat` (L274) :

```powershell
# Conséquence de la politique choisie, toujours visible (v2.5) : le mot
# « Agressif » ne porte plus seul. Mise à jour au changement de sélection.
$lblDebloatHint = New-Object System.Windows.Forms.Label
$lblDebloatHint.AutoSize = $true
$lblDebloatHint.Font = New-Object System.Drawing.Font('Segoe UI', 8.25)
$lblDebloatHint.ForeColor = ConvertTo-KitColor $script:Palette.InkMuted
$lblDebloatHint.Location = New-Object System.Drawing.Point(12, 52)
$lblDebloatHint.Text = Get-DebloatPolicyHint -PolicyFr 'Standard'
$cardSettings.Controls.Add($lblDebloatHint)
```

Puis décaler les contrôles suivants de la carte (Y uniquement, X inchangés) : `$rbStd` 58 -> 74 ; `$rbKeep` 80 -> 94 ; `$cbBackupData` 106 -> 116 ; `$cbScanDefender` 128 -> 136 ; `$cbDryRun` 150 -> 156. (Le bas de carte reste dans les 190 px : 156 + ~20 = 176.)

Dans le handler existant `$cmbDebloat.Add_SelectedIndexChanged` (L901-905), ajouter la mise à jour du hint :

```powershell
$cmbDebloat.Add_SelectedIndexChanged({
    # Choisir une politique est un acte volontaire : l'aide suit sans délai.
    $lblDebloatHint.Text = Get-DebloatPolicyHint -PolicyFr ([string]$cmbDebloat.SelectedItem)
    $k = $debloatKeys[[string]$cmbDebloat.SelectedItem]
    if ($k) { Request-KitHelp -Key $k -Source Direct }
})
```

- [ ] **Step 4: Confirmation conditionnelle avant lancement.** Dans `$btnRun.Add_Click` (L1393+), insérer APRÈS le bloc « fiche étrangère » (après son `if ($r -eq 'No') { return }`, L1411-1412) et AVANT `Build-Queue` (L1413) :

```powershell
    # Vérité avant lancement (v2.5) : en intervention réelle, tout ce qui
    # mérite d'être su s'annonce en UNE confirmation. Rien à signaler = aucune
    # friction. En simulation, jamais de confirmation.
    Update-KitBackupNetIndicator -Force
    $policyMapPre = @{ 'Conservateur' = 'Conservative'; 'Standard' = 'Standard'; 'Agressif' = 'Aggressive' }
    $m03 = $script:ModuleRows.ContainsKey('03') -and $script:ModuleRows['03'].CheckBox.Checked
    $preRunWarnings = @(Get-KitPreRunWarnings -BackupState $script:BackupNetState -Module03Checked $m03 `
        -Policy ([string]$policyMapPre[[string]$cmbDebloat.SelectedItem]) -IsDryRun $cbDryRun.Checked)
    if ($preRunWarnings.Count -gt 0) {
        $msg = "Avant de lancer :`r`n`r`n- " + ($preRunWarnings -join "`r`n`r`n- ") + "`r`n`r`nContinuer ?"
        $r = [System.Windows.Forms.MessageBox]::Show($msg, 'PC-Refresh-Kit - avant de lancer', 'YesNo', 'Warning')
        if ($r -eq 'No') { return }
    }
```

- [ ] **Step 5: Réécrire `debloat.standard`** dans `config/help.fr.json` (remplacer les champs `what` et `whenNot`, les autres inchangés) :

```json
      "what": "L'usage récent est mesuré sur les fichiers de l'application. Une application utilisée ces trois derniers mois est conservée. Depuis le cockpit, les autres sont retirées sans question posée : la liste ne compte que cinq groupes (Xbox, Spotify, Lien avec le téléphone, Skype, Films et TV), tous réinstallables gratuitement depuis le Microsoft Store. La confirmation avant lancement le rappelle.",
      "whenNot": "Sur la machine d'une personne qui utilise des applications de façon saisonnière : la politique conservatrice les garde toutes."
```

- [ ] **Step 6: SelfTest** - après les assertions Task 5 :

```powershell
    # --- v2.5 : hint de politique ---
    Assert-KitSelfTest 'hint v2.5 : suit la selection courante' ($lblDebloatHint.Text -eq (Get-DebloatPolicyHint -PolicyFr ([string]$cmbDebloat.SelectedItem)))
    $cmbDebloat.SelectedIndex = 2
    Assert-KitSelfTest 'hint v2.5 : agressif annonce la suppression' ($lblDebloatHint.Text -eq 'Les apps douteuses sont retirées, même utilisées')
    $cmbDebloat.SelectedIndex = 1
    Assert-KitSelfTest 'hint v2.5 : retour standard' ($lblDebloatHint.Text -eq (Get-DebloatPolicyHint -PolicyFr 'Standard'))
```

(Aucun SelfTest ne clique LANCER : la confirmation modale n'est couverte que par ses fonctions pures, testées Pester.)

- [ ] **Step 7: Vérifier** - jeu de portes complet. Contrôle visuel manuel facultatif : `powershell -ExecutionPolicy Bypass -File .\Run-GUI.ps1 -UiPreview`.
- [ ] **Step 8: Commit** - `git commit -m "feat: verite avant lancement et consequence visible de la politique de debloatage"`.

---

### Task 7: Passe confiance du catalogue + version v2.5.0

**Files:**
- Modify: `config/help.fr.json` (`module.01`, `module.03`, `option.backupdata`)
- Modify: `lib/Report.ps1` (`Get-KitVersion`), `tools/Build-ReleaseZip.ps1` (version par défaut), `tests/Common.Tests.ps1` (assertion de version)

**Interfaces:**
- Consumes: rien de nouveau. Aucun identifiant renommé (profils, logs, rapports intacts).

- [ ] **Step 1: Retouches catalogue.** Dans `config/help.fr.json` :

`module.01` - remplacer `short` et `what` (les autres champs inchangés) :

```json
      "short": "Copie les dossiers personnels vers un disque externe - jamais déplacés ni supprimés - et crée un point de restauration Windows.",
      "what": "Crée un point de restauration système, puis copie Documents, Bureau, Images, Téléchargements, Vidéos et Musique vers le disque externe branché. Pourquoi un disque externe : le point de restauration Windows ne protège pas les fichiers personnels, et une copie laissée sur le même disque disparaîtrait avec lui en cas de panne pendant l'intervention. Le disque externe est le seul filet qui survit.",
```

`module.03` - remplacer `short` :

```json
      "short": "Retire des applications préinstallées d'une liste fermée - jamais vos logiciels installés.",
```

`option.backupdata` - remplacer `short` :

```json
      "short": "Copie les dossiers personnels vers le disque externe branché - une copie, jamais un déplacement.",
```

- [ ] **Step 2: Version v2.5.0.**
  - `lib/Report.ps1` : `function Get-KitVersion { return 'v2.4.1' }` -> `return 'v2.5.0'`.
  - `tools/Build-ReleaseZip.ps1` : `[string]$Version = "2.4.1"` -> `"2.5.0"`.
  - `tests/Common.Tests.ps1` : l'assertion `Get-KitVersion | Should -Be 'v2.4.1'` -> `'v2.5.0'` (le test de concordance Build-ReleaseZip/Get-KitVersion existant suit tout seul).

- [ ] **Step 3: Relever le plancher d'assertions du SelfTest.** Les Tasks 2 à 6 ont ajouté 22 assertions aux 27 existantes : 17 prévues au plan (4+4+2+4+3) plus 5 nées des revues (ordre de docking T2, témoin d'unicité et extinction T3, extinction des badges T4, géométrie du filet T5). Dans `Run-GUI.ps1` : `if ($script:SelfTestCount -lt 27)` -> `-lt 49`, et le message `"... sur 27 attendues"` -> `"... sur 49 attendues"`. Le plancher garantit qu'aucune assertion du nouvel oracle n'est sautée silencieusement.

- [ ] **Step 4: Vérifier** - jeu de portes COMPLET (Pester entier, parse, encodage, anonymat, smoke, SelfTest : `[SELFTEST] SUCCES : parcours complet (49 assertions)`). Vérifier aussi : `pwsh -NoProfile -Command "(Get-Content config/help.fr.json -Raw -Encoding UTF8 | ConvertFrom-Json).entries.'module.01'.what"` contient « Pourquoi un disque externe ».
- [ ] **Step 5: Commit** - `git commit -m "feat: passe confiance du catalogue et version v2.5.0"`.

---

## Après les tâches

1. Revue whole-branch (subagent Opus, superpowers:requesting-code-review), périmètre `public-main..HEAD`.
2. Correctifs éventuels, re-revue, portes complètes.
3. Anonymat historique : `pwsh -NoProfile -File .\tests\Test-KitAnonymity.ps1 -CI -History` -> 0.
4. STOP : rapport à l'utilisateur. Squash vers `public-main`, notes de release `docs/RELEASE-NOTES-v2.5.0.md` et release GitHub v2.5.0 UNIQUEMENT après accord explicite.
