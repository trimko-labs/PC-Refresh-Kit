# v2.3 Passe UX terrain - plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corriger les six griefs UX du premier test terrain de la v2.2 (spec docs/superpowers/specs/2026-08-22-passe-ux-terrain-design.md) et ajouter le garde-fou de parcours scripté qui manquait au process.

**Architecture:** Tout se joue dans Run-GUI.ps1 (comportements), lib/Help.ps1 (décision d'aide pure), lib/Theme.ps1 (ligne de timeline), config/help.fr.json (textes). Aucun module de modules/ n'est touché. Un nouveau mode `-SelfTest` de Run-GUI.ps1 déroule le parcours utilisateur sans afficher la fenêtre et entre en CI.

**Tech Stack:** PowerShell 5.1, WinForms, Pester 5 (sous pwsh), CI GitHub windows-latest.

## Global Constraints

- PowerShell 5.1 + `Set-StrictMode -Version Latest` : garder les gardes `PSObject.Properties`, jamais d'accès à une propriété non garantie.
- Contrats collections EXISTANTS par fonction : ne pas changer un contrat (return nu + appelant `@(...)` vs `return ,@()`).
- Tous les fichiers suivis : BOM UTF-8 + newline finale + zéro U+2014/U+2013 (CI Encoding). Accents français corrects dans tout texte visible.
- Messages de commit ASCII sans accents ni apostrophes (hook).
- JAMAIS exécuter le kit en réel : pas d'élévation, pas de clic LANCER, validation GUI par `-UiPreview`/`-SelfTest` uniquement. Lancer Run-GUI avec `powershell.exe` (5.1), jamais pwsh.
- Anonymat dépôt public : aucun nom de personne, aucun chemin C:\Users\<nom>, aucun e-mail hors trimko.com dans les fichiers suivis (`.\tests\Test-KitAnonymity.ps1 -CI`).
- Tout nouveau glyphe Segoe MDL2 : présence vérifiée dans segmdl2.ttf par GlyphTypeface AVANT usage, repli texte sinon (leçon v2.2 : U+25CB absent).
- Suite Pester complète verte à la fin de chaque tâche : `pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"` (365 tests au départ ; les tâches en ajoutent/adaptent).
- Après toute modification de .ps1 : `powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI` puis `powershell -ExecutionPolicy Bypass -File .\tests\Test-KitEncoding.ps1 -CI` (0 violation).
- vendor/TelemetryGuard : intouchable.
- Libellé sentinelle profils : EXACTEMENT `(personnalisé)` ; nom passé au résumé : EXACTEMENT `personnalisé`.
- Table étapes/labels français (spec §4) : 1 Diagnostic (00), 2 Sauvegarde (01), 3 Antivirus (02), 4 Désencombrement (03), 5 Confidentialité (04), 6 Mises à jour (05), 7 Logiciels (06), 8 Nettoyage (07), 9 Comptes (08), 10 Confort (09), 11 Nettoyage profond (11), 12 Démarrage (12), 13 Navigateurs (13), 14 Réseau (15), 15 Rapport (10).

Repères de lignes = état au début de la vague (HEAD 1a5e028). Ils glissent au fil des tâches : chercher le motif cité, pas la ligne aveugle.

---

### Task 1: Étapes 1 à 15 et noms français

**Files:**
- Modify: `Run-GUI.ps1:36-52` ($script:Modules), `:181-191` (boucle timeline), `:1196-1197` (libellé du run)
- Modify: `lib/Theme.ps1:421-460` (New-KitModuleRow)
- Modify: `config/help.fr.json` (15 titres module.NN + corps), `README.md` (une phrase)
- Test: `tests/Theme.Tests.ps1` (usages New-KitModuleRow), `tests/Help.Tests.ps1` si des titres y sont assertés

**Interfaces:**
- Produces: `$script:Modules` avec champ `Label` (français) en plus de `Id`/`Name`/`File` ; `New-KitModuleRow -Index <string> -Name <string> -Mdl2Available <bool>` (le paramètre `-Id` est renommé `-Index` et reçoit le numéro d'étape « 1 ».. « 15 », `-Name` reçoit le Label français). Le retour (hashtable .Panel/.CheckBox/.Glyph/.IdLabel/.NameLabel/.Detail) est inchangé.
- Consumes: rien des autres tâches.

- [ ] **Step 1 : Ajouter les labels français à $script:Modules**

Remplacer le bloc `Run-GUI.ps1:36-52` par (ordre inchangé = ordre d'exécution) :

```powershell
$script:Modules     = @(
    [PSCustomObject]@{ Id='00'; Name='Diagnostic'; Label='Diagnostic';        File='00-Diagnostic.ps1' }
    [PSCustomObject]@{ Id='01'; Name='Backup';     Label='Sauvegarde';        File='01-Backup.ps1' }
    [PSCustomObject]@{ Id='02'; Name='Antivirus';  Label='Antivirus';         File='02-Antivirus.ps1' }
    [PSCustomObject]@{ Id='03'; Name='Debloat';    Label='Désencombrement';   File='03-Debloat.ps1' }
    [PSCustomObject]@{ Id='04'; Name='Privacy';    Label='Confidentialité';   File='04-Privacy.ps1' }
    [PSCustomObject]@{ Id='05'; Name='Updates';    Label='Mises à jour';      File='05-Updates.ps1' }
    [PSCustomObject]@{ Id='06'; Name='Software';   Label='Logiciels';         File='06-Software.ps1' }
    [PSCustomObject]@{ Id='07'; Name='Cleanup';    Label='Nettoyage';         File='07-Cleanup.ps1' }
    [PSCustomObject]@{ Id='08'; Name='Accounts';   Label='Comptes';           File='08-Accounts.ps1' }
    [PSCustomObject]@{ Id='09'; Name='Comfort';    Label='Confort';           File='09-Comfort.ps1' }
    [PSCustomObject]@{ Id='11'; Name='DeepClean';  Label='Nettoyage profond'; File='11-DeepClean.ps1' }
    [PSCustomObject]@{ Id='12'; Name='Startup';    Label='Démarrage';         File='12-Startup.ps1' }
    [PSCustomObject]@{ Id='13'; Name='BrowserPUP'; Label='Navigateurs';       File='13-BrowserPUP.ps1' }
    [PSCustomObject]@{ Id='15'; Name='Network';    Label='Réseau';            File='15-Network.ps1' }
    [PSCustomObject]@{ Id='10'; Name='Report';     Label='Rapport';           File='10-Report.ps1' }
)
```

`Id`/`Name`/`File` restent : Build-Queue, logs, profils (Modules indexés par Id) et le rapport en dépendent.

- [ ] **Step 2 : Test Pester de la nouvelle signature de New-KitModuleRow**

Inventorier les usages : `grep -n "New-KitModuleRow" tests/Theme.Tests.ps1`. Adapter chaque appel `-Id '<xx>'` en `-Index '<n>'` (mêmes assertions par ailleurs) et ajouter dans le Describe de New-KitModuleRow :

```powershell
It 'affiche le numero d etape et le label francais' {
    $row = New-KitModuleRow -Index '15' -Name 'Rapport' -Mdl2Available $false
    $row.IdLabel.Text  | Should -Be '15'
    $row.NameLabel.Text | Should -Be 'Rapport'
}
```

Lancer : `pwsh -NoProfile -Command "Invoke-Pester -Path .\tests\Theme.Tests.ps1 -CI"` - le nouveau test DOIT échouer (paramètre -Index inconnu) avant le Step 3.

- [ ] **Step 3 : Renommer le paramètre dans New-KitModuleRow**

Dans `lib/Theme.ps1:421-457` : `[Parameter(Mandatory)][string]$Id` devient `[Parameter(Mandatory)][string]$Index`, et `$idLbl.Text = $Id` devient `$idLbl.Text = $Index`. Rien d'autre ne change (tailles, positions, retour). Le commentaire de tête (:422) devient : « Ligne de la timeline d'intervention : case (préparation) OU glyphe d'état (run/clôture), numéro d'étape grisé, nom français, détail droit (durée/état). »

- [ ] **Step 4 : Boucle de construction : numéro d'étape + label**

Dans `Run-GUI.ps1:181-191`, la boucle devient (seules les deux premières lignes du corps changent) :

```powershell
$step = 0
foreach ($m in $script:Modules) {
    $step++
    $row = New-KitModuleRow -Index ([string]$step) -Name $m.Label -Mdl2Available $script:Mdl2
```

- [ ] **Step 5 : Libellé du module courant pendant le run**

`Run-GUI.ps1:1196-1197` : remplacer `$lbl = if ($cur) { "$($cur.Mod.Id) $($cur.Mod.Name)" } else { '' }` par `$lbl = if ($cur) { [string]$cur.Mod.Label } else { '' }` et, ligne suivante, `"Module $(...)..."` devient `"Étape $([Math]::Min($script:QueueIndex + 1, $script:Queue.Count))/$($script:Queue.Count) - $lbl - écoulé $elapsed"`.

- [ ] **Step 6 : Titres et corps de l'aide**

Dans `config/help.fr.json`, les 15 rubriques `module.NN` : le titre devient « Étape N - <suite> » où N est le numéro de la table des Global Constraints et <suite> est le titre actuel débarrassé de l'ID et du nom anglais. Exemples imposés :
- `module.00` : « 00 Diagnostic - ... » -> « Étape 1 - Diagnostic : état des lieux de la machine » (adapter à la suite réellement présente ; si le titre actuel est « 00 Diagnostic - état des lieux », le nouveau est « Étape 1 - Diagnostic : état des lieux ») ;
- `module.10` : « 10 Report - rapport d'intervention » -> « Étape 15 - Rapport d'intervention » ;
- `module.15` : -> « Étape 14 - Réseau : ... ».
Puis passer les CORPS au crible : `grep -n "module [0-9]" config/help.fr.json` et remplacer chaque mention par le nom français ou « l'étape N » (ex. help.fr.json:313 « produit par le module 10 » -> « produit par l'étape Rapport »). Vérifier `tests/Help.Tests.ps1` : si des titres exacts y sont assertés, les adapter.

- [ ] **Step 7 : README**

Dans la section du tableau des modules (README.md, autour de « | 10 | 10-Report.ps1 | »), ajouter après le tableau : « L'interface numérote les étapes 1 à 15 dans l'ordre réel d'exécution ; les identifiants 00 à 15 ci-dessus sont les noms de fichiers, visibles dans les logs et le rapport. »

- [ ] **Step 8 : Vérifications et commit**

```
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitEncoding.ps1 -CI
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
git add -A
git commit -m "feat: la timeline affiche les etapes 1 a 15 en francais"
```
Attendu : 0 erreur parse, 0 violation encodage, suite verte (le compte peut monter d'un test).

---

### Task 2: Vocabulaire unique du mode + bouton LANCER dynamique

**Files:**
- Modify: `Run-GUI.ps1:276` (texte case), `:68` (commentaire), `:571` (bouton), `:1140` (préfixes titre), zone `:598` (nouvelle fonction), zone `:284-320` (câblage cases sensibles - repérer les créations `$cbRecycle`..`$cbNetReset`), `:871-913` (Set-KitActionPhase)
- Modify: `config/help.fr.json` (rubrique option.dryrun), `README.md` (section préfixe de titre, autour de « [DRY-RUN] »)
- Test: suite existante (aucune fonction pure nouvelle ; la vérification comportementale arrive au SelfTest, Task 6)

**Interfaces:**
- Produces: `Update-KitRunButtonText` (aucun paramètre ; lit `$cbDryRun.Checked`, écrit `$btnRun.Text`, rappelle `Update-KitActionBarLayout`). Les cases `$cbRecycle,$cbWinOld,$cbCache,$cbOneDrive,$cbOem,$cbNetReset` et `$cbDryRun` portent un `Add_CheckedChanged` (la Task 4 en AJOUTERA d'autres sur les mêmes contrôles : ne pas fusionner, WinForms empile les handlers dans l'ordre d'ajout).
- Consumes: rien.

- [ ] **Step 1 : Libellé de la case**

`Run-GUI.ps1:276` : `$cbDryRun.Text = "Mode dry-run (-WhatIf)"` devient `$cbDryRun.Text = "Simulation : montrer ce qui serait fait, sans rien modifier"`. La case est AutoSize dans la carte Réglages (largeur ~moitié de zone droite) : si le texte dépasse la carte à la taille minimale 1000x576, raccourcir en « Simulation : montrer sans rien modifier » (décision à prendre sur capture en Task 7, garder la version longue par défaut).

- [ ] **Step 2 : Préfixes de titre**

`Run-GUI.ps1:1140` : `'[DRY-RUN] '` -> `'[SIMULATION] '` et `'[RUN RÉEL] '` -> `'[INTERVENTION RÉELLE] '`. Commentaire `:68` mis en accord.

- [ ] **Step 3 : Fonction Update-KitRunButtonText**

Insérer APRÈS la fermeture d'`Update-KitActionBarLayout` (fonction qui commence `Run-GUI.ps1:598` - insérer après son accolade fermante) :

```powershell
# Le bouton principal annonce le mode : LANCER LA SIMULATION quand la case
# Simulation est cochée, LANCER L'INTERVENTION sinon. Le layout de la barre est
# rejoué : la largeur du libellé conditionne la position de la piste.
function Update-KitRunButtonText {
    $btnRun.Text = if ($cbDryRun.Checked) { "▶  LANCER LA SIMULATION" } else { "▶  LANCER L'INTERVENTION" }
    Update-KitActionBarLayout
}
```

Le littéral d'initialisation `:571` reste « ▶  LANCER L'INTERVENTION » (état décoché) ; si `-WhatIf` a coché la case (`:281`), le câblage du Step 4 et l'appel d'init du Step 5 rattrapent le texte avant affichage.

- [ ] **Step 4 : Câblages CheckedChanged**

Après la création des contrôles concernés (zone des cartes, chercher `$cbNetReset` puis insérer après le dernier `Controls.Add` du groupe sensibles) :

```powershell
# Le résumé de la barre suit les actions sensibles et le mode en direct.
foreach ($cbSensitive in @($cbRecycle, $cbWinOld, $cbCache, $cbOneDrive, $cbOem, $cbNetReset)) {
    $cbSensitive.Add_CheckedChanged({ Update-KitActionSummary })
}
$cbDryRun.Add_CheckedChanged({
    Update-KitActionSummary
    Update-KitRunButtonText
    if (-not $UiPreview) {
        $mode = if ($cbDryRun.Checked) { 'Simulation' } else { 'Real' }
        Set-KitBadgeMode -Band $script:Band -Mode $mode
    }
})
```

ATTENTION ordre de fichier : ces handlers référencent des fonctions définies plus bas (`Update-KitActionSummary:856`, `Update-KitRunButtonText` du Step 3) - légal, les handlers ne s'exécutent qu'aux événements, après que tout le script est chargé. Si le câblage est placé AVANT la définition d'`Update-KitActionBarLayout` (`:598`), le premier `-WhatIf` coché au chargement (`:281`) précéderait les définitions : `:281` PRÉCÈDE tout câblage ci-dessus, donc aucun événement ne part avant la fin du chargement. Placer le câblage APRÈS `:598` par sécurité.

- [ ] **Step 5 : Rafraîchissements de phase et d'init**

Dans `Set-KitActionPhase` (`:871`), branche `'Prepare'`, ajouter `Update-KitRunButtonText` juste avant `Update-KitActionSummary` (`:888`). Puis à l'initialisation (zone `:1100`, après `Update-ProfileComboBox`), ajouter la ligne `Update-KitRunButtonText` (rattrape le cas `-WhatIf`).

- [ ] **Step 6 : Aide et docs**

`config/help.fr.json`, rubrique `option.dryrun` : réécrire `what` en « En simulation, chaque étape décrit dans le journal ce qu'elle ferait, sans rien modifier sur la machine. Décochée, l'intervention est réelle : les actions sont appliquées. » (conserver les autres champs, les mettre en accord si « dry-run » y figure ; le MOT dry-run ne doit plus apparaître dans une chaîne affichée - le nom technique `-WhatIf` peut rester dans README/docs techniques). README.md : section du préfixe de titre (chercher « [DRY-RUN] ») réécrite avec les nouveaux préfixes et une phrase reprenant la définition ci-dessus.

- [ ] **Step 7 : Vérifications et commit**

```
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitEncoding.ps1 -CI
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
grep -rn "dry-run" Run-GUI.ps1 lib/ config/help.fr.json
git add -A
git commit -m "feat: vocabulaire unique simulation intervention reelle et bouton dynamique"
```
Attendu grep : plus AUCUNE occurrence dans une chaîne visible (commentaires de code tolérés, chaînes non).

---

### Task 3: L'onglet Journal n'apparaît qu'au lancement

**Files:**
- Modify: `Run-GUI.ps1:365-366` (init TabPages), zone `:415` (nouvelle fonction, après Update-KitRightZoneForPhase), `:1180-1183` (handler LANCER)
- Test: suite existante (assertion comportementale au SelfTest, Task 6)

**Interfaces:**
- Produces: `Show-KitJournalTab` (aucun paramètre ; ajoute `$script:TabLog` au TabControl s'il en est absent et le sélectionne ; idempotente).
- Consumes: rien.

- [ ] **Step 1 : Ne plus ajouter le Journal à l'initialisation**

`Run-GUI.ps1:365-366` : supprimer la ligne `$script:Tabs.TabPages.Add($script:TabLog)` et commenter : « Le Journal n'a rien à dire avant le run : son onglet est ajouté par Show-KitJournalTab au clic LANCER (grief G1 v2.3). »

- [ ] **Step 2 : Fonction Show-KitJournalTab**

Insérer après la fermeture d'`Update-KitRightZoneForPhase` (`:392-415`) :

```powershell
# Ajoute l'onglet Journal (premier run de la session) et l'affiche. Idempotente,
# même pattern que la page Clôture (TabPages.Contains).
function Show-KitJournalTab {
    if (-not $script:Tabs.TabPages.Contains($script:TabLog)) {
        $script:Tabs.TabPages.Add($script:TabLog)
    }
    $script:Tabs.SelectedTab = $script:TabLog
}
```

- [ ] **Step 3 : Appel au lancement**

Dans le handler LANCER (`:1180-1183`), garder le retrait de TabClose et remplacer `$script:Tabs.SelectedTab = $script:TabLog` par `Show-KitJournalTab`.

- [ ] **Step 4 : Vérifications et commit**

```
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
git add -A
git commit -m "feat: l onglet journal n apparait qu au lancement du run"
```

---

### Task 4: Profils à application immédiate

**Files:**
- Modify: `Run-GUI.ps1` : zone d'état `:66-78`, création `:150-164` (retrait btnApplyProfile, btnSaveProfile pleine largeur), boucle `:181-191` (câblage divergence), helpBindings `:672` (retrait ligne applyprofile), handlers profils `:694-700`, fonctions profils `:1005-1098` (Update-ProfileComboBox, nouvelles fonctions, Set-KitPrepareEnabled `:1093`), init `:1100-1106`, handler btnApplyProfile `:1420-1450` (suppression), handler btnSaveProfile `:1481-1488`, câblages divergence sur cmbDebloat/radios/options (zones `:284-320` et `:689-692`)
- Modify: `config/help.fr.json` : suppression `action.applyprofile`, ajout `profile.custom`, réécriture what/reversible de `profile.standard|senior|gamer`
- Test: `tests/Help.Tests.ps1` (compte/liste des rubriques : -1 +1 = compte inchangé à 42, vérifier les clés listées)

**Interfaces:**
- Consumes: les `Add_CheckedChanged` posés par la Task 2 (on EMPILE de nouveaux handlers sur les mêmes contrôles, on ne modifie pas les leurs).
- Produces: `$script:CustomProfileLabel = '(personnalisé)'` ; `$script:ApplyingProfile` (bool) ; `Invoke-KitProfileSelection` (handler de sélection) ; `Set-KitProfileCustom` (bascule d'étiquette) ; `Update-ProfileComboBox([string]$Select = 'standard')`. La Task 6 s'appuie sur tout cela.

- [ ] **Step 1 : État**

Dans la zone d'état (`:66-78`, près de `$script:AppliedProfileName`) :

```powershell
$script:ApplyingProfile    = $false            # vrai pendant Set-GuiFromProfile : coupe la détection de divergence
$script:CustomProfileLabel = '(personnalisé)'  # entrée sentinelle de la liste des profils, toujours en dernier
```

- [ ] **Step 2 : Retrait du bouton Appliquer, Enregistrer pleine largeur**

- Supprimer `:150-155` (création `$btnApplyProfile`).
- `:157-162` : `$btnSaveProfile.Location = New-Object System.Drawing.Point(0, 48)` et `Size(276, 24)` (il occupe seul la rangée).
- helpBindings `:672` : supprimer la ligne `@{ Ctrl = $btnApplyProfile; Key = 'action.applyprofile' }`.
- `Set-KitPrepareEnabled:1093` : retirer `$btnApplyProfile` de la liste.
- Supprimer le handler `$btnApplyProfile.Add_Click({ ... })` (`:1420-1450`).

- [ ] **Step 3 : Sélection = application (Invoke-KitProfileSelection)**

Dans la zone des fonctions profils (après `Set-KitPrepareEnabled`, avant l'init `:1100`) :

```powershell
# Sélection dans la liste = application immédiate (v2.3, grief G2). La sentinelle
# (personnalisé) représente l'état courant : la sélectionner ne touche aucune case.
function Invoke-KitProfileSelection {
    if ($script:ApplyingProfile) { return }   # bascule programmatique en cours
    if ($script:Running) { return }           # la liste est désactivée pendant un run, ceinture
    $sel = [string]$cmbProfile.SelectedItem
    if ([string]::IsNullOrWhiteSpace($sel)) { return }
    if ($sel -eq $script:CustomProfileLabel) {
        $script:AppliedProfileName = 'personnalisé'
        Update-KitActionSummary
        Show-KitProfileHelp -Name $sel
        return
    }
    $profPath = Join-Path $script:Root "config\profiles\$sel.json"
    if (-not (Test-Path $profPath)) {
        [System.Windows.Forms.MessageBox]::Show(
            "Fichier de profil introuvable : $profPath", "PC-Refresh-Kit", 'OK', 'Warning') | Out-Null
        return
    }
    try {
        $prof = Read-KitProfile -Path $profPath
        $script:ApplyingProfile = $true
        try { Set-GuiFromProfile $prof } finally { $script:ApplyingProfile = $false }
        $script:AppliedProfileName = $sel
        Update-KitActionSummary
        Show-KitProfileHelp -Name $sel
    } catch {
        [System.Windows.Forms.MessageBox]::Show(
            "Erreur lors de la lecture du profil : $_", "PC-Refresh-Kit", 'OK', 'Error') | Out-Null
    }
}

# Un contrôle piloté par le profil vient de changer à la main : l'étiquette de la
# liste bascule sur (personnalisé) sans toucher aux cases.
function Set-KitProfileCustom {
    if ($script:ApplyingProfile) { return }
    if ([string]$cmbProfile.SelectedItem -eq $script:CustomProfileLabel) { return }
    $script:ApplyingProfile = $true
    try { $cmbProfile.SelectedItem = $script:CustomProfileLabel } finally { $script:ApplyingProfile = $false }
    $script:AppliedProfileName = 'personnalisé'
    Update-KitActionSummary
}
```

NOTE `profile.custom` : `Show-KitProfileHelp -Name '(personnalisé)'` ne trouvera ni `profile.(personnalisé)` ni description JSON et retomberait sur l'invite : ajouter EN TÊTE de `Show-KitProfileHelp` (`:632`) :

```powershell
    if ($Name -eq $script:CustomProfileLabel) { Show-KitHelp -Key 'profile.custom'; return }
```

- [ ] **Step 4 : Câblage de la sélection**

`:698-700` : le handler devient `$cmbProfile.Add_SelectedIndexChanged({ Invoke-KitProfileSelection })` (l'aide est affichée PAR Invoke-KitProfileSelection ; le MouseEnter `:695-697` ne change pas ici - Task 5 s'en charge).

- [ ] **Step 5 : Câblage de la divergence**

- Boucle timeline (`:181-191`, déjà modifiée en Task 1) : ajouter dans le corps, après le `Add_CheckedChanged` existant : `$row.CheckBox.Add_CheckedChanged({ Set-KitProfileCustom })`.
- Après les câblages de la Task 2 (même zone de fichier), ajouter :

```powershell
# Divergence profil : tout contrôle piloté par Set-GuiFromProfile bascule l'étiquette.
foreach ($cbProfiled in @($cbRecycle, $cbWinOld, $cbCache, $cbOneDrive, $cbOem, $cbNetReset,
                          $cbBackupData, $cbScanDefender, $rbKeep, $rbStd)) {
    $cbProfiled.Add_CheckedChanged({ Set-KitProfileCustom })
}
$cmbDebloat.Add_SelectedIndexChanged({ Set-KitProfileCustom })
```

`$cbDryRun` n'y figure PAS (pas piloté par les profils). Les radios déclenchent deux événements par bascule : la garde « déjà (personnalisé) » coupe le second.

- [ ] **Step 6 : Update-ProfileComboBox avec sentinelle et sélection nommée**

Remplacer la fonction (`:1005-1030`) par :

```powershell
function Update-ProfileComboBox {
    # Repeuple la liste depuis config\profiles\*.json + la sentinelle (personnalisé)
    # en dernier, relit les Descriptions en mémoire, puis sélectionne $Select :
    # la sélection déclenche SelectedIndexChanged, donc l'APPLICATION du profil (v2.3).
    [CmdletBinding()]
    param([string]$Select = 'standard')
    $cmbProfile.Items.Clear()
    $script:ProfileDescriptions = @{}
    $profilesDir = Join-Path $script:Root 'config\profiles'
    if (Test-Path $profilesDir) {
        $files = @(Get-ChildItem -Path $profilesDir -Filter '*.json' -ErrorAction SilentlyContinue)
        foreach ($f in $files) {
            [void]$cmbProfile.Items.Add($f.BaseName)
            # Un JSON illisible ne doit pas amputer la liste : le profil reste
            # sélectionnable, il n'aura simplement pas de description.
            try { $script:ProfileDescriptions[$f.BaseName] = [string](Read-KitProfile -Path $f.FullName).Description }
            catch { }
        }
    }
    [void]$cmbProfile.Items.Add($script:CustomProfileLabel)
    $idx = $cmbProfile.Items.IndexOf($Select)
    if ($idx -lt 0) { $idx = 0 }   # standard absent : premier profil, sinon la sentinelle
    $cmbProfile.SelectedIndex = $idx
}
```

- [ ] **Step 7 : Init et enregistrement**

- Init `:1100-1106` : garder `Update-ProfileComboBox` (le défaut `-Select 'standard'` applique standard au démarrage) et SUPPRIMER le commentaire `:1102-1105` et la ligne `$txtHelp.Text = $script:HelpPrompt` (`:1106`) : l'aide affiche désormais le profil appliqué. Supprimer aussi, dans l'ancienne fonction, le cas spécial `-UiPreview` (`:1024-1027`) - standard est le défaut pour tout le monde.
- Handler btnSaveProfile `:1481-1488` : remplacer les lignes `Update-ProfileComboBox` / `$idx = ...IndexOf($nom)` / `if ($idx -ge 0) { ... }` / `$script:AppliedProfileName = $nom` / `Update-KitActionSummary` par la seule ligne `Update-ProfileComboBox -Select $nom` (la sélection applique le profil fraîchement écrit - identique à l'état courant, donc sans effet visible - et pose nom + résumé via Invoke-KitProfileSelection). Garder MessageBox de confirmation.

- [ ] **Step 8 : Textes d'aide**

`config/help.fr.json` :
- Supprimer la rubrique `action.applyprofile`.
- Ajouter (à côté des profile.*) :

```json
"profile.custom": {
  "title": "Profil (personnalisé)",
  "what": "Votre sélection actuelle. Cette entrée apparaît dès que vous modifiez une case après avoir choisi un profil : elle dit simplement que l'écran ne correspond plus mot pour mot à un profil enregistré.",
  "when": "Vous n'avez pas à la choisir : elle se sélectionne toute seule. Utilisez Enregistrer comme profil pour conserver cette sélection sous un nom.",
  "whenNot": "Sans objet.",
  "protects": "Rien n'est écrit sur le disque : c'est un simple état de l'écran.",
  "reversible": "Oui : choisir un profil de la liste reprend la main immédiatement."
}
```

(Adapter les NOMS de champs à ceux réellement présents dans les rubriques voisines - reprendre exactement les clés de `profile.standard`.)
- `profile.standard`, `profile.senior`, `profile.gamer` : le `what` commence désormais par « Sélectionner ce profil coche immédiatement » + le contenu descriptif actuel reformulé ; le `reversible` devient pour les trois : « Oui : choisir un autre profil ou modifier une case reprend la main. Aucun réglage n'est écrit sur le disque. »

- [ ] **Step 9 : Tests et commit**

`tests/Help.Tests.ps1` : chercher le compte de rubriques et toute liste de clés (`grep -n "applyprofile\|42" tests/Help.Tests.ps1`) ; retirer `action.applyprofile`, ajouter `profile.custom` (compte inchangé : 42).

```
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitEncoding.ps1 -CI
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
git add -A
git commit -m "feat: les profils s appliquent a la selection avec etat personnalise"
```

---

### Task 5: Aide praticable - délai, gel, épinglage

**Files:**
- Modify: `lib/Help.ps1` (nouvelle fonction pure Get-KitHelpDecision)
- Modify: `Run-GUI.ps1` : zone d'état, zone TabHelp `:335-351` (bandeau épingle), `Show-KitHelp:618-626` (inchangé, reste l'écrivain final), `Show-KitProfileHelp:632-649` (refonte -Source), câblages `:676-711`, nouveau timer + fonctions (zone après Show-KitProfileHelp), Échap (KeyPreview, zone de création de `$form`)
- Test: `tests/Help.Tests.ps1` (table de vérité complète)

**Interfaces:**
- Consumes: `Show-KitProfileHelp` étendue par la Task 4 (garde profile.custom en tête).
- Produces: `Get-KitHelpDecision -Source Hover|Direct -Pinned <bool> -Frozen <bool>` -> 'Show'|'Defer'|'Ignore' (lib/Help.ps1, PURE) ; `Request-KitHelp -Key <string> [-Source Hover|Direct]` ; `Invoke-KitHelpRequest -Action <scriptblock> -Source <string>` ; `Set-KitHelpPinned <bool>` ; états `$script:HelpPinned`, `$script:HelpFrozen`.

- [ ] **Step 1 : Test Pester de la décision (échoue d'abord)**

Dans `tests/Help.Tests.ps1`, nouveau Describe :

```powershell
Describe 'Get-KitHelpDecision' {
    It 'renvoie <Expected> pour <Source>/pinned=<Pinned>/frozen=<Frozen>' -ForEach @(
        @{ Source='Hover';  Pinned=$true;  Frozen=$true;  Expected='Ignore' }
        @{ Source='Hover';  Pinned=$true;  Frozen=$false; Expected='Ignore' }
        @{ Source='Direct'; Pinned=$true;  Frozen=$true;  Expected='Ignore' }
        @{ Source='Direct'; Pinned=$true;  Frozen=$false; Expected='Ignore' }
        @{ Source='Direct'; Pinned=$false; Frozen=$true;  Expected='Show' }
        @{ Source='Direct'; Pinned=$false; Frozen=$false; Expected='Show' }
        @{ Source='Hover';  Pinned=$false; Frozen=$true;  Expected='Ignore' }
        @{ Source='Hover';  Pinned=$false; Frozen=$false; Expected='Defer' }
    ) {
        Get-KitHelpDecision -Source $Source -Pinned $Pinned -Frozen $Frozen | Should -Be $Expected
    }
}
```

Lancer Help.Tests : 8 échecs attendus (fonction absente).

- [ ] **Step 2 : Get-KitHelpDecision dans lib/Help.ps1**

Après `Format-HelpTooltip` :

```powershell
# ---------------------------------------------------------------------------
# Get-KitHelpDecision : le panneau d'aide doit-il être remplacé ? PURE.
# Épinglé : rien ne remplace (le lecteur a demandé la stabilité).
# Direct (sélection, application de profil) : remplace tout de suite.
# Hover gelé (souris dans le panneau) : ignoré, on ne vole pas la lecture.
# Hover libre : différé (le délai anti-transit filtre les survols de passage).
# ---------------------------------------------------------------------------
function Get-KitHelpDecision {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][ValidateSet('Hover','Direct')][string]$Source,
        [Parameter(Mandatory)][bool]$Pinned,
        [Parameter(Mandatory)][bool]$Frozen
    )
    if ($Pinned) { return 'Ignore' }
    if ($Source -eq 'Direct') { return 'Show' }
    if ($Frozen) { return 'Ignore' }
    return 'Defer'
}
```

Relancer Help.Tests : 8 passés.

- [ ] **Step 3 : État, timer, mécanique différée dans Run-GUI**

Zone d'état (`:66-78`) :

```powershell
$script:HelpPinned       = $false   # épingle : plus aucun remplacement du panneau
$script:HelpFrozen       = $false   # souris dans le panneau : lecture en cours
$script:PendingHelpAction = $null   # action différée par le délai anti-transit
```

Après `Show-KitProfileHelp` (fonctions d'aide, avant helpBindings `:652`) :

```powershell
# Délai anti-transit : un survol ne remplace le panneau qu'après 350 ms de
# stabilité. Timer unique ; chaque survol écrase l'action en attente.
$script:HelpHoverTimer = New-Object System.Windows.Forms.Timer
$script:HelpHoverTimer.Interval = 350
$script:HelpHoverTimer.Add_Tick({
    $script:HelpHoverTimer.Stop()
    # L'état a pu changer pendant l'attente (épinglage, entrée dans le panneau).
    if (-not $script:HelpPinned -and -not $script:HelpFrozen -and $script:PendingHelpAction) {
        & $script:PendingHelpAction
    }
})

function Invoke-KitHelpRequest {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][scriptblock]$Action,
        [ValidateSet('Hover','Direct')][string]$Source = 'Hover'
    )
    switch (Get-KitHelpDecision -Source $Source -Pinned $script:HelpPinned -Frozen $script:HelpFrozen) {
        'Show'   { $script:HelpHoverTimer.Stop(); & $Action }
        'Defer'  { $script:PendingHelpAction = $Action; $script:HelpHoverTimer.Stop(); $script:HelpHoverTimer.Start() }
        'Ignore' { }
    }
}

function Request-KitHelp {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][string]$Key,
        [ValidateSet('Hover','Direct')][string]$Source = 'Hover'
    )
    $k = $Key
    Invoke-KitHelpRequest -Action { Show-KitHelp -Key $k }.GetNewClosure() -Source $Source
}
```

(GetNewClosure : capture par COPIE - la leçon v2.2 des handlers de boucle s'applique.)

- [ ] **Step 4 : Show-KitProfileHelp passe par la décision**

Refondre `:632-649` : extraire le corps actuel (cascade catalogue -> Description -> invite, PLUS la garde profile.custom de la Task 4) dans `Show-KitProfileHelpCore` (mêmes lignes, aucun changement de logique), puis :

```powershell
function Show-KitProfileHelp {
    [CmdletBinding()]
    param(
        [AllowEmptyString()][AllowNull()][string]$Name,
        [ValidateSet('Hover','Direct')][string]$Source = 'Direct'
    )
    if ([string]::IsNullOrWhiteSpace($Name)) { return }
    $n = $Name
    Invoke-KitHelpRequest -Action { Show-KitProfileHelpCore -Name $n }.GetNewClosure() -Source $Source
}
```

Les appels existants sans `-Source` (Invoke-KitProfileSelection, Task 4) prennent le défaut Direct : rien à modifier chez eux.

- [ ] **Step 5 : Basculer les survols sur Request-KitHelp**

- `:680` : `$b.Ctrl.Add_MouseEnter({ Show-KitHelp -Key $cle }.GetNewClosure())` -> `$b.Ctrl.Add_MouseEnter({ Request-KitHelp -Key $cle }.GetNewClosure())`.
- `:685-688` (cmbDebloat MouseEnter) : `Show-KitHelp -Key $k` -> `Request-KitHelp -Key $k` ; `:689-692` (SelectedIndexChanged) : -> `Request-KitHelp -Key $k -Source Direct`.
- `:695-697` (cmbProfile MouseEnter) : -> `Show-KitProfileHelp -Name ([string]$cmbProfile.SelectedItem) -Source Hover`.
- `:707` (boucle timeline) : `Show-KitHelp` -> `Request-KitHelp` (même GetNewClosure).
- Le forçage d'aperçu (`Show-KitHelp -Key 'module.02'` dans le bloc -UiPreview) reste un appel direct : inchangé ici (retiré en Task 7).

- [ ] **Step 6 : Gel du panneau**

Après la création de `$txtHelp` (`:351`) :

```powershell
# Souris dans le panneau d'aide = lecture en cours : le contenu est gelé.
foreach ($helpZone in @($script:TabHelp, $txtHelp)) {
    $helpZone.Add_MouseEnter({ $script:HelpFrozen = $true; $script:HelpHoverTimer.Stop() })
}
$script:Tabs.Add_MouseLeave({ $script:HelpFrozen = $false })
```

(Le MouseLeave du TabControl couvre la sortie de toute la zone onglets ; passer de l'onglet Aide au Journal garde le gel - voulu, on ne survole rien d'autre depuis les onglets.)

- [ ] **Step 7 : Vérifier les glyphes puis bandeau épingle**

Vérifier E718 (Pin) et E77A (Unpin) dans segmdl2.ttf AVANT usage :

```powershell
powershell -Command "Add-Type -AssemblyName PresentationCore; $g = New-Object System.Windows.Media.GlyphTypeface('C:\Windows\Fonts\segmdl2.ttf'); 0xE718, 0xE77A | ForEach-Object { '{0:X4} : {1}' -f $_, $g.CharacterToGlyphMap.ContainsKey($_) }"
```

Attendu : True/True. Si l'un manque : repli texte « Épingler »/« Épinglé » partout (et le noter au rapport). Puis, après l'ajout de `$txtHelp` à `$script:TabHelp` (`:351`) :

```powershell
# Bandeau d'épinglage en tête du panneau d'aide. Ordre de docking : le RichTextBox
# (Fill) est ajouté avant la barre (Top) puis renvoyé au fond - sans BringToFront,
# WinForms docke dans l'ordre inverse d'ajout et le Fill passerait SOUS la barre.
$script:HelpPinBar = New-Object System.Windows.Forms.Panel
$script:HelpPinBar.Dock = 'Top'
$script:HelpPinBar.Height = 24
$script:HelpPinBar.BackColor = ConvertTo-KitColor $script:Palette.Card
$script:BtnHelpPin = New-Object System.Windows.Forms.Button
$script:BtnHelpPin.FlatStyle = 'Flat'
$script:BtnHelpPin.FlatAppearance.BorderSize = 0
$script:BtnHelpPin.Size = New-Object System.Drawing.Size(60, 20)
$script:BtnHelpPin.Location = New-Object System.Drawing.Point(($script:HelpPinBar.Width - 64), 2)
$script:BtnHelpPin.Anchor = 'Top,Right'
$script:BtnHelpPin.Cursor = 'Hand'
if ($script:Mdl2) {
    $script:BtnHelpPin.Font = New-Object System.Drawing.Font('Segoe MDL2 Assets', 9)
    $script:BtnHelpPin.Text = [string][char]0xE718
} else {
    $script:BtnHelpPin.Font = New-Object System.Drawing.Font('Segoe UI', 8.25)
    $script:BtnHelpPin.Text = 'Épingler'
}
$script:BtnHelpPin.ForeColor = ConvertTo-KitColor $script:Palette.InkMuted
$script:BtnHelpPin.BackColor = ConvertTo-KitColor $script:Palette.Card
$script:HelpPinBar.Controls.Add($script:BtnHelpPin)
$script:TabHelp.Controls.Add($script:HelpPinBar)
$txtHelp.BringToFront()
$toolTip.SetToolTip($script:BtnHelpPin, "Épingler cette rubrique : le panneau ne changera plus jusqu'au prochain clic (ou Échap).")
```

ATTENTION : `$toolTip` doit exister à ce point du fichier - si le ToolTip est créé plus bas (zone helpBindings), déplacer le SetToolTip près des autres (`:678`). La gestion MouseEnter du bandeau : ajouter `$script:HelpPinBar` à la boucle `$helpZone` du Step 6.

```powershell
function Set-KitHelpPinned {
    [CmdletBinding()]
    param([Parameter(Mandatory)][bool]$On)
    $script:HelpPinned = $On
    if ($On) { $script:HelpHoverTimer.Stop() }
    if ($script:Mdl2) { $script:BtnHelpPin.Text = [string][char]$(if ($On) { 0xE77A } else { 0xE718 }) }
    else { $script:BtnHelpPin.Text = if ($On) { 'Épinglé' } else { 'Épingler' } }
    $script:BtnHelpPin.BackColor = ConvertTo-KitColor $(if ($On) { $script:Palette.AccentDark } else { $script:Palette.Card })
    $script:BtnHelpPin.ForeColor = if ($On) { [System.Drawing.Color]::White } else { ConvertTo-KitColor $script:Palette.InkMuted }
}
$script:BtnHelpPin.Add_Click({ Set-KitHelpPinned (-not $script:HelpPinned) })
```

(Définir `Set-KitHelpPinned` AVANT le `Add_Click` dans le fichier, ou à un endroit exécuté avant tout événement - même règle que les autres fonctions : seul l'ordre d'exécution au chargement compte.)

- [ ] **Step 8 : Échap dés-épingle**

À la création de `$form` (chercher `$form = New-Object System.Windows.Forms.Form`) : `$form.KeyPreview = $true`. Puis près des autres handlers du formulaire :

```powershell
$form.Add_KeyDown({
    param($sender, $e)
    if ($e.KeyCode -eq [System.Windows.Forms.Keys]::Escape -and $script:HelpPinned) {
        Set-KitHelpPinned $false
        $e.Handled = $true
    }
})
```

(Vérifié en conception : le formulaire n'a pas de CancelButton, Échap est libre.)

- [ ] **Step 9 : Vérifications et commit**

```
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitEncoding.ps1 -CI
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
git add -A
git commit -m "feat: aide au survol avec delai gel et epinglage"
```

---

### Task 6: Parcours utilisateur scripté (-SelfTest) + CI

**Files:**
- Modify: `Run-GUI.ps1` : bloc param en tête (ajout [switch]$SelfTest), bloc SelfTest juste avant le ShowDialog final (fin de fichier)
- Modify: `.github/workflows/ci.yml` (nouveau step), `README.md` (section tests : une ligne)
- Test: le SelfTest EST le test ; suite Pester inchangée

**Interfaces:**
- Consumes: `$script:CustomProfileLabel`, `Show-KitJournalTab`, les câblages des Tasks 1-5, `Read-KitProfile` (lib/Common.ps1), `$script:ModuleRows`, `$script:ActionSummary`, `$btnRun`, `$cbDryRun`, `$script:Tabs`/`$script:TabLog`.
- Produces: `powershell -ExecutionPolicy Bypass -File Run-GUI.ps1 -SelfTest` -> sortie `[SELFTEST]` par assertion, exit 0 (tout passé) / 1.

- [ ] **Step 1 : Paramètre**

Ajouter `[switch]$SelfTest` au bloc `param(...)` en tête de Run-GUI.ps1 (à côté de `$UiPreview`). `-SelfTest` ne se combine PAS avec `-UiPreview` (états de préversion factices) : si les deux sont passés, écrire `[SELFTEST] ERREUR : incompatible avec -UiPreview` et `exit 2` dès le début du script (garde en tête de fichier, juste après le param).

- [ ] **Step 2 : Bloc SelfTest avant ShowDialog**

Localiser le `ShowDialog` final (fin de fichier, chercher `ShowDialog`) et insérer AVANT :

```powershell
if ($SelfTest) {
    # Parcours utilisateur scripté (spec v2.3 §8) : états LOGIQUES seulement
    # (Checked, SelectedItem, Text, TabPages) - jamais Visible, qui vaut faux
    # tant que la fenêtre n'est pas affichée.
    $script:SelfTestFailures = 0
    function Assert-KitSelfTest {
        param([Parameter(Mandatory)][string]$Label, [Parameter(Mandatory)][bool]$Condition)
        if ($Condition) { Write-Host "[SELFTEST] OK   $Label" }
        else { Write-Host "[SELFTEST] FAIL $Label"; $script:SelfTestFailures++ }
    }
    function Test-KitGuiMatchesProfile {
        param([Parameter(Mandatory)][string]$ProfileName)
        $p = Read-KitProfile -Path (Join-Path $script:Root "config\profiles\$ProfileName.json")
        foreach ($m in $script:Modules) {
            $want = $true
            if ($p.Modules.ContainsKey($m.Id)) { $want = [bool]$p.Modules[$m.Id] }
            if ($script:ModuleRows[$m.Id].CheckBox.Checked -ne $want) { return $false }
        }
        if ($cbRecycle.Checked  -ne [bool]$p.Recycle)  { return $false }
        if ($cbWinOld.Checked   -ne [bool]$p.WinOld)   { return $false }
        if ($cbCache.Checked    -ne [bool]$p.Cache)    { return $false }
        if ($cbOneDrive.Checked -ne [bool]$p.OneDrive) { return $false }
        if ($cbOem.Checked      -ne [bool]$p.Oem)      { return $false }
        if ($cbNetReset.Checked -ne [bool]$p.NetReset) { return $false }
        if ($rbKeep.Checked     -ne [bool]$p.KeepAdmin) { return $false }
        return $true
    }

    # 1. État de démarrage : standard appliqué, pas de Journal, sentinelle en dernier.
    Assert-KitSelfTest 'demarrage : selection standard' ([string]$cmbProfile.SelectedItem -eq 'standard')
    Assert-KitSelfTest 'demarrage : cases conformes a standard.json' (Test-KitGuiMatchesProfile 'standard')
    Assert-KitSelfTest 'demarrage : resume annonce le profil standard' ($script:ActionSummary.Text -match 'profil standard')
    Assert-KitSelfTest 'demarrage : onglet Journal absent' (-not $script:Tabs.TabPages.Contains($script:TabLog))
    Assert-KitSelfTest 'liste : sentinelle en derniere position' ([string]$cmbProfile.Items[$cmbProfile.Items.Count - 1] -eq $script:CustomProfileLabel)

    # 2. Sélection senior : application immédiate.
    $cmbProfile.SelectedItem = 'senior'
    Assert-KitSelfTest 'selection senior : cases conformes a senior.json' (Test-KitGuiMatchesProfile 'senior')
    Assert-KitSelfTest 'selection senior : resume a suivi' ($script:ActionSummary.Text -match 'profil senior')

    # 3. Divergence par une case module : bascule (personnalisé), cases intactes.
    $before = $script:ModuleRows['03'].CheckBox.Checked
    $script:ModuleRows['03'].CheckBox.Checked = -not $before
    Assert-KitSelfTest 'divergence module : etiquette (personnalise)' ([string]$cmbProfile.SelectedItem -eq $script:CustomProfileLabel)
    Assert-KitSelfTest 'divergence module : resume personnalise' ($script:ActionSummary.Text -match 'profil personnalisé')
    Assert-KitSelfTest 'divergence module : la case a bien change' ($script:ModuleRows['03'].CheckBox.Checked -eq (-not $before))

    # 4. Retour standard : les cases reviennent.
    $cmbProfile.SelectedItem = 'standard'
    Assert-KitSelfTest 'retour standard : cases conformes' (Test-KitGuiMatchesProfile 'standard')

    # 5. Divergence par une option sensible.
    $cbRecycle.Checked = -not $cbRecycle.Checked
    Assert-KitSelfTest 'divergence option : etiquette (personnalise)' ([string]$cmbProfile.SelectedItem -eq $script:CustomProfileLabel)

    # 6. Étapes : numérotation 1..15 et labels français aux deux bouts.
    Assert-KitSelfTest 'timeline : ligne 1 = Diagnostic' ($script:ModuleRows['00'].IdLabel.Text -eq '1' -and $script:ModuleRows['00'].NameLabel.Text -eq 'Diagnostic')
    Assert-KitSelfTest 'timeline : ligne 15 = Rapport' ($script:ModuleRows['10'].IdLabel.Text -eq '15' -and $script:ModuleRows['10'].NameLabel.Text -eq 'Rapport')

    # 7. Mode : le bouton principal suit la case Simulation.
    $cbDryRun.Checked = $true
    Assert-KitSelfTest 'mode : bouton LANCER LA SIMULATION' ($btnRun.Text -match 'SIMULATION')
    $cbDryRun.Checked = $false
    Assert-KitSelfTest 'mode : bouton LANCER L INTERVENTION' ($btnRun.Text -match "INTERVENTION")

    # 8. Journal : apparaît et prend le focus via Show-KitJournalTab.
    Show-KitJournalTab
    Assert-KitSelfTest 'journal : onglet present apres lancement simule' ($script:Tabs.TabPages.Contains($script:TabLog))
    Assert-KitSelfTest 'journal : onglet selectionne' ($script:Tabs.SelectedTab -eq $script:TabLog)

    if ($script:SelfTestFailures -gt 0) {
        Write-Host "[SELFTEST] ECHEC : $script:SelfTestFailures assertion(s)"
        exit 1
    }
    Write-Host '[SELFTEST] SUCCES : parcours complet (17 assertions)'
    exit 0
}
[void]$form.ShowDialog()
```

NOTE StrictMode : `$row.IdLabel` - vérifier le nom exact de la clé du retour de New-KitModuleRow (`grep -n "IdLabel\|IdLbl" lib/Theme.ps1` ; si le retour expose une autre casse/nom, aligner les assertions dessus).

- [ ] **Step 3 : Exécuter le SelfTest**

```
powershell -ExecutionPolicy Bypass -File Run-GUI.ps1 -SelfTest
```

Attendu : 17 lignes `[SELFTEST] OK`, `SUCCES`, exit 0 (`$LASTEXITCODE` = 0). Toute assertion FAIL est un bug d'une tâche précédente : le corriger AVANT de continuer (c'est le but du garde-fou).

- [ ] **Step 4 : Step CI GitHub**

Lire `.github/workflows/ci.yml`, ajouter après le step de smoke (ou le dernier step de tests) :

```yaml
      - name: SelfTest GUI (parcours utilisateur)
        shell: powershell
        run: |
          powershell -ExecutionPolicy Bypass -File Run-GUI.ps1 -SelfTest
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
```

(Indentation à caler sur les steps existants. `shell: powershell` = Windows PowerShell 5.1, comme le kit. En cas d'échec d'environnement WinForms sur le runner, le step ÉCHOUE - jamais de contournement silencieux.)

- [ ] **Step 5 : README + commit**

README, section des tests/CI : ajouter « `Run-GUI.ps1 -SelfTest` déroule un parcours utilisateur scripté (profils, étapes, mode, journal) sans afficher la fenêtre ; il fait partie de la CI. »

```
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
git add -A
git commit -m "test: parcours utilisateur scripte -SelfTest integre a la CI"
```

---

### Task 7: Version, captures, relecture des textes, docs v2.3

**Files:**
- Modify: `lib/Report.ps1` (Get-KitVersion 'v2.2' -> 'v2.3' ; adapter tout test qui asserte v2.2 : `grep -rn "v2\.2" tests/`)
- Modify: `Run-GUI.ps1` (bloc -UiPreview : retirer le forçage `Show-KitHelp -Key 'module.02'` du PreviewPhase Prepare - l'aide naturelle affiche désormais la rubrique du profil standard appliqué)
- Modify: `README.md` (sections Préparer/profils/aide/préfixes/étapes), Create: `docs/RELEASE-NOTES-v2.3.0.md`
- Update: `docs/img/gui-prepare.png`, `docs/img/gui-run.png`, `docs/img/gui-close.png`

**Interfaces:**
- Consumes: tout ce qui précède.
- Produces: les trois captures finales et les notes de release (utilisées telles quelles par la release GitHub, hors plan).

- [ ] **Step 1 : Version et forçage d'aperçu**

`lib/Report.ps1` : Get-KitVersion retourne 'v2.3'. `grep -rn "v2\.2" tests/ lib/ Run-GUI.ps1` : adapter les tests qui assertent la version ; les mentions historiques de docs ne bougent pas. Run-GUI.ps1, bloc PreviewPhase Prepare : supprimer le `Show-KitHelp -Key 'module.02'` (et son commentaire) - la capture montre l'aide du profil standard, l'état RÉEL d'ouverture.

- [ ] **Step 2 : Captures des trois phases**

Depuis une console NON élevée, avec `powershell.exe` 5.1 (jamais pwsh - DPI), curseur écarté en (4,4) par le script de capture :

```
powershell -ExecutionPolicy Bypass -File .\tools\Capture-Fenetre.ps1 -Phase Prepare -Out docs\img\gui-prepare.png
powershell -ExecutionPolicy Bypass -File .\tools\Capture-Fenetre.ps1 -Phase Running -Out docs\img\gui-run.png
powershell -ExecutionPolicy Bypass -File .\tools\Capture-Fenetre.ps1 -Phase Done    -Out docs\img\gui-close.png
```

(Si tools/Capture-Fenetre.ps1 expose d'autres paramètres, s'y conformer - lire son en-tête. Il est untracked : ne pas le committer.)

- [ ] **Step 3 : RELECTURE des captures (le contrôle qui a manqué à la v2.2)**

Ouvrir chaque PNG (outil Read) et vérifier point par point :
- Prepare : étapes numérotées 1..15 sans saut, noms français, « Rapport » en 15 ; liste des profils sur « standard » ; PAS de bouton Appliquer ; bouton « ▶  LANCER L'INTERVENTION » ; case « Simulation : montrer ce qui serait fait... » lisible et non tronquée ; UN SEUL onglet (Aide) ; bandeau épingle visible ; panneau d'aide = rubrique du profil standard ; badge APERÇU ; « PC-DEMO » partout, zéro hostname réel.
- Running : onglets Aide + Journal, journal actif ; états de timeline ; libellé « Étape N/M - <nom français> ».
- Close : bilan accordé, checklist, onglets présents.
- Les trois : zéro « dry-run », zéro nom anglais de module, zéro em-dash.
Toute anomalie = retour à la tâche fautive AVANT de committer les captures.

- [ ] **Step 4 : README et notes de release**

README : mettre à jour la section « Préparer » (profil appliqué à la sélection, entrée (personnalisé)), la section aide (délai, gel, épingle, Échap), la section préfixe de titre (déjà Task 2 - vérifier), la phrase de numérotation (déjà Task 1 - vérifier). Créer `docs/RELEASE-NOTES-v2.3.0.md` :

```markdown
# PC-Refresh-Kit v2.3.0 - passe UX terrain

Corrections issues du premier retour terrain de la v2.2 : l'interface se comporte
désormais comme elle se lit.

## Profils
- Sélectionner un profil applique immédiatement ses cases (plus de bouton Appliquer).
- Au démarrage, le profil standard est appliqué d'office.
- L'entrée (personnalisé) apparaît dès qu'une case est modifiée à la main ;
  Enregistrer comme profil la conserve sous un nom.

## Lisibilité
- La colonne d'intervention affiche les étapes 1 à 15, en français, dans l'ordre
  réel d'exécution (le Rapport clôt l'intervention en étape 15).
- Un seul vocabulaire de mode : Simulation / Intervention réelle - sur la case,
  le badge, le titre de fenêtre et le bouton principal (LANCER LA SIMULATION /
  LANCER L'INTERVENTION).
- L'onglet Journal n'apparaît qu'au lancement, quand il a quelque chose à dire.

## Aide intégrée
- Le survol ne remplace le panneau qu'après un court délai : traverser l'écran
  ne fait plus disparaître ce que vous lisiez.
- Entrer dans le panneau fige son contenu : lecture et défilement tranquilles.
- Une épingle en tête du panneau fige la rubrique affichée (Échap la libère).

## Qualité
- Nouveau parcours utilisateur scripté (Run-GUI.ps1 -SelfTest, 17 assertions)
  exécuté en CI : profils, étapes, mode et journal sont vérifiés à chaque commit.
```

(Ajuster le compte d'assertions au réel de la Task 6.)

- [ ] **Step 5 : Vérifications et commit**

```
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitEncoding.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitAnonymity.ps1 -CI
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
powershell -ExecutionPolicy Bypass -File Run-GUI.ps1 -SelfTest
git add -A
git commit -m "docs: captures et documentation v2.3"
```

---

### Task 8: Validation finale de branche

**Files:**
- Aucune modification de code attendue (toute anomalie renvoie à la tâche fautive).
- Rapport : `.superpowers/sdd/task-8-report.md`

- [ ] **Step 1 : Suite complète**

```
pwsh -NoProfile -Command "Invoke-Pester -Path .\tests -CI"
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitParse.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitEncoding.ps1 -CI
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitAnonymity.ps1 -CI
powershell -ExecutionPolicy Bypass -File Run-GUI.ps1 -SelfTest
powershell -ExecutionPolicy Bypass -File .\tests\Invoke-SmokeTest.ps1 -CI
```

Attendu : Pester 0 échec ; Parse/Encoding/Anonymity 0 violation ; SelfTest exit 0 ; smoke : échecs « porte d'élévation » attendus hors admin (documentés, couverts par la CI distante élevée) - toute AUTRE erreur est bloquante.

- [ ] **Step 2 : État git et anonymat d'historique**

```
git status --short
git log --oneline public-main..HEAD
powershell -ExecutionPolicy Bypass -File .\tests\Test-KitAnonymity.ps1 -History
```

Attendu : arbre propre ; historique lisible (feat/fix/docs/test) ; -History : violations UNIQUEMENT si des blobs hérités en portent (la branche part de public-main propre : attendu 0 - toute violation est bloquante AVANT toute publication).

- [ ] **Step 3 : Rapport**

Écrire `.superpowers/sdd/task-8-report.md` : résultats chiffrés de chaque contrôle, anomalies et leur traitement, verdict « prêt pour revue finale de branche ». Pas de commit (rapport non versionné, .superpowers/ est ignoré).

---

## Self-review du plan (faite à l'écriture)

- Couverture spec : G1 -> Task 3 ; G2 -> Task 4 ; G3 -> Task 1 ; G4 -> Task 2 ; G5 -> Task 5 ; G6 -> Tasks 1+2+7 (relecture sur captures) ; garde-fou §8 -> Task 6 ; critères §11 couverts par SelfTest (T6) + captures (T7).
- Cohérence de types : `New-KitModuleRow -Index/-Name` (T1) utilisée par T1/T6 ; `Update-KitRunButtonText` (T2) appelée par T2/T6 ; `Show-KitJournalTab` (T3) par T3/T6 ; `$script:CustomProfileLabel`/`Invoke-KitProfileSelection`/`Set-KitProfileCustom`/`Update-ProfileComboBox -Select` (T4) par T4/T6 ; `Get-KitHelpDecision`/`Request-KitHelp`/`Invoke-KitHelpRequest`/`Set-KitHelpPinned` (T5) par T5 seulement.
- Points de vigilance transmis aux implémenteurs : ordre de docking (T5), GetNewClosure par copie (T5), getter Visible interdit en SelfTest (T6), handlers empilés et jamais fusionnés entre T2 et T4, repères de lignes qui glissent.
