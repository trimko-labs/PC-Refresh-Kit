# Refonte cockpit Trimko v2.2 - plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Donner au cockpit l'identité visuelle Trimko et un parcours opérateur en trois phases (préparer, exécuter, clôturer), en absorbant la documentation intégrée v2.2, sans toucher à la mécanique de run éprouvée.

**Architecture:** Un nouveau `lib/Theme.ps1` (palette + fabriques de contrôles + helpers purs) alimente un `Run-GUI.ps1` restructuré en TableLayoutPanel (bandeau / corps 2 colonnes / barre d'action à 3 états). La colonne des modules devient une timeline d'états pendant le run. L'aide intégrée (catalogue + lib/Help.ps1) est reprise du plan `2026-08-14-documentation-integree.md`, dont seule la partie « intégration cockpit » est remplacée.

**Tech Stack:** PowerShell 5.1 natif, WinForms, Pester 5.5+, aucune dépendance externe.

**Spécifications de référence:**
- `docs/superpowers/specs/2026-08-21-refonte-cockpit-trimko-design.md` (LA référence de cette vague)
- `docs/superpowers/specs/2026-08-14-documentation-integree-design.md` (contrat aide/profils, sections 4 et 6 à 10)
- `docs/superpowers/plans/2026-08-14-documentation-integree.md` (code complet des tâches aide/profils, réutilisé par les Tasks 10, 12 et 14)

## Global Constraints

- **Branche :** travailler sur `feat/v2.2-cockpit-trimko` (déjà créée depuis public-main). AUCUN push tant que la revue finale de branche n'est pas passée et que le mainteneur n'a pas donné son accord explicite.
- **Identité de commit :** `git -c user.name=Trimko -c user.email=contact@trimko.com commit ...` et terminer chaque message par les deux trailers de session (Co-Authored-By et Claude-Session), fournis verbatim par le contrôleur dans chaque dispatch. Ils ne sont volontairement PAS recopiés ici : le garde-fou d'anonymat interdit toute adresse e-mail hors domaine du projet dans les fichiers suivis, et ce plan est un fichier suivi. Messages de commit en ASCII sans accents ni apostrophes (hook local).
- **Encodage :** tout `.ps1` créé ou modifié en **UTF-8 avec BOM** + newline finale, sinon `tests\Test-KitEncoding.ps1` échoue. Après création : `$c = Get-Content $f -Raw -Encoding UTF8 ; [System.IO.File]::WriteAllText((Resolve-Path $f), $c, (New-Object System.Text.UTF8Encoding($true)))`. Les `.json` restent UTF-8 **sans** BOM.
- **Typographie :** aucun U+2014 ni U+2013 nulle part. Utiliser ` - `. Accents français corrects dans tout texte affiché et commentaire.
- **Anonymat :** aucun nom de personne, chemin `C:\Users\<nom>`, e-mail hors `@trimko.com` dans un fichier suivi. La marque « Trimko Labs » est l'identité publique : autorisée partout.
- **Contrôles avant chaque commit de tâche :** `.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI` : zéro erreur.
- **Tests :** `pwsh -c "Invoke-Pester .\tests\<fichier> -Output Detailed"` (Pester 5 est sous pwsh, pas sous PowerShell 5.1). Baseline au départ : TOUTE la suite `tests/` est verte ; aucune tâche ne fait baisser le compte.
- **Piège PS 5.1 collections :** une fonction qui retourne une collection suit UN des deux contrats : (a) `return ,@(...)` + appel par assignation directe ; (b) `return` nu + `@()` chez l'appelant. Documenter le contrat en commentaire. Ne jamais mélanger.
- **StrictMode :** `Set-StrictMode -Version Latest` partout ; garde `@()` sur tout `Get-Content`/`Get-ChildItem` compté ; `PSObject.Properties[...]` avant d'accéder à une propriété incertaine.
- **INTERDIT :** exécuter le kit en réel ou en -WhatIf élevé sur cette machine. La validation visuelle passe EXCLUSIVEMENT par `-UiPreview` (non élevé, aucune écriture) et `tools/Capture-Fenetre.ps1`. Ne pas toucher `vendor/TelemetryGuard`.
- **Boucle visuelle standard** (utilisée par plusieurs tâches, session NON élevée) :
  ```powershell
  $p = Start-Process powershell -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File','.\Run-GUI.ps1','-UiPreview' -PassThru
  Start-Sleep -Seconds 5
  $png = .\tools\Capture-Fenetre.ps1 -TitrePattern '*PC-Refresh-Kit*'
  $p | Stop-Process -Force
  $png   # lire ce PNG avec l'outil Read pour verifier le rendu
  ```

---

### Task 1: lib/Theme.ps1 - palette et helpers purs

**Files:**
- Create: `lib/Theme.ps1`
- Create: `tests/Theme.Tests.ps1`
- Modify: `lib/Common.ps1` (dot-sourcing en tête, à côté de celui de Log.ps1)

**Interfaces:**
- Consumes: `Get-LogLineParts -Line <string>` et `Format-Elapsed -Seconds <int>` (lib/Log.ps1) ; `Get-ReportSummary -Lines <string[]>` (lib/Report.ps1, champs `.CountOK`, `.CountWarn`, `.CountError`).
- Produces: `Get-KitPalette` (hashtable nom -> hex string) ; `Get-KitLogLevelColorHex -Line <string>` -> hex ; `Get-ModuleStateGlyph -State <string> -Mdl2Available <bool>` -> PSCustomObject { Glyph, ColorHex } ; `Get-RunSummaryText -ModuleCount <int> -SensitiveCount <int> -ProfileName <string> -IsDryRun <bool>` -> string ; `Get-ModuleTrancheState -ExitCode <int> -TrancheLines <string[]>` -> 'Ok'|'Warn'|'Error'.

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `tests/Theme.Tests.ps1` :

```powershell
# tests/Theme.Tests.ps1 - Tests Pester v5 de lib/Theme.ps1 (palette, helpers purs, fabriques).
# Les fabriques WinForms sont instanciées SANS ShowDialog : proprietes verifiees puis Dispose.
BeforeAll {
    $script:KitLogFile = Join-Path $env:TEMP "pester-theme-$(New-Guid).log"
    . "$PSScriptRoot\..\lib\Common.ps1"
    Add-Type -AssemblyName System.Windows.Forms
    Add-Type -AssemblyName System.Drawing
}
AfterAll {
    if (Test-Path $script:KitLogFile) { Remove-Item $script:KitLogFile -Force -ErrorAction SilentlyContinue }
}

Describe 'Get-KitPalette' {
    BeforeAll { $script:P = Get-KitPalette }

    It 'expose les couleurs de la charte Trimko (spec 3.1)' {
        $script:P.Accent     | Should -Be '#0d9488'
        $script:P.AccentDark | Should -Be '#0f766e'
        $script:P.Ink        | Should -Be '#1e293b'
        $script:P.Ground     | Should -Be '#f8fafc'
        $script:P.Card       | Should -Be '#ffffff'
        $script:P.Line       | Should -Be '#e2e8f0'
        $script:P.Ok         | Should -Be '#16a34a'
        $script:P.Warn       | Should -Be '#d97706'
        $script:P.Err        | Should -Be '#dc2626'
        $script:P.JournalBg  | Should -Be '#0f172a'
    }

    It 'aligne les couleurs de journal sur celles du rapport HTML' {
        $script:P.LogOK     | Should -Be '#4ade80'
        $script:P.LogWarn   | Should -Be '#fbbf24'
        $script:P.LogError  | Should -Be '#f87171'
        $script:P.LogWhatIf | Should -Be '#22d3ee'
        $script:P.LogInfo   | Should -Be '#cbd5e1'
    }

    It 'ne contient que des hex valides' {
        foreach ($k in $script:P.Keys) {
            $script:P[$k] | Should -Match '^#[0-9a-f]{6}$' -Because "$k doit etre un hex minuscule"
        }
    }
}

Describe 'Get-KitLogLevelColorHex' {
    It 'mappe chaque niveau sur la couleur du rapport' {
        Get-KitLogLevelColorHex -Line '[2026-08-21 10:00:00] [OK] fait'      | Should -Be '#4ade80'
        Get-KitLogLevelColorHex -Line '[2026-08-21 10:00:00] [WARN] gare'    | Should -Be '#fbbf24'
        Get-KitLogLevelColorHex -Line '[2026-08-21 10:00:00] [ERROR] rate'   | Should -Be '#f87171'
        Get-KitLogLevelColorHex -Line '[2026-08-21 10:00:00] [WHATIF] simule'| Should -Be '#22d3ee'
        Get-KitLogLevelColorHex -Line '[2026-08-21 10:00:00] [INFO] note'    | Should -Be '#cbd5e1'
    }
    It 'traite une ligne hors format comme INFO' {
        Get-KitLogLevelColorHex -Line 'ligne brute' | Should -Be '#cbd5e1'
        Get-KitLogLevelColorHex -Line ''            | Should -Be '#cbd5e1'
    }
}

Describe 'Get-ModuleStateGlyph' {
    It 'renvoie glyphe MDL2 et couleur pour chaque etat quand MDL2 est disponible' {
        (Get-ModuleStateGlyph -State 'Ok'      -Mdl2Available $true).Glyph    | Should -Be ([string][char]0xE73E)
        (Get-ModuleStateGlyph -State 'Running' -Mdl2Available $true).Glyph    | Should -Be ([string][char]0xE768)
        (Get-ModuleStateGlyph -State 'Error'   -Mdl2Available $true).Glyph    | Should -Be ([string][char]0xE711)
        (Get-ModuleStateGlyph -State 'Warn'    -Mdl2Available $true).ColorHex | Should -Be '#d97706'
        (Get-ModuleStateGlyph -State 'Ok'      -Mdl2Available $true).ColorHex | Should -Be '#16a34a'
    }
    It 'replie sur des caracteres Unicode sans MDL2' {
        (Get-ModuleStateGlyph -State 'Ok'      -Mdl2Available $false).Glyph | Should -Be ([string][char]0x2713)
        (Get-ModuleStateGlyph -State 'Running' -Mdl2Available $false).Glyph | Should -Be ([string][char]0x25B6)
        (Get-ModuleStateGlyph -State 'Error'   -Mdl2Available $false).Glyph | Should -Be 'x'
        (Get-ModuleStateGlyph -State 'Warn'    -Mdl2Available $false).Glyph | Should -Be '!'
    }
    It 'Pending et Skipped n utilisent jamais MDL2' {
        (Get-ModuleStateGlyph -State 'Pending' -Mdl2Available $true).Glyph | Should -Be ([string][char]0x25CB)
        (Get-ModuleStateGlyph -State 'Skipped' -Mdl2Available $true).Glyph | Should -Be '-'
    }
}

Describe 'Get-RunSummaryText' {
    It 'compose le resume de preparation' {
        Get-RunSummaryText -ModuleCount 14 -SensitiveCount 0 -ProfileName 'standard' -IsDryRun $true |
            Should -Be '14 modules - 0 action(s) sensible(s) - profil standard - SIMULATION'
    }
    It 'gere singulier, absence de profil et mode reel' {
        Get-RunSummaryText -ModuleCount 1 -SensitiveCount 2 -ProfileName '' -IsDryRun $false |
            Should -Be '1 module - 2 action(s) sensible(s) - INTERVENTION RÉELLE'
    }
}

Describe 'Get-ModuleTrancheState' {
    It 'Error si le code de sortie est non nul, quel que soit le log' {
        Get-ModuleTrancheState -ExitCode 3 -TrancheLines @('[2026-08-21 10:00:00] [OK] tout va bien') | Should -Be 'Error'
    }
    It 'Error si la tranche contient un ERROR' {
        Get-ModuleTrancheState -ExitCode 0 -TrancheLines @('[2026-08-21 10:00:00] [ERROR] echec') | Should -Be 'Error'
    }
    It 'Warn si la tranche contient un WARN sans ERROR' {
        Get-ModuleTrancheState -ExitCode 0 -TrancheLines @(
            '[2026-08-21 10:00:00] [OK] etape 1',
            '[2026-08-21 10:00:01] [WARN] detail') | Should -Be 'Warn'
    }
    It 'Ok sinon, y compris sur tranche vide ou nulle' {
        Get-ModuleTrancheState -ExitCode 0 -TrancheLines @('[2026-08-21 10:00:00] [OK] fait') | Should -Be 'Ok'
        Get-ModuleTrancheState -ExitCode 0 -TrancheLines @()    | Should -Be 'Ok'
        Get-ModuleTrancheState -ExitCode 0 -TrancheLines $null  | Should -Be 'Ok'
    }
}
```

- [ ] **Step 2: Lancer les tests pour vérifier l'échec**

Run: `pwsh -c "Invoke-Pester .\tests\Theme.Tests.ps1 -Output Detailed"`
Expected: FAIL, `Get-KitPalette` n'est pas reconnue (Theme.ps1 n'existe pas encore).

- [ ] **Step 3: Créer lib/Theme.ps1 (partie helpers purs)**

```powershell
# lib/Theme.ps1 - Identite visuelle Trimko du cockpit : palette, helpers purs,
# fabriques de controles WinForms. Dot-source par lib/Common.ps1 (apres Log.ps1
# et Report.ps1 : Get-KitLogLevelColorHex et Get-ModuleTrancheState en dependent).
# Source des couleurs : charte trimko.com, spec 2026-08-21 section 3.1.
# Encodage : UTF-8 avec BOM pour PowerShell 5.1.

Set-StrictMode -Version Latest

# ---------------------------------------------------------------------------
# Get-KitPalette : la palette Trimko en chaines hex. PURE, source unique de
# toute couleur du cockpit : aucune couleur en dur ailleurs.
# ---------------------------------------------------------------------------
function Get-KitPalette {
    [CmdletBinding()]
    param()
    return @{
        Accent       = '#0d9488'   # bandeau, LANCER, onglet actif, cases cochees
        AccentDark   = '#0f766e'   # fond badge SIMULATION, hover primaire
        AccentPale   = '#99f6e4'   # sous-titre bandeau, bordure encadre passphrase
        AccentGhost  = '#ccfbf1'   # texte secondaire sur fond teal
        AccentBgSoft = '#f0fdfa'   # fond encadre passphrase
        Ink          = '#1e293b'   # texte principal
        InkSoft      = '#475569'   # texte secondaire
        InkMuted     = '#64748b'   # eyebrows, durees, heartbeat
        Ground       = '#f8fafc'   # fond fenetre et zone droite
        Card         = '#ffffff'   # cartes, colonne intervention, barre d action
        Line         = '#e2e8f0'   # bordures 1px
        Ok           = '#16a34a'
        Warn         = '#d97706'   # avertissement, liseret actions sensibles, badge REEL
        Err          = '#dc2626'
        Skip         = '#94a3b8'   # modules ignores, etats en attente
        JournalBg    = '#0f172a'   # fond du journal (identique au pre.log du rapport)
        LogOK        = '#4ade80'   # niveaux du journal : memes hex que lib/Report.ps1
        LogWarn      = '#fbbf24'
        LogError     = '#f87171'
        LogWhatIf    = '#22d3ee'
        LogInfo      = '#cbd5e1'
        LogHeartbeat = '#64748b'
        BadgeRealBg     = '#d97706'
        BadgeRealBorder = '#fbbf24'
        BadgeSimBg      = '#0f766e'
        BadgeSimBorder  = '#2dd4bf'
        BadgePreviewBg  = '#475569'
    }
}

# ---------------------------------------------------------------------------
# Get-KitLogLevelColorHex : couleur hex d'une ligne de journal sur fond sombre.
# Get-LogLevelColor (Log.ps1) reste la reference du mode console sur fond
# clair : les deux coexistent, chacune pour son rendu. PURE.
# ---------------------------------------------------------------------------
function Get-KitLogLevelColorHex {
    [CmdletBinding()]
    param([AllowEmptyString()][AllowNull()][string]$Line)
    $p = Get-KitPalette
    $parts = Get-LogLineParts -Line $Line
    $level = if ($parts) { $parts.Level } else { 'INFO' }
    switch ($level) {
        'OK'     { return $p.LogOK }
        'WARN'   { return $p.LogWarn }
        'ERROR'  { return $p.LogError }
        'WHATIF' { return $p.LogWhatIf }
        default  { return $p.LogInfo }
    }
}

# ---------------------------------------------------------------------------
# Get-ModuleStateGlyph : glyphe et couleur d'un etat de module de la timeline.
# Glyphes Segoe MDL2 Assets (E73E coche, E768 lecture, E7BA attention, E711
# croix) avec repli Unicode si la police manque (tres vieux Windows 10). PURE.
# ---------------------------------------------------------------------------
function Get-ModuleStateGlyph {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][ValidateSet('Pending','Running','Ok','Warn','Error','Skipped')][string]$State,
        [bool]$Mdl2Available = $true
    )
    $p = Get-KitPalette
    switch ($State) {
        'Pending' { return [PSCustomObject]@{ Glyph = [string][char]0x25CB; ColorHex = $p.Skip } }
        'Skipped' { return [PSCustomObject]@{ Glyph = '-';                  ColorHex = $p.Skip } }
        'Running' {
            $g = if ($Mdl2Available) { [string][char]0xE768 } else { [string][char]0x25B6 }
            return [PSCustomObject]@{ Glyph = $g; ColorHex = $p.Accent }
        }
        'Ok' {
            $g = if ($Mdl2Available) { [string][char]0xE73E } else { [string][char]0x2713 }
            return [PSCustomObject]@{ Glyph = $g; ColorHex = $p.Ok }
        }
        'Warn' {
            $g = if ($Mdl2Available) { [string][char]0xE7BA } else { '!' }
            return [PSCustomObject]@{ Glyph = $g; ColorHex = $p.Warn }
        }
        'Error' {
            $g = if ($Mdl2Available) { [string][char]0xE711 } else { 'x' }
            return [PSCustomObject]@{ Glyph = $g; ColorHex = $p.Err }
        }
    }
}

# ---------------------------------------------------------------------------
# Get-RunSummaryText : texte du resume de la barre d'action en preparation. PURE.
# ---------------------------------------------------------------------------
function Get-RunSummaryText {
    [CmdletBinding()]
    param(
        [int]$ModuleCount,
        [int]$SensitiveCount,
        [AllowEmptyString()][AllowNull()][string]$ProfileName,
        [bool]$IsDryRun
    )
    $mode   = if ($IsDryRun) { 'SIMULATION' } else { 'INTERVENTION RÉELLE' }
    $plural = if ($ModuleCount -gt 1) { 's' } else { '' }
    $prof   = if ([string]::IsNullOrWhiteSpace($ProfileName)) { '' } else { " - profil $ProfileName" }
    return "$ModuleCount module$plural - $SensitiveCount action(s) sensible(s)$prof - $mode"
}

# ---------------------------------------------------------------------------
# Get-ModuleTrancheState : etat final d'un module depuis son code de sortie et
# SA tranche du log unifie (bornee par l'offset au demarrage du module).
# Reutilise Get-ReportSummary (Report.ps1) : aucun double comptage. PURE.
# ---------------------------------------------------------------------------
function Get-ModuleTrancheState {
    [CmdletBinding()]
    param(
        [int]$ExitCode,
        [AllowNull()][string[]]$TrancheLines
    )
    if ($ExitCode -ne 0) { return 'Error' }
    $sum = Get-ReportSummary -Lines $TrancheLines
    if ($sum.CountError -gt 0) { return 'Error' }
    if ($sum.CountWarn -gt 0)  { return 'Warn' }
    return 'Ok'
}
```

- [ ] **Step 4: Brancher le dot-sourcing dans lib/Common.ps1**

Ouvrir `lib/Common.ps1`, repérer en tête la série de dot-sourcing des libs (`. "$PSScriptRoot\Log.ps1"` etc.). Ajouter `Theme.ps1` EN DERNIER de la série (il consomme Log.ps1 et Report.ps1) :

```powershell
. "$PSScriptRoot\Theme.ps1"
```

- [ ] **Step 5: Convertir en UTF-8 BOM et relancer les tests**

```powershell
foreach ($f in 'lib\Theme.ps1','tests\Theme.Tests.ps1') {
    $c = Get-Content $f -Raw -Encoding UTF8
    [System.IO.File]::WriteAllText((Resolve-Path $f), $c, (New-Object System.Text.UTF8Encoding($true)))
}
```

Run: `pwsh -c "Invoke-Pester .\tests\Theme.Tests.ps1 -Output Detailed"`
Expected: PASS intégral.

Run: `pwsh -c "Invoke-Pester .\tests\Common.Tests.ps1 -Output Detailed"`
Expected: le compte de départ inchangé (le dot-sourcing ajouté ne casse rien).

- [ ] **Step 6: Contrôles CI et commit**

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add lib/Theme.ps1 lib/Common.ps1 tests/Theme.Tests.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: palette Trimko et helpers purs du theme (lib/Theme.ps1)"
```

---

### Task 2: lib/Theme.ps1 - fabriques de contrôles

**Files:**
- Modify: `lib/Theme.ps1` (ajout en fin de fichier)
- Modify: `tests/Theme.Tests.ps1` (ajout)

**Interfaces:**
- Consumes: `Get-KitPalette`, `Get-ModuleStateGlyph`, `Format-Elapsed` (Task 1 et lib/Log.ps1).
- Produces:
  - `ConvertTo-KitColor -Hex <string>` -> System.Drawing.Color
  - `Test-KitMdl2Available` -> bool
  - `Set-KitButtonStyle -Button <Button> -Kind Primary|Ghost|Mini|MiniGhost|Danger` (style un bouton EXISTANT, garde ses handlers)
  - `New-KitButton -Text <string> -Kind <string>` -> Button
  - `New-KitCard -WarnAccent <switch>` -> Panel (fond blanc, bordure Line via Paint, liseré Warn optionnel)
  - `New-KitEyebrow -Text <string>` -> Label
  - `New-KitBand -Machine <string>` -> PSCustomObject { Panel, BadgeLabel }
  - `Set-KitBadgeMode -Band <object> -Mode Simulation|Real|Preview`
  - `New-KitModuleRow -Id <string> -Name <string> -Mdl2Available <bool>` -> PSCustomObject { Panel, CheckBox, GlyphLabel, NameLabel, DetailLabel, Id }
  - `Set-KitModuleRowState -Row <object> -State <string> -Detail <string> -Mdl2Available <bool>`
  - `Show-KitInputDialog -Owner <Form> -Title <string> -Prompt <string>` -> string ou $null

- [ ] **Step 1: Ajouter les tests des fabriques**

Ajouter à la fin de `tests/Theme.Tests.ps1` :

```powershell
Describe 'ConvertTo-KitColor' {
    It 'convertit un hex en Color' {
        $c = ConvertTo-KitColor -Hex '#0d9488'
        $c.R | Should -Be 13 ; $c.G | Should -Be 148 ; $c.B | Should -Be 136
    }
}

Describe 'New-KitButton et Set-KitButtonStyle' {
    It 'Primary : fond accent, texte blanc, flat sans bordure' {
        $b = New-KitButton -Text 'LANCER' -Kind Primary
        try {
            $b.FlatStyle | Should -Be 'Flat'
            $b.FlatAppearance.BorderSize | Should -Be 0
            $b.BackColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#0d9488').ToArgb()
            $b.ForeColor.ToArgb() | Should -Be ([System.Drawing.Color]::White).ToArgb()
            $b.Font.Bold | Should -BeTrue
        } finally { $b.Dispose() }
    }
    It 'Ghost : fond carte, bordure fine, texte doux' {
        $b = New-KitButton -Text 'Annuler' -Kind Ghost
        try {
            $b.FlatAppearance.BorderSize | Should -Be 1
            $b.BackColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#ffffff').ToArgb()
        } finally { $b.Dispose() }
    }
    It 'Danger : texte rouge sur fond carte' {
        $b = New-KitButton -Text 'Supprimer' -Kind Danger
        try { $b.ForeColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#dc2626').ToArgb() }
        finally { $b.Dispose() }
    }
    It 'Set-KitButtonStyle restyle un bouton existant sans le remplacer' {
        $b = New-Object System.Windows.Forms.Button
        try {
            Set-KitButtonStyle -Button $b -Kind Primary
            $b.FlatStyle | Should -Be 'Flat'
            $b.BackColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#0d9488').ToArgb()
        } finally { $b.Dispose() }
    }
}

Describe 'New-KitCard' {
    It 'carte blanche avec padding' {
        $c = New-KitCard
        try {
            $c.BackColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#ffffff').ToArgb()
            $c.Padding.Left | Should -BeGreaterThan 0
        } finally { $c.Dispose() }
    }
    It 'liseret d avertissement en option' {
        $c = New-KitCard -WarnAccent
        try { $c.Padding.Left | Should -BeGreaterThan (New-KitCard).Padding.Left }
        finally { $c.Dispose() }
    }
}

Describe 'New-KitEyebrow' {
    It 'label majuscules gris' {
        $l = New-KitEyebrow -Text 'Modules'
        try {
            $l.Text | Should -Be 'MODULES'
            $l.ForeColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#64748b').ToArgb()
            $l.Font.Bold | Should -BeTrue
        } finally { $l.Dispose() }
    }
}

Describe 'New-KitBand et Set-KitBadgeMode' {
    It 'bandeau teal avec nom produit, machine et badge' {
        $band = New-KitBand -Machine 'PC-TEST'
        try {
            $band.Panel.BackColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#0d9488').ToArgb()
            $band.BadgeLabel | Should -Not -BeNullOrEmpty
            ($band.Panel.Controls | ForEach-Object { $_.Text }) -join ' ' | Should -Match 'PC-TEST'
        } finally { $band.Panel.Dispose() }
    }
    It 'le badge change avec le mode' {
        $band = New-KitBand -Machine 'PC-TEST'
        try {
            Set-KitBadgeMode -Band $band -Mode Real
            $band.BadgeLabel.Text | Should -Be 'INTERVENTION RÉELLE'
            $band.BadgeLabel.BackColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#d97706').ToArgb()
            Set-KitBadgeMode -Band $band -Mode Simulation
            $band.BadgeLabel.Text | Should -Be 'SIMULATION'
            Set-KitBadgeMode -Band $band -Mode Preview
            $band.BadgeLabel.Text | Should -Be 'APERÇU'
        } finally { $band.Panel.Dispose() }
    }
}

Describe 'New-KitModuleRow et Set-KitModuleRowState' {
    It 'ligne avec case cochee par defaut, id et nom' {
        $r = New-KitModuleRow -Id '03' -Name 'Debloat' -Mdl2Available $false
        try {
            $r.Id | Should -Be '03'
            $r.CheckBox.Checked | Should -BeTrue
            $r.CheckBox.Visible | Should -BeTrue
            $r.GlyphLabel.Visible | Should -BeFalse
            $r.NameLabel.Text | Should -Be 'Debloat'
        } finally { $r.Panel.Dispose() }
    }
    It 'Running : glyphe visible, case masquee, nom en gras, detail affiche' {
        $r = New-KitModuleRow -Id '03' -Name 'Debloat' -Mdl2Available $false
        try {
            Set-KitModuleRowState -Row $r -State Running -Detail 'en cours - 01:12' -Mdl2Available $false
            $r.CheckBox.Visible | Should -BeFalse
            $r.GlyphLabel.Visible | Should -BeTrue
            $r.GlyphLabel.Text | Should -Be ([string][char]0x25B6)
            $r.NameLabel.Font.Bold | Should -BeTrue
            $r.DetailLabel.Text | Should -Be 'en cours - 01:12'
        } finally { $r.Panel.Dispose() }
    }
    It 'Ok puis Skipped : glyphes, couleurs et graisse coherents' {
        $r = New-KitModuleRow -Id '05' -Name 'Updates' -Mdl2Available $false
        try {
            Set-KitModuleRowState -Row $r -State Ok -Detail '08:05' -Mdl2Available $false
            $r.GlyphLabel.ForeColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#16a34a').ToArgb()
            $r.NameLabel.Font.Bold | Should -BeFalse
            Set-KitModuleRowState -Row $r -State Skipped -Detail 'ignoré' -Mdl2Available $false
            $r.NameLabel.ForeColor.ToArgb() | Should -Be (ConvertTo-KitColor -Hex '#94a3b8').ToArgb()
        } finally { $r.Panel.Dispose() }
    }
    It 'Pending remet la case visible (retour preparation)' {
        $r = New-KitModuleRow -Id '05' -Name 'Updates' -Mdl2Available $false
        try {
            Set-KitModuleRowState -Row $r -State Ok -Detail '08:05' -Mdl2Available $false
            Set-KitModuleRowState -Row $r -State Pending -Detail '' -Mdl2Available $false
            $r.CheckBox.Visible | Should -BeTrue
            $r.GlyphLabel.Visible | Should -BeFalse
            $r.DetailLabel.Text | Should -Be ''
        } finally { $r.Panel.Dispose() }
    }
}
```

- [ ] **Step 2: Vérifier l'échec**

Run: `pwsh -c "Invoke-Pester .\tests\Theme.Tests.ps1 -Output Detailed"`
Expected: FAIL sur les nouveaux Describe (fonctions absentes) ; les Describe de Task 1 restent PASS.

- [ ] **Step 3: Ajouter les fabriques à lib/Theme.ps1**

Ajouter en fin de `lib/Theme.ps1` :

```powershell
# ===========================================================================
# Fabriques de controles WinForms. Testables sans affichage : instancier,
# verifier les proprietes, Dispose. Aucun ShowDialog ici sauf Show-KitInputDialog.
# ===========================================================================

function ConvertTo-KitColor {
    # Hex '#rrggbb' -> System.Drawing.Color.
    [CmdletBinding()]
    param([Parameter(Mandatory)][string]$Hex)
    return [System.Drawing.ColorTranslator]::FromHtml($Hex)
}

function Test-KitMdl2Available {
    # $true si la police Segoe MDL2 Assets est installee (Windows 10+ standard).
    [CmdletBinding()]
    param()
    try {
        $ff = New-Object System.Drawing.FontFamily 'Segoe MDL2 Assets'
        $ff.Dispose()
        return $true
    } catch { return $false }
}

function Set-KitButtonStyle {
    # Style Trimko applique a un bouton EXISTANT (handlers conserves).
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][System.Windows.Forms.Button]$Button,
        [Parameter(Mandatory)][ValidateSet('Primary','Ghost','Mini','MiniGhost','Danger')][string]$Kind
    )
    $p = Get-KitPalette
    $Button.FlatStyle = 'Flat'
    $Button.UseVisualStyleBackColor = $false
    $Button.Cursor = [System.Windows.Forms.Cursors]::Hand
    switch ($Kind) {
        'Primary' {
            $Button.FlatAppearance.BorderSize = 0
            $Button.BackColor = ConvertTo-KitColor $p.Accent
            $Button.ForeColor = [System.Drawing.Color]::White
            $Button.Font = New-Object System.Drawing.Font('Segoe UI', 10.5, [System.Drawing.FontStyle]::Bold)
            $Button.FlatAppearance.MouseOverBackColor = ConvertTo-KitColor $p.AccentDark
        }
        'Ghost' {
            $Button.FlatAppearance.BorderSize = 1
            $Button.FlatAppearance.BorderColor = ConvertTo-KitColor $p.Line
            $Button.BackColor = ConvertTo-KitColor $p.Card
            $Button.ForeColor = ConvertTo-KitColor $p.InkSoft
            $Button.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)
        }
        'Mini' {
            $Button.FlatAppearance.BorderSize = 1
            $Button.FlatAppearance.BorderColor = ConvertTo-KitColor $p.Accent
            $Button.BackColor = ConvertTo-KitColor $p.Card
            $Button.ForeColor = ConvertTo-KitColor $p.Accent
            $Button.Font = New-Object System.Drawing.Font('Segoe UI', 8.25)
        }
        'MiniGhost' {
            $Button.FlatAppearance.BorderSize = 1
            $Button.FlatAppearance.BorderColor = ConvertTo-KitColor $p.Line
            $Button.BackColor = ConvertTo-KitColor $p.Card
            $Button.ForeColor = ConvertTo-KitColor $p.InkMuted
            $Button.Font = New-Object System.Drawing.Font('Segoe UI', 8.25)
        }
        'Danger' {
            $Button.FlatAppearance.BorderSize = 1
            $Button.FlatAppearance.BorderColor = ConvertTo-KitColor '#fca5a5'
            $Button.BackColor = ConvertTo-KitColor $p.Card
            $Button.ForeColor = ConvertTo-KitColor $p.Err
            $Button.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)
        }
    }
}

function New-KitButton {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][string]$Text,
        [Parameter(Mandatory)][ValidateSet('Primary','Ghost','Mini','MiniGhost','Danger')][string]$Kind
    )
    $b = New-Object System.Windows.Forms.Button
    $b.Text = $Text
    Set-KitButtonStyle -Button $b -Kind $Kind
    return $b
}

function New-KitCard {
    # Panel blanc, bordure 1px Line dessinee au Paint, padding interieur.
    # -WarnAccent ajoute un liseret gauche 3px Warn (actions sensibles).
    [CmdletBinding()]
    param([switch]$WarnAccent)
    $p = Get-KitPalette
    $card = New-Object System.Windows.Forms.Panel
    $card.BackColor = ConvertTo-KitColor $p.Card
    $card.Padding = if ($WarnAccent) { New-Object System.Windows.Forms.Padding(15, 10, 12, 10) }
                    else             { New-Object System.Windows.Forms.Padding(12, 10, 12, 10) }
    $lineColor = ConvertTo-KitColor $p.Line
    $warnColor = ConvertTo-KitColor $p.Warn
    $hasWarn   = [bool]$WarnAccent
    $card.Add_Paint({
        param($sender, $e)
        $rect = $sender.ClientRectangle
        $pen = New-Object System.Drawing.Pen $lineColor
        try { $e.Graphics.DrawRectangle($pen, 0, 0, $rect.Width - 1, $rect.Height - 1) }
        finally { $pen.Dispose() }
        if ($hasWarn) {
            $brush = New-Object System.Drawing.SolidBrush $warnColor
            try { $e.Graphics.FillRectangle($brush, 0, 0, 3, $rect.Height) }
            finally { $brush.Dispose() }
        }
    }.GetNewClosure())
    return $card
}

function New-KitEyebrow {
    [CmdletBinding()]
    param([Parameter(Mandatory)][string]$Text)
    $p = Get-KitPalette
    $l = New-Object System.Windows.Forms.Label
    $l.Text = $Text.ToUpperInvariant()
    $l.AutoSize = $true
    $l.ForeColor = ConvertTo-KitColor $p.InkMuted
    $l.Font = New-Object System.Drawing.Font('Segoe UI', 8.25, [System.Drawing.FontStyle]::Bold)
    return $l
}

function New-KitBand {
    # Bandeau d'identite : nom produit + Trimko Labs, machine, badge de mode.
    [CmdletBinding()]
    param([Parameter(Mandatory)][string]$Machine)
    $p = Get-KitPalette
    $panel = New-Object System.Windows.Forms.Panel
    $panel.BackColor = ConvertTo-KitColor $p.Accent
    $panel.Height = 46

    $name = New-Object System.Windows.Forms.Label
    $name.Text = 'PC-Refresh-Kit'
    $name.ForeColor = [System.Drawing.Color]::White
    $name.Font = New-Object System.Drawing.Font('Segoe UI', 12, [System.Drawing.FontStyle]::Bold)
    $name.AutoSize = $true
    $name.Location = New-Object System.Drawing.Point(16, 5)

    $by = New-Object System.Windows.Forms.Label
    $by.Text = 'Trimko Labs'
    $by.ForeColor = ConvertTo-KitColor $p.AccentPale
    $by.Font = New-Object System.Drawing.Font('Segoe UI', 7.5)
    $by.AutoSize = $true
    $by.Location = New-Object System.Drawing.Point(18, 28)

    $machineLbl = New-Object System.Windows.Forms.Label
    $machineLbl.Text = "Machine : $Machine"
    $machineLbl.ForeColor = ConvertTo-KitColor $p.AccentGhost
    $machineLbl.Font = New-Object System.Drawing.Font('Segoe UI', 9)
    $machineLbl.AutoSize = $true
    $machineLbl.Anchor = 'Top,Right'

    $badge = New-Object System.Windows.Forms.Label
    $badge.AutoSize = $false
    $badge.Size = New-Object System.Drawing.Size(170, 24)
    $badge.TextAlign = 'MiddleCenter'
    $badge.Font = New-Object System.Drawing.Font('Segoe UI', 8.25, [System.Drawing.FontStyle]::Bold)
    $badge.Anchor = 'Top,Right'

    $panel.Controls.AddRange(@($name, $by, $machineLbl, $badge))
    # Positionnement droit recalcule au redimensionnement du bandeau.
    $panel.Add_Resize({
        param($sender, $e)
        $badge.Location     = New-Object System.Drawing.Point(($sender.Width - $badge.Width - 16), 11)
        $machineLbl.Location = New-Object System.Drawing.Point(($sender.Width - $badge.Width - 16 - $machineLbl.Width - 18), 15)
    }.GetNewClosure())

    return [PSCustomObject]@{ Panel = $panel; BadgeLabel = $badge }
}

function Set-KitBadgeMode {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][object]$Band,
        [Parameter(Mandatory)][ValidateSet('Simulation','Real','Preview')][string]$Mode
    )
    $p = Get-KitPalette
    switch ($Mode) {
        'Simulation' {
            $Band.BadgeLabel.Text = 'SIMULATION'
            $Band.BadgeLabel.BackColor = ConvertTo-KitColor $p.BadgeSimBg
            $Band.BadgeLabel.ForeColor = ConvertTo-KitColor $p.AccentGhost
        }
        'Real' {
            $Band.BadgeLabel.Text = 'INTERVENTION RÉELLE'
            $Band.BadgeLabel.BackColor = ConvertTo-KitColor $p.BadgeRealBg
            $Band.BadgeLabel.ForeColor = [System.Drawing.Color]::White
        }
        'Preview' {
            $Band.BadgeLabel.Text = 'APERÇU'
            $Band.BadgeLabel.BackColor = ConvertTo-KitColor $p.BadgePreviewBg
            $Band.BadgeLabel.ForeColor = ConvertTo-KitColor $p.Line
        }
    }
}

function New-KitModuleRow {
    # Ligne de la timeline d'intervention : case (preparation) OU glyphe d'etat
    # (run/cloture), id grise, nom, detail droit (duree/etat).
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][string]$Id,
        [Parameter(Mandatory)][string]$Name,
        [bool]$Mdl2Available = $true
    )
    $p = Get-KitPalette
    $panel = New-Object System.Windows.Forms.Panel
    $panel.Height = 24
    $panel.BackColor = ConvertTo-KitColor $p.Card

    $cbx = New-Object System.Windows.Forms.CheckBox
    $cbx.Checked = $true
    $cbx.AutoSize = $false
    $cbx.Size = New-Object System.Drawing.Size(16, 16)
    $cbx.Location = New-Object System.Drawing.Point(2, 4)

    $glyph = New-Object System.Windows.Forms.Label
    $glyph.Visible = $false
    $glyph.AutoSize = $false
    $glyph.Size = New-Object System.Drawing.Size(18, 18)
    $glyph.TextAlign = 'MiddleCenter'
    $glyph.Location = New-Object System.Drawing.Point(0, 3)
    $glyph.Font = if ($Mdl2Available) { New-Object System.Drawing.Font('Segoe MDL2 Assets', 9) }
                  else                { New-Object System.Drawing.Font('Segoe UI', 9) }

    $idLbl = New-Object System.Windows.Forms.Label
    $idLbl.Text = $Id
    $idLbl.AutoSize = $false
    $idLbl.Size = New-Object System.Drawing.Size(20, 18)
    $idLbl.Location = New-Object System.Drawing.Point(22, 4)
    $idLbl.ForeColor = ConvertTo-KitColor $p.Skip
    $idLbl.Font = New-Object System.Drawing.Font('Segoe UI', 7.5)
    $idLbl.TextAlign = 'MiddleLeft'

    $nameLbl = New-Object System.Windows.Forms.Label
    $nameLbl.Text = $Name
    $nameLbl.AutoSize = $true
    $nameLbl.Location = New-Object System.Drawing.Point(44, 4)
    $nameLbl.ForeColor = ConvertTo-KitColor $p.Ink
    $nameLbl.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)

    $detail = New-Object System.Windows.Forms.Label
    $detail.Text = ''
    $detail.AutoSize = $true
    $detail.ForeColor = ConvertTo-KitColor $p.InkMuted
    $detail.Font = New-Object System.Drawing.Font('Segoe UI', 8.25)
    $detail.Anchor = 'Top,Right'
    $panel.Add_Resize({
        param($sender, $e)
        $detail.Location = New-Object System.Drawing.Point(($sender.Width - $detail.Width - 4), 6)
    }.GetNewClosure())

    $panel.Controls.AddRange(@($cbx, $glyph, $idLbl, $nameLbl, $detail))
    return [PSCustomObject]@{
        Panel = $panel; CheckBox = $cbx; GlyphLabel = $glyph
        NameLabel = $nameLbl; DetailLabel = $detail; Id = $Id
    }
}

function Set-KitModuleRowState {
    # Bascule une ligne de timeline dans un etat. Pending = retour preparation
    # (case visible) ; tous les autres etats masquent la case au profit du glyphe.
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][object]$Row,
        [Parameter(Mandatory)][ValidateSet('Pending','Running','Ok','Warn','Error','Skipped')][string]$State,
        [AllowEmptyString()][string]$Detail = '',
        [bool]$Mdl2Available = $true
    )
    $p = Get-KitPalette
    $Row.DetailLabel.Text = $Detail
    if ($State -eq 'Pending') {
        $Row.CheckBox.Visible = $true
        $Row.GlyphLabel.Visible = $false
        $Row.NameLabel.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)
        $Row.NameLabel.ForeColor = ConvertTo-KitColor $p.Ink
        return
    }
    $g = Get-ModuleStateGlyph -State $State -Mdl2Available $Mdl2Available
    $Row.CheckBox.Visible = $false
    $Row.GlyphLabel.Visible = $true
    $Row.GlyphLabel.Text = $g.Glyph
    $Row.GlyphLabel.ForeColor = ConvertTo-KitColor $g.ColorHex
    $bold = if ($State -eq 'Running') { [System.Drawing.FontStyle]::Bold } else { [System.Drawing.FontStyle]::Regular }
    $Row.NameLabel.Font = New-Object System.Drawing.Font('Segoe UI', 9.75, $bold)
    $Row.NameLabel.ForeColor = if ($State -eq 'Skipped') { ConvertTo-KitColor $p.Skip } else { ConvertTo-KitColor $p.Ink }
}

function Show-KitInputDialog {
    # Remplace l'InputBox VisualBasic : petit dialogue charte. Renvoie la saisie
    # ou $null si Annuler/fermeture. Seule fonction du theme a faire ShowDialog :
    # exclue des tests unitaires.
    [CmdletBinding()]
    param(
        [AllowNull()][object]$Owner,
        [Parameter(Mandatory)][string]$Title,
        [Parameter(Mandatory)][string]$Prompt
    )
    $p = Get-KitPalette
    $dlg = New-Object System.Windows.Forms.Form
    $dlg.Text = $Title
    $dlg.Size = New-Object System.Drawing.Size(400, 160)
    $dlg.StartPosition = 'CenterParent'
    $dlg.FormBorderStyle = 'FixedDialog'
    $dlg.MaximizeBox = $false ; $dlg.MinimizeBox = $false
    $dlg.BackColor = ConvertTo-KitColor $p.Card

    $lbl = New-Object System.Windows.Forms.Label
    $lbl.Text = $Prompt
    $lbl.Location = New-Object System.Drawing.Point(15, 12)
    $lbl.Size = New-Object System.Drawing.Size(355, 34)
    $lbl.ForeColor = ConvertTo-KitColor $p.Ink
    $lbl.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)

    $txt = New-Object System.Windows.Forms.TextBox
    $txt.Location = New-Object System.Drawing.Point(15, 50)
    $txt.Size = New-Object System.Drawing.Size(355, 24)
    $txt.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)

    $ok = New-KitButton -Text 'Enregistrer' -Kind Primary
    $ok.Size = New-Object System.Drawing.Size(120, 30)
    $ok.Location = New-Object System.Drawing.Point(250, 85)
    $ok.DialogResult = 'OK'
    $cancel = New-KitButton -Text 'Annuler' -Kind Ghost
    $cancel.Size = New-Object System.Drawing.Size(90, 30)
    $cancel.Location = New-Object System.Drawing.Point(152, 85)
    $cancel.DialogResult = 'Cancel'

    $dlg.Controls.AddRange(@($lbl, $txt, $ok, $cancel))
    $dlg.AcceptButton = $ok ; $dlg.CancelButton = $cancel
    $result = if ($Owner) { $dlg.ShowDialog($Owner) } else { $dlg.ShowDialog() }
    $value = if ($result -eq 'OK') { $txt.Text } else { $null }
    $dlg.Dispose()
    return $value
}
```

- [ ] **Step 4: BOM, tests, contrôles, commit**

Reconvertir les deux fichiers en UTF-8 BOM (commande du Task 1 Step 5).

Run: `pwsh -c "Invoke-Pester .\tests\Theme.Tests.ps1 -Output Detailed"`
Expected: PASS intégral.

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add lib/Theme.ps1 tests/Theme.Tests.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: fabriques de controles du theme Trimko"
```

---

### Task 3: Mode aperçu -UiPreview et boucle de capture

**Files:**
- Modify: `Run-GUI.ps1:5-21` (param et contrôle admin), `Run-GUI.ps1:58-61` (titre)

**Interfaces:**
- Consumes: rien de nouveau.
- Produces: `Run-GUI.ps1 -UiPreview [-PreviewPhase Prepare|Running|Done]` s'ouvre SANS élévation, LANCER désactivé, aucune écriture disque. `-PreviewPhase` est accepté dès maintenant (défaut 'Prepare') ; les données factices des phases Running/Done arrivent en Task 9.

- [ ] **Step 1: Étendre le param et conditionner le contrôle admin**

Dans `Run-GUI.ps1`, remplacer :

```powershell
param([switch]$WhatIf)
```

par :

```powershell
param(
    [switch]$WhatIf,
    # -UiPreview : apercu de l'interface SANS elevation et sans aucune ecriture.
    # Sert aux contributeurs, aux demonstrations sans UAC et a la boucle de
    # verification visuelle (tools/Capture-Fenetre.ps1). LANCER est desactive.
    [switch]$UiPreview,
    [ValidateSet('Prepare','Running','Done')][string]$PreviewPhase = 'Prepare'
)
```

Puis remplacer le bloc :

```powershell
if (-not (Test-IsAdmin)) {
    [System.Windows.Forms.MessageBox]::Show(
        "Lancer via Lancer.bat (droits administrateur requis).",
        "PC-Refresh-Kit", 'OK', 'Warning') | Out-Null
    exit 1
}
```

par :

```powershell
if (-not $UiPreview -and -not (Test-IsAdmin)) {
    [System.Windows.Forms.MessageBox]::Show(
        "Lancer via Lancer.bat (droits administrateur requis).`r`nPour un simple aperçu de l'interface : Run-GUI.ps1 -UiPreview",
        "PC-Refresh-Kit", 'OK', 'Warning') | Out-Null
    exit 1
}
```

- [ ] **Step 2: Désactiver LANCER et marquer le titre en aperçu**

Juste APRÈS la ligne `$panelLeft.Controls.Add($btnRun)` (le bouton existe alors), ajouter :

```powershell
if ($UiPreview) {
    $btnRun.Enabled = $false
    $form.Text = "[APERÇU] PC-Refresh-Kit - $env:COMPUTERNAME"
}
```

- [ ] **Step 3: Valider la boucle de capture (première utilisation réelle)**

Exécuter la boucle visuelle standard des Global Constraints. Expected : un chemin de PNG affiché ; LIRE ce PNG (outil Read) : le cockpit ACTUEL (encore gris, c'est normal à ce stade) est visible, titre `[APERÇU]`, bouton LANCER grisé.

- [ ] **Step 4: Parse, contrôles, commit**

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: mode apercu -UiPreview sans elevation"
```

---

### Task 4: Squelette du nouveau layout (fenêtre, bandeau, hôtes, barre)

**Files:**
- Modify: `Run-GUI.ps1:57-224` (construction de la fenêtre et des zones)

**Interfaces:**
- Consumes: `New-KitBand`, `Set-KitBadgeMode`, `Get-KitPalette`, `ConvertTo-KitColor`, `Test-KitMdl2Available` (Tasks 1-2).
- Produces: `$script:Band` (bandeau, badge pilotable), `$script:HostLeft` (Panel colonne intervention, 300px), `$script:HostRight` (Panel zone droite), `$script:HostAction` (Panel barre d'action, vide à ce stade), `$script:Mdl2` (bool). Les contrôles existants sont re-parentés : colonne gauche dans `$script:HostLeft`, tout le reste de la colonne droite dans `$script:HostRight` (positions absolues conservées, AutoScroll transitoire).

- [ ] **Step 1: Remplacer la création de la fenêtre**

Remplacer :

```powershell
$form = New-Object System.Windows.Forms.Form
$form.Text = "PC-Refresh-Kit - $env:COMPUTERNAME"
$form.Size = New-Object System.Drawing.Size(900, 840)
$form.StartPosition = 'CenterScreen'
```

par :

```powershell
$script:Palette = Get-KitPalette
$script:Mdl2    = Test-KitMdl2Available

$form = New-Object System.Windows.Forms.Form
$form.Text = "PC-Refresh-Kit - $env:COMPUTERNAME"
$form.StartPosition = 'CenterScreen'
$form.AutoScaleMode = 'Dpi'                       # les tailles suivent le DPI (125/150 %)
$form.Size = New-Object System.Drawing.Size(1200, 720)
$form.MinimumSize = New-Object System.Drawing.Size(1000, 576)   # = 1250x720 physiques a 125 %
$form.BackColor = ConvertTo-KitColor $script:Palette.Ground

# Racine : bandeau (haut) / corps (centre) / barre d'action (bas).
$script:Band = New-KitBand -Machine $env:COMPUTERNAME
$script:Band.Panel.Dock = 'Top'

$script:HostAction = New-Object System.Windows.Forms.Panel
$script:HostAction.Dock = 'Bottom'
$script:HostAction.Height = 56
$script:HostAction.BackColor = ConvertTo-KitColor $script:Palette.Card

$body = New-Object System.Windows.Forms.TableLayoutPanel
$body.Dock = 'Fill'
$body.ColumnCount = 2
$body.RowCount = 1
[void]$body.ColumnStyles.Add((New-Object System.Windows.Forms.ColumnStyle([System.Windows.Forms.SizeType]::Absolute, 300)))
[void]$body.ColumnStyles.Add((New-Object System.Windows.Forms.ColumnStyle([System.Windows.Forms.SizeType]::Percent, 100)))

$script:HostLeft = New-Object System.Windows.Forms.Panel
$script:HostLeft.Dock = 'Fill'
$script:HostLeft.BackColor = ConvertTo-KitColor $script:Palette.Card
$script:HostLeft.Padding = New-Object System.Windows.Forms.Padding(12, 10, 12, 10)

$script:HostRight = New-Object System.Windows.Forms.Panel
$script:HostRight.Dock = 'Fill'
$script:HostRight.BackColor = ConvertTo-KitColor $script:Palette.Ground
$script:HostRight.Padding = New-Object System.Windows.Forms.Padding(12, 10, 12, 10)
$script:HostRight.AutoScroll = $true    # TRANSITOIRE : retire en Task 9, le temps que
                                        # les zones migrent depuis les positions absolues

$body.Controls.Add($script:HostLeft, 0, 0)
$body.Controls.Add($script:HostRight, 1, 0)

# Ordre d'ajout impose par le docking WinForms : Fill d'abord, puis Bottom, puis Top.
$form.Controls.Add($body)
$form.Controls.Add($script:HostAction)
$form.Controls.Add($script:Band.Panel)

# Badge de mode initial (le mode dry-run peut encore changer avant LANCER).
if ($UiPreview) { Set-KitBadgeMode -Band $script:Band -Mode Preview }
elseif ($WhatIf) { Set-KitBadgeMode -Band $script:Band -Mode Simulation }
else { Set-KitBadgeMode -Band $script:Band -Mode Real }
```

- [ ] **Step 2: Re-parenter les zones existantes**

Dans tout le reste de la construction (lignes actuelles 63-305) : remplacer chaque `$form.Controls.Add(...)` par le bon hôte, SANS changer les positions pour l'instant :

- `$panelLeft` : remplacer `$form.Controls.Add($panelLeft)` par `$script:HostLeft.Controls.Add($panelLeft)` et changer `$panelLeft.Location` en `New-Object System.Drawing.Point(0, 0)`.
- Contrôles de la colonne droite (`$txtLog`, `$progress`, `$lblPwd`, `$btnCopy`, `$btnReport`, `$script:GbChecklist`, `$btnDelFiche`) : remplacer chaque `$form.Controls.Add(X)` par `$script:HostRight.Controls.Add(X)` et soustraire 300 à chaque X de Location (ex : `$txtLog.Location` passe de `(300, 20)` à `(0, 10)` ; `$progress` de `(300, 425)` à `(0, 415)` ; `$lblPwd` de `(300, 455)` à `(0, 445)` ; `$btnCopy` de `(300, 480)` à `(0, 470)` ; `$btnReport` de `(490, 480)` à `(190, 470)` ; `$script:GbChecklist` de `(300, 520)` à `(0, 510)` ; `$btnDelFiche` de `(300, 748)` à `(0, 738)`).
- Bloc profils (`$lblProfileTitle`, `$cmbProfile`, `$btnApplyProfile`, `$btnSaveProfile`) : remplacer `$form.Controls.Add(X)` par `$script:HostLeft.Controls.Add(X)` avec les Y actuels moins 692 plus 640 provisoirement (`$lblProfileTitle` en `(0, 640)`, `$cmbProfile` en `(0, 658)`, `$btnApplyProfile` en `(0, 686)`, `$btnSaveProfile` en `(138, 686)`). La Task 5 les remonte en tête de colonne proprement.

- [ ] **Step 3: Vérifier au parse et à l'écran**

Run: `.\tests\Test-KitParse.ps1 -CI`
Expected: 0 erreur.

Boucle visuelle standard, lire le PNG. Expected : bandeau teal « PC-Refresh-Kit / Trimko Labs / Machine / APERÇU », fenêtre 1200x720 redimensionnable, colonne gauche blanche, zone droite gris clair, barre blanche vide en bas. Les contrôles droits peuvent être rognés ou scrollables : transitoire assumé jusqu'en Task 9.

- [ ] **Step 4: Contrôles et commit**

```powershell
.\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: squelette du layout Trimko (bandeau, colonnes, barre d action)"
```

---

### Task 5: Colonne intervention (profil en tête + timeline de modules)

**Files:**
- Modify: `Run-GUI.ps1` (contenu de `$script:HostLeft` : suppression de `$panelLeft`/`$clbModules`, redistribution ; `Build-Queue` ; `Set-GuiFromProfile` ; `Get-GuiProfileObject`)

**Interfaces:**
- Consumes: `New-KitModuleRow`, `Set-KitModuleRowState`, `New-KitEyebrow`, `Set-KitButtonStyle` (Task 2) ; `$script:HostLeft`, `$script:Mdl2` (Task 4).
- Produces: `$script:ModuleRows` (hashtable Id -> objet row de `New-KitModuleRow`), dans l'ordre de `$script:Modules` ; `Get-KitCheckedIds` -> string[] des Id cochés (contrat : return nu + `@()` chez l'appelant) ; `$lblModulesEyebrow` (eyebrow MODULES/DÉROULÉ). Les contrôles d'options (`$cmbDebloat`, `$gbProfile`, `$gbSensitive`, `$gbUserData`, `$cbScanDefender`, `$cbDryRun`, `$cbBackupData` et les 6 cases sensibles) gardent leurs NOMS : la Task 6 les migre à droite.

- [ ] **Step 1: Restructurer la colonne gauche**

Remplacer TOUT le bloc de construction de `$panelLeft` et son contenu (de `$panelLeft = New-Object ...` jusqu'à `$panelLeft.Controls.Add($btnCancel)` inclus, positions issues de la Task 4) par :

```powershell
# --- Colonne intervention : profil en tete, puis la timeline des modules. ---
# Les controles d'options (debloat, compte, sensibles, donnees, Defender,
# dry-run) sont construits ici mais migrent dans la zone droite en Task 6 :
# ils restent temporairement invisibles a cote de la colonne.
$lblProfileTitle = New-KitEyebrow -Text 'Intervention'
$lblProfileTitle.Location = New-Object System.Drawing.Point(0, 2)
$script:HostLeft.Controls.Add($lblProfileTitle)

$cmbProfile = New-Object System.Windows.Forms.ComboBox
$cmbProfile.DropDownStyle = 'DropDownList'
$cmbProfile.Location = New-Object System.Drawing.Point(0, 20)
$cmbProfile.Size = New-Object System.Drawing.Size(276, 24)
$cmbProfile.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)
$script:HostLeft.Controls.Add($cmbProfile)

$btnApplyProfile = New-Object System.Windows.Forms.Button
$btnApplyProfile.Text = 'Appliquer'
$btnApplyProfile.Location = New-Object System.Drawing.Point(0, 48)
$btnApplyProfile.Size = New-Object System.Drawing.Size(132, 24)
Set-KitButtonStyle -Button $btnApplyProfile -Kind Mini
$script:HostLeft.Controls.Add($btnApplyProfile)

$btnSaveProfile = New-Object System.Windows.Forms.Button
$btnSaveProfile.Text = 'Enregistrer comme profil'
$btnSaveProfile.Location = New-Object System.Drawing.Point(138, 48)
$btnSaveProfile.Size = New-Object System.Drawing.Size(138, 24)
Set-KitButtonStyle -Button $btnSaveProfile -Kind MiniGhost
$script:HostLeft.Controls.Add($btnSaveProfile)

$lblModulesEyebrow = New-KitEyebrow -Text 'Modules'
$lblModulesEyebrow.Location = New-Object System.Drawing.Point(0, 84)
$script:HostLeft.Controls.Add($lblModulesEyebrow)

# Timeline : une ligne par module, cochee par defaut, pilotee par Id.
$script:ModuleRows = @{}
$rowsHost = New-Object System.Windows.Forms.Panel
$rowsHost.Location = New-Object System.Drawing.Point(0, 104)
$rowsHost.Size = New-Object System.Drawing.Size(276, (24 * $script:Modules.Count))
$rowsHost.Anchor = 'Top,Left,Right'
$y = 0
foreach ($m in $script:Modules) {
    $row = New-KitModuleRow -Id $m.Id -Name $m.Name -Mdl2Available $script:Mdl2
    $row.Panel.Location = New-Object System.Drawing.Point(0, $y)
    $row.Panel.Width = 276
    $row.Panel.Anchor = 'Top,Left,Right'
    $rowsHost.Controls.Add($row.Panel)
    $script:ModuleRows[$m.Id] = $row
    $y += 24
}
$script:HostLeft.Controls.Add($rowsHost)

function Get-KitCheckedIds {
    # Ids des modules coches, dans l'ordre de $script:Modules.
    # Contrat collections (b) : return nu, l'appelant encadre de @().
    [CmdletBinding()]
    param()
    return ($script:Modules | Where-Object { $script:ModuleRows[$_.Id].CheckBox.Checked } |
            ForEach-Object { $_.Id })
}
```

Les anciens contrôles de `$panelLeft` qui SURVIVENT (mêmes variables, reconstruits à l'identique sauf parent et position, dans l'attente de la Task 6) : recréer à la suite, provisoirement dans `$script:HostRight` en bas (positions provisoires, la Task 6 les range en cartes) :

```powershell
# --- TRANSITOIRE Task 5 -> Task 6 : options reconstruites hors colonne, la
# Task 6 les range dans les cartes de la zone droite. Fonctionnellement
# identiques a l'existant (memes variables, memes defauts).
$lblDebloat = New-Object System.Windows.Forms.Label
$lblDebloat.Text = 'Politique debloat :'
$lblDebloat.AutoSize = $true
$lblDebloat.Location = New-Object System.Drawing.Point(0, 770)
$script:HostRight.Controls.Add($lblDebloat)

$cmbDebloat = New-Object System.Windows.Forms.ComboBox
$cmbDebloat.DropDownStyle = 'DropDownList'
[void]$cmbDebloat.Items.AddRange(@('Conservateur', 'Standard', 'Agressif'))
$cmbDebloat.SelectedIndex = 1
$cmbDebloat.Location = New-Object System.Drawing.Point(115, 766)
$cmbDebloat.Size = New-Object System.Drawing.Size(165, 24)
$script:HostRight.Controls.Add($cmbDebloat)
```

et re-parenter (déplacer les `Controls.Add` existants vers `$script:HostRight`, positions provisoires Y >= 800, X = 0) : `$gbProfile` (avec `$rbStd`, `$rbKeep`), `$gbSensitive` (avec les 6 cases), `$gbUserData` (avec `$cbBackupData`), `$cbScanDefender`, `$cbDryRun`, `$btnRun`, `$btnCancel`. Aucun handler ne change.

ATTENTION séquence : le bloc `if ($UiPreview) { $btnRun.Enabled = $false ; $form.Text = "[APERÇU] ..." }` posé par la Task 3 est à l'intérieur de la zone remplacée. Le CONSERVER en le déplaçant en fin de fichier, juste avant `[void]$form.ShowDialog()` (là où la Task 9 ajoutera le bloc -PreviewPhase) : à cet endroit, `$btnRun` existe quelle que soit la tâche en cours.

- [ ] **Step 2: Adapter Build-Queue**

Remplacer la boucle indexée :

```powershell
    for ($i = 0; $i -lt $clbModules.Items.Count; $i++) {
        if (-not $clbModules.GetItemChecked($i)) { continue }
        $mod = $script:Modules[$i]
```

par :

```powershell
    foreach ($mod in $script:Modules) {
        if (-not $script:ModuleRows[$mod.Id].CheckBox.Checked) { continue }
```

(la suite du corps de boucle est inchangée).

- [ ] **Step 3: Adapter Set-GuiFromProfile et Get-GuiProfileObject**

Dans `Set-GuiFromProfile`, remplacer la boucle :

```powershell
    for ($i = 0; $i -lt $script:Modules.Count; $i++) {
        $id  = $script:Modules[$i].Id
        $val = $true
        if ($Prof.Modules.ContainsKey($id)) { $val = [bool]$Prof.Modules[$id] }
        $clbModules.SetItemChecked($i, $val)
    }
```

par :

```powershell
    foreach ($m in $script:Modules) {
        $val = $true
        if ($Prof.Modules.ContainsKey($m.Id)) { $val = [bool]$Prof.Modules[$m.Id] }
        $script:ModuleRows[$m.Id].CheckBox.Checked = $val
    }
```

Dans `Get-GuiProfileObject`, remplacer :

```powershell
    $mods = @{}
    for ($i = 0; $i -lt $script:Modules.Count; $i++) {
        $mods[$script:Modules[$i].Id] = [bool]$clbModules.GetItemChecked($i)
    }
```

par :

```powershell
    $mods = @{}
    foreach ($m in $script:Modules) {
        $mods[$m.Id] = [bool]$script:ModuleRows[$m.Id].CheckBox.Checked
    }
```

- [ ] **Step 4: Purger les références restantes à clbModules**

```powershell
Select-String -Path .\Run-GUI.ps1 -Pattern 'clbModules'
```

Supprimer chaque occurrence restante : la création du contrôle, `$toolTip.SetToolTip($clbModules, ...)`. Expected après purge : aucune sortie.

- [ ] **Step 5: Vérifier à l'écran, contrôles, commit**

Run: `.\tests\Test-KitParse.ps1 -CI` puis boucle visuelle standard ; lire le PNG.
Expected : colonne gauche : INTERVENTION + combo profil + 2 boutons, MODULES + 15 lignes avec cases cochées (15 Network peut être décochée à la main), zone droite brouillonne (transitoire).

```powershell
.\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: timeline des modules et profil en tete de colonne"
```

---

### Task 6: Zone droite (cartes Réglages et Actions sensibles, onglets Aide/Journal, journal sombre)

**Files:**
- Modify: `Run-GUI.ps1` (zone droite entière), fonction `Add-LogLines`

**Interfaces:**
- Consumes: `New-KitCard`, `New-KitEyebrow`, `ConvertTo-KitColor`, `Get-KitLogLevelColorHex` (Tasks 1-2) ; `$script:HostRight` (Task 4).
- Produces: `$script:Tabs` (TabControl), `$script:TabHelp`, `$script:TabLog` (TabPage), `$txtHelp` (RichTextBox lecture seule), `$cardSettings` et `$cardSensitive` (Panels cartes) : NOMS IDENTIQUES à ceux de la Task 5 du plan 2026-08-14 pour que sa Task 6 (câblage aide) s'applique telle quelle en Task 11. `$txtLog` garde son nom et son type.

- [ ] **Step 1: Construire les deux cartes et y ranger les options**

Remplacer les positions provisoires de la Task 5 (bloc TRANSITOIRE + re-parentages Y >= 770) par :

```powershell
# --- Zone droite, phase preparation : 2 cartes puis les onglets. ---
$cardSettings = New-KitCard
$cardSettings.Location = New-Object System.Drawing.Point(0, 0)
$cardSettings.Size = New-Object System.Drawing.Size(420, 190)
$cardSettings.Anchor = 'Top,Left'

$eyeSettings = New-KitEyebrow -Text 'Réglages'
$eyeSettings.Location = New-Object System.Drawing.Point(12, 8)
$cardSettings.Controls.Add($eyeSettings)

$lblDebloat.Location = New-Object System.Drawing.Point(12, 30)
$cardSettings.Controls.Add($lblDebloat)
$cmbDebloat.Location = New-Object System.Drawing.Point(127, 26)
$cardSettings.Controls.Add($cmbDebloat)

# Radios compte : directement dans la carte (meme parent = exclusivite conservee).
$rbStd.Location = New-Object System.Drawing.Point(12, 58)
$rbKeep.Location = New-Object System.Drawing.Point(12, 80)
$cardSettings.Controls.Add($rbStd)
$cardSettings.Controls.Add($rbKeep)

$cbBackupData.Location = New-Object System.Drawing.Point(12, 106)
$cbScanDefender.Location = New-Object System.Drawing.Point(12, 128)
$cbDryRun.Location = New-Object System.Drawing.Point(12, 150)
$cardSettings.Controls.Add($cbBackupData)
$cardSettings.Controls.Add($cbScanDefender)
$cardSettings.Controls.Add($cbDryRun)

$cardSensitive = New-KitCard -WarnAccent
$cardSensitive.Location = New-Object System.Drawing.Point(430, 0)
$cardSensitive.Size = New-Object System.Drawing.Size(420, 190)
$cardSensitive.Anchor = 'Top,Left,Right'

$eyeSensitive = New-KitEyebrow -Text 'Actions sensibles - décochées = non faites'
$eyeSensitive.Location = New-Object System.Drawing.Point(15, 8)
$cardSensitive.Controls.Add($eyeSensitive)

$cbRecycle.Location  = New-Object System.Drawing.Point(15, 30)
$cbWinOld.Location   = New-Object System.Drawing.Point(15, 52)
$cbCache.Location    = New-Object System.Drawing.Point(15, 74)
$cbOneDrive.Location = New-Object System.Drawing.Point(15, 96)
$cbOem.Location      = New-Object System.Drawing.Point(15, 118)
$cbNetReset.Location = New-Object System.Drawing.Point(15, 140)
$cardSensitive.Controls.AddRange(@($cbRecycle, $cbWinOld, $cbCache, $cbOneDrive, $cbOem, $cbNetReset))

$script:HostRight.Controls.Add($cardSettings)
$script:HostRight.Controls.Add($cardSensitive)
```

Supprimer les créations devenues vides : `$gbProfile`, `$gbSensitive`, `$gbUserData` (les GroupBox n'existent plus, leurs enfants vivent dans les cartes). Vérifier : `Select-String -Path .\Run-GUI.ps1 -Pattern 'gbProfile|gbSensitive|gbUserData'` -> seule `$script:GbChecklist` reste (elle migre en Task 9).

Appliquer la police de base à toutes les cases et radios re-parentés (une ligne par contrôle, `Font = New-Object System.Drawing.Font('Segoe UI', 9.75)`), et `ForeColor = ConvertTo-KitColor $script:Palette.Ink`.

- [ ] **Step 2: Onglets Aide/Journal et journal sombre**

Remplacer la création actuelle de `$txtLog` (Task 4 l'avait mise en `(0, 10)`) par :

```powershell
# --- Onglets Aide / Journal sous les cartes. Ancres : remplissent le reste. ---
$script:Tabs = New-Object System.Windows.Forms.TabControl
$script:Tabs.Location = New-Object System.Drawing.Point(0, 200)
$script:Tabs.Size = New-Object System.Drawing.Size(850, 340)
$script:Tabs.Anchor = 'Top,Bottom,Left,Right'
$script:Tabs.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)

$script:TabHelp = New-Object System.Windows.Forms.TabPage
$script:TabHelp.Text = 'Aide'
$script:TabHelp.BackColor = ConvertTo-KitColor $script:Palette.Card
$script:TabLog = New-Object System.Windows.Forms.TabPage
$script:TabLog.Text = 'Journal'

$txtHelp = New-Object System.Windows.Forms.RichTextBox
$txtHelp.Dock = 'Fill'
$txtHelp.ReadOnly = $true
$txtHelp.BorderStyle = 'None'
$txtHelp.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)
$txtHelp.ForeColor = ConvertTo-KitColor $script:Palette.Ink
$txtHelp.Text = "Survolez un module ou une option : son explication complète s'affiche ici."
$script:TabHelp.Controls.Add($txtHelp)

# RichTextBox : coloration par niveau. Fond sombre assorti au rapport HTML.
$txtLog = New-Object System.Windows.Forms.RichTextBox
$txtLog.Dock = 'Fill'
$txtLog.Multiline = $true
$txtLog.ScrollBars = 'Vertical'
$txtLog.ReadOnly = $true
$txtLog.BorderStyle = 'None'
$txtLog.Font = New-Object System.Drawing.Font('Consolas', 9)
$txtLog.BackColor = ConvertTo-KitColor $script:Palette.JournalBg
$txtLog.ForeColor = ConvertTo-KitColor $script:Palette.LogInfo
$script:TabLog.Controls.Add($txtLog)

$script:Tabs.TabPages.Add($script:TabHelp)
$script:Tabs.TabPages.Add($script:TabLog)
$script:HostRight.Controls.Add($script:Tabs)
```

Ajuster les cartes pour l'ancrage horizontal : donner aux deux cartes une largeur calculée au premier Resize du host :

```powershell
$script:HostRight.Add_Resize({
    $w = [int](($script:HostRight.ClientSize.Width - 24 - 10) / 2)
    $cardSettings.Width = $w
    $cardSensitive.Location = New-Object System.Drawing.Point(($w + 10), 0)
    $cardSensitive.Width = $w
}.GetNewClosure())
```

(et retirer les Anchor Left/Right posés sur les cartes au Step 1 : le Resize pilote tout).

- [ ] **Step 3: Coloration hex dans Add-LogLines**

Dans `Add-LogLines`, remplacer :

```powershell
                $txtLog.SelectionColor  = [System.Drawing.Color]::FromName((Get-LogLevelColor -Line $line))
```

par :

```powershell
                $txtLog.SelectionColor  = ConvertTo-KitColor (Get-KitLogLevelColorHex -Line $line)
```

Dans le handler du Tick, bloc heartbeat, remplacer `$txtLog.SelectionColor  = [System.Drawing.Color]::Gray` par `$txtLog.SelectionColor  = ConvertTo-KitColor $script:Palette.LogHeartbeat`. Dans le handler `$btnDelFiche.Add_Click`, remplacer `[System.Drawing.Color]::Green` par `ConvertTo-KitColor $script:Palette.LogOK`.

- [ ] **Step 4: Vérifier à l'écran, contrôles, commit**

Boucle visuelle standard, lire le PNG. Expected : cartes Réglages et Actions sensibles (liseré ambre) côte à côte, onglets Aide/Journal dessous, Aide au premier plan avec la phrase d'invite. Redimensionner mentalement : les ancres tiennent (vérifier une 2e capture après avoir agrandi la fenêtre à la souris n'est pas possible ici ; la vérification bornes se fait en Task 9 Step 6).

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: cartes reglages et sensibles, onglets aide journal, journal sombre"
```

---

### Task 7: Barre d'action à trois états

**Files:**
- Modify: `Run-GUI.ps1` (contenu de `$script:HostAction`, handler `$btnRun.Add_Click`, bloc terminal de `Start-NextModule`, Tick)

**Interfaces:**
- Consumes: `Set-KitButtonStyle`, `New-KitButton`, `Get-RunSummaryText`, `Format-Elapsed`, `ConvertTo-KitColor` (Tasks 1-2) ; `$script:ModuleRows`, `Get-KitCheckedIds` (Task 5).
- Produces: `$script:ActionSummary` (Label), `$script:ActionProgressTrack`/`$script:ActionProgressFill` (Panels), `Set-KitActionPhase -Phase Prepare|Running|Done -Data <hashtable>`, `Update-KitActionSummary` (recalcule le résumé en préparation). L'ancienne `$progress` (ProgressBar) disparaît. `$btnRun`, `$btnCancel`, `$btnReport` vivent dans la barre. `$script:BtnNewRun` (bouton « Préparer une nouvelle intervention », caché hors phase Done).

- [ ] **Step 1: Construire la barre**

Ajouter après la construction de la zone droite (Task 6) :

```powershell
# --- Barre d'action : le fil conducteur des trois phases. ---
$script:ActionSummary = New-Object System.Windows.Forms.Label
$script:ActionSummary.AutoSize = $true
$script:ActionSummary.Location = New-Object System.Drawing.Point(14, 18)
$script:ActionSummary.ForeColor = ConvertTo-KitColor $script:Palette.InkSoft
$script:ActionSummary.Font = New-Object System.Drawing.Font('Segoe UI', 9)
$script:HostAction.Controls.Add($script:ActionSummary)

# Progression custom (la ProgressBar systeme ne se colore pas) : piste + remplissage.
$script:ActionProgressTrack = New-Object System.Windows.Forms.Panel
$script:ActionProgressTrack.Height = 8
$script:ActionProgressTrack.BackColor = ConvertTo-KitColor $script:Palette.Line
$script:ActionProgressTrack.Visible = $false
$script:ActionProgressFill = New-Object System.Windows.Forms.Panel
$script:ActionProgressFill.Height = 8
$script:ActionProgressFill.Width = 0
$script:ActionProgressFill.BackColor = ConvertTo-KitColor $script:Palette.Accent
$script:ActionProgressFill.Location = New-Object System.Drawing.Point(0, 0)
$script:ActionProgressTrack.Controls.Add($script:ActionProgressFill)
$script:HostAction.Controls.Add($script:ActionProgressTrack)

# Boutons existants, restyles et migres dans la barre (handlers conserves).
$btnRun.Text = "▶  LANCER L'INTERVENTION"
$btnRun.Size = New-Object System.Drawing.Size(230, 36)
Set-KitButtonStyle -Button $btnRun -Kind Primary
$script:HostAction.Controls.Add($btnRun)

$btnCancel.Size = New-Object System.Drawing.Size(100, 36)
Set-KitButtonStyle -Button $btnCancel -Kind Ghost
$btnCancel.Visible = $false          # visible uniquement pendant le run
$script:HostAction.Controls.Add($btnCancel)

$btnReport.Text = 'Ouvrir le rapport'
$btnReport.Size = New-Object System.Drawing.Size(160, 36)
Set-KitButtonStyle -Button $btnReport -Kind Primary
$btnReport.Visible = $false          # visible en phase Done
$script:HostAction.Controls.Add($btnReport)

$script:BtnNewRun = New-KitButton -Text 'Préparer une nouvelle intervention' -Kind Ghost
$script:BtnNewRun.Size = New-Object System.Drawing.Size(230, 36)
$script:BtnNewRun.Visible = $false
$script:HostAction.Controls.Add($script:BtnNewRun)

# Placement droit, recalcule au redimensionnement.
$script:HostAction.Add_Resize({
    $w = $script:HostAction.ClientSize.Width
    $btnRun.Location    = New-Object System.Drawing.Point(($w - $btnRun.Width - 14), 10)
    $btnCancel.Location = New-Object System.Drawing.Point(($w - $btnCancel.Width - 14), 10)
    $btnReport.Location = New-Object System.Drawing.Point(($w - $btnReport.Width - 14), 10)
    $script:BtnNewRun.Location = New-Object System.Drawing.Point(($w - $btnReport.Width - 14 - $script:BtnNewRun.Width - 10), 10)
    $left = $script:ActionSummary.Right + 20
    $script:ActionProgressTrack.Location = New-Object System.Drawing.Point($left, 24)
    $script:ActionProgressTrack.Width = [Math]::Max(80, $w - $left - $btnCancel.Width - 40)
}.GetNewClosure())
```

- [ ] **Step 2: Les fonctions de phase et de résumé**

Ajouter après `Build-Queue` :

```powershell
function Update-KitActionSummary {
    # Resume live de la phase preparation. Appele a chaque changement de case.
    # NOTE pipeline : le tableau DOIT etre construit avant le Where-Object,
    # sinon le pipe ne s'applique qu'au dernier element de la liste.
    $checked = @(Get-KitCheckedIds)
    $sensitives = @(@($cbRecycle, $cbWinOld, $cbCache, $cbOneDrive, $cbOem, $cbNetReset) |
                    Where-Object { $_.Checked })
    $profName = [string]$cmbProfile.SelectedItem
    $script:ActionSummary.Text = Get-RunSummaryText -ModuleCount $checked.Count `
        -SensitiveCount $sensitives.Count -ProfileName $profName -IsDryRun $cbDryRun.Checked
}

function Set-KitActionPhase {
    # Bascule la barre d'action (et les boutons) dans une phase.
    # Data : Prepare -> rien ; Running -> rien (le Tick alimente) ;
    # Done -> @{ Bilan = <string> }.
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][ValidateSet('Prepare','Running','Done')][string]$Phase,
        [hashtable]$Data = @{}
    )
    switch ($Phase) {
        'Prepare' {
            $btnRun.Visible = $true
            $btnCancel.Visible = $false
            $btnReport.Visible = $false
            $script:BtnNewRun.Visible = $false
            $script:ActionProgressTrack.Visible = $false
            $script:ActionSummary.ForeColor = ConvertTo-KitColor $script:Palette.InkSoft
            Update-KitActionSummary
        }
        'Running' {
            $btnRun.Visible = $false
            $btnCancel.Visible = $true
            $btnReport.Visible = $false
            $script:BtnNewRun.Visible = $false
            $script:ActionProgressTrack.Visible = $true
            $script:ActionProgressFill.Width = 0
        }
        'Done' {
            $btnRun.Visible = $false
            $btnCancel.Visible = $false
            $btnReport.Visible = $true
            $script:BtnNewRun.Visible = $true
            $script:ActionProgressTrack.Visible = $false
            if ($Data.ContainsKey('Bilan')) { $script:ActionSummary.Text = [string]$Data.Bilan }
        }
    }
    $script:HostAction.PerformLayout()
}

function Set-KitActionProgress {
    # Progression : Current modules termines sur Total.
    [CmdletBinding()]
    param([int]$Current, [int]$Total)
    if ($Total -le 0) { return }
    $ratio = [Math]::Min(1.0, $Current / [double]$Total)
    $script:ActionProgressFill.Width = [int]($script:ActionProgressTrack.ClientSize.Width * $ratio)
}
```

- [ ] **Step 3: Câbler le live et remplacer $progress**

1. Après la construction des rows (Task 5), câbler chaque case : dans la boucle `foreach ($m in $script:Modules)`, après `$script:ModuleRows[$m.Id] = $row`, ajouter `$row.CheckBox.Add_CheckedChanged({ Update-KitActionSummary })`.
2. Câbler les options : après la construction des cartes (Task 6), ajouter :

```powershell
foreach ($c in @($cbRecycle, $cbWinOld, $cbCache, $cbOneDrive, $cbOem, $cbNetReset, $cbDryRun)) {
    $c.Add_CheckedChanged({ Update-KitActionSummary })
}
$cbDryRun.Add_CheckedChanged({
    if (-not $UiPreview) {
        if ($cbDryRun.Checked) { Set-KitBadgeMode -Band $script:Band -Mode Simulation }
        else { Set-KitBadgeMode -Band $script:Band -Mode Real }
    }
})
Set-KitActionPhase -Phase Prepare
```

3. Supprimer la création de `$progress` (ProgressBar) et remplacer ses usages :
   - dans `$btnRun.Add_Click` : supprimer `$progress.Minimum/Maximum/Value = ...`, ajouter `Set-KitActionPhase -Phase Running` juste après le bloc du log unifié, et remplacer la bascule d'onglet éventuelle (aucune encore : elle arrive en Task 11) ;
   - dans le Tick (`$script:QueueIndex++` puis `$progress.Value = ...`) : remplacer par `Set-KitActionProgress -Current $script:QueueIndex -Total $script:Queue.Count` ;
   - dans le bloc terminal de `Start-NextModule` : remplacer `$progress.Value = $progress.Maximum` par `Set-KitActionProgress -Current 1 -Total 1`, et après le calcul du bilan (`$sum`, `$elapsedStr`) ajouter :

```powershell
            Set-KitActionPhase -Phase Done -Data @{
                Bilan = "Terminé : $($sum.CountOK) OK - $($sum.CountWarn) avertissement(s) - $($sum.CountError) erreur(s) - durée $elapsedStr"
            }
```

   - dans `$btnCancel.Add_Click` : après `$btnRun.Enabled = $true`, ajouter `Set-KitActionPhase -Phase Prepare`.
   - Vérifier `Select-String -Path .\Run-GUI.ps1 -Pattern '\$progress'` : aucune sortie.
4. Le résumé pendant le run, dans le Tick : remplacer la mise à jour du titre existante par (en la conservant) :

```powershell
    if ($script:Running -and $script:StartTime) {
        try {
            $elapsed = Format-Elapsed ([int]((Get-Date) - $script:StartTime).TotalSeconds)
            $form.Text = "$($script:RunLabel)PC-Refresh-Kit - $env:COMPUTERNAME - écoulé $elapsed"
            $cur = $script:Queue[$script:QueueIndex]
            $lbl = if ($cur) { "$($cur.Mod.Id) $($cur.Mod.Name)" } else { '' }
            $script:ActionSummary.Text = "Module $([Math]::Min($script:QueueIndex + 1, $script:Queue.Count))/$($script:Queue.Count) - $lbl - écoulé $elapsed"
        } catch { }
    }
```

5. Câbler le bouton nouvelle intervention (après les autres handlers) :

```powershell
$script:BtnNewRun.Add_Click({
    # Retour en phase preparation : les etats du run precedent restent visibles
    # sur la timeline jusqu'au prochain LANCER (choix de la spec 4.4) ; les cases
    # redeviennent editables.
    foreach ($m in $script:Modules) {
        Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Pending -Detail '' -Mdl2Available $script:Mdl2
    }
    $lblModulesEyebrow.Text = 'MODULES'
    Set-KitPrepareEnabled -Enabled $true
    Set-KitActionPhase -Phase Prepare
})
```

(`Set-KitPrepareEnabled` arrive en Task 8 ; à ce stade, laisser la ligne commentée avec `# Task 8:` devant, la Task 8 la décommente.)

- [ ] **Step 4: Vérifier à l'écran, contrôles, commit**

Boucle visuelle standard, lire le PNG. Expected : barre en bas avec « 15 modules - 0 action(s) sensible(s) - SIMULATION » (aperçu : pas de profil appliqué, 15 cochés par défaut) et le bouton teal LANCER L'INTERVENTION à droite, grisé (aperçu). Décocher mentalement : non vérifiable ici ; les tests du résumé sont ceux de `Get-RunSummaryText` (Task 1).

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: barre d action a trois etats avec resume live"
```

---

### Task 8: États par module pendant le run

**Files:**
- Modify: `Run-GUI.ps1` (`Start-NextModule`, Tick, `$btnRun.Add_Click`, `$btnCancel.Add_Click`)

**Interfaces:**
- Consumes: `Set-KitModuleRowState`, `Get-ModuleTrancheState`, `Format-Elapsed` (Tasks 1-2), `$script:ModuleRows`, `$lblModulesEyebrow` (Task 5), `Set-KitActionPhase` (Task 7).
- Produces: `$script:ModuleLogStart` (offset de tranche du module courant) ; `Set-KitPrepareEnabled -Enabled <bool>` (verrouille cases, options, profils pendant le run).

- [ ] **Step 1: Verrouillage de la préparation**

Ajouter après `Get-GuiProfileObject` :

```powershell
function Set-KitPrepareEnabled {
    # Verrouille/deverrouille tout ce qui parametre l'intervention pendant un run.
    [CmdletBinding()]
    param([Parameter(Mandatory)][bool]$Enabled)
    foreach ($m in $script:Modules) { $script:ModuleRows[$m.Id].CheckBox.Enabled = $Enabled }
    foreach ($c in @($cmbDebloat, $rbStd, $rbKeep, $cbBackupData, $cbScanDefender, $cbDryRun,
                     $cbRecycle, $cbWinOld, $cbCache, $cbOneDrive, $cbOem, $cbNetReset,
                     $cmbProfile, $btnApplyProfile, $btnSaveProfile)) {
        $c.Enabled = $Enabled
    }
}
```

Décommenter la ligne `Set-KitPrepareEnabled -Enabled $true` du handler `$script:BtnNewRun` (Task 7 Step 3.5).

- [ ] **Step 2: Poser les états au lancement**

Dans `$btnRun.Add_Click`, juste après `Build-Queue` et le contrôle `$script:Queue.Count -eq 0`, ajouter :

```powershell
    # Timeline : tous les etats poses d'un coup au lancement.
    $lblModulesEyebrow.Text = 'DÉROULÉ'
    $queuedIds = @($script:Queue | ForEach-Object { $_.Mod.Id })
    foreach ($m in $script:Modules) {
        if ($queuedIds -contains $m.Id) {
            Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Pending -Detail 'en attente' -Mdl2Available $script:Mdl2
        } else {
            Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Skipped -Detail 'ignoré' -Mdl2Available $script:Mdl2
        }
    }
    Set-KitPrepareEnabled -Enabled $false
```

et dans le badge : après `$script:RunLabel = ...`, ajouter :

```powershell
    if ($cbDryRun.Checked) { Set-KitBadgeMode -Band $script:Band -Mode Simulation }
    else { Set-KitBadgeMode -Band $script:Band -Mode Real }
```

- [ ] **Step 3: Running au démarrage de chaque module, verdict à sa sortie**

Ajouter d'abord la variable d'état à côté des autres (`$script:DismLastSize = ...` en tête de fichier) :

```powershell
$script:ModuleLogStart  = 0            # offset du log au demarrage du module courant (borne de tranche)
```

Puis dans `Start-NextModule`, juste après `$item = $script:Queue[$script:QueueIndex]` et `$mod = $item.Mod`, ajouter :

```powershell
    $script:ModuleLogStart = $script:LogOffset   # borne de la tranche de log du module
    Set-KitModuleRowState -Row $script:ModuleRows[$mod.Id] -State Running -Detail 'en cours' -Mdl2Available $script:Mdl2
```

Dans le Tick, bloc `if ($script:CurrentProc -and $script:CurrentProc.HasExited)`, juste après `$finished = $script:Queue[$script:QueueIndex]`, ajouter :

```powershell
        # Verdict du module : code de sortie + tranche de son log (Add-LogLines a
        # deja pousse $script:LogOffset au bout : la tranche est [start, offset)).
        if ($finished) {
            try {
                $all = @(Get-Content $script:LogFile -Encoding UTF8 -ErrorAction SilentlyContinue)
                $tranche = @()
                if ($all.Count -gt $script:ModuleLogStart) {
                    $tranche = $all[$script:ModuleLogStart..($all.Count - 1)]
                }
                $state = Get-ModuleTrancheState -ExitCode $exitCode -TrancheLines $tranche
                $durSec = [int]((Get-Date) - $script:ModuleStartTime).TotalSeconds
                $detail = switch ($state) {
                    'Ok'    { Format-Elapsed $durSec }
                    'Warn'  { "$(Format-Elapsed $durSec) - avertissement(s)" }
                    'Error' { "$(Format-Elapsed $durSec) - erreur" }
                }
                Set-KitModuleRowState -Row $script:ModuleRows[$finished.Mod.Id] -State $state -Detail $detail -Mdl2Available $script:Mdl2
            } catch { }   # un echec d'affichage d'etat ne casse jamais le tick
        }
```

(NOTE : ce bloc se place APRÈS la ligne existante `$exitCode = $script:CurrentProc.ExitCode` qui définit `$exitCode`.)

Toujours dans le Tick, dans la mise à jour du résumé (Task 7 Step 3.4), ajouter la durée vivante du module courant : après la ligne `$script:ActionSummary.Text = ...`, ajouter :

```powershell
            if ($cur -and $script:ModuleStartTime) {
                $curSec = [int]((Get-Date) - $script:ModuleStartTime).TotalSeconds
                Set-KitModuleRowState -Row $script:ModuleRows[$cur.Mod.Id] -State Running -Detail "en cours - $(Format-Elapsed $curSec)" -Mdl2Available $script:Mdl2
            }
```

- [ ] **Step 4: Fin de run et annulation**

Dans le bloc terminal de `Start-NextModule` (après `Set-KitActionPhase -Phase Done`), ajouter `Set-KitPrepareEnabled -Enabled $true` UNIQUEMENT sur les contrôles de fin ? Non : la préparation reste verrouillée en phase Done (les états du run sont affichés) ; c'est `$script:BtnNewRun` qui déverrouille. En revanche, dans `$btnCancel.Add_Click`, après `Set-KitActionPhase -Phase Prepare`, ajouter :

```powershell
    Set-KitPrepareEnabled -Enabled $true
    foreach ($m in $script:Modules) {
        Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Pending -Detail '' -Mdl2Available $script:Mdl2
    }
    $lblModulesEyebrow.Text = 'MODULES'
```

- [ ] **Step 5: Parse, contrôles, commit**

Run: `.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI`
Expected: 0 erreur. (La validation dynamique de cette mécanique arrive en Task 9 avec -PreviewPhase Running, puis au run -WhatIf du mainteneur.)

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: etats des modules sur la timeline pendant le run"
```

---

### Task 9: Phase clôture, passphrase masquée, -PreviewPhase, captures des trois phases

**Files:**
- Modify: `Run-GUI.ps1` (TabPage Clôture, bloc terminal de `Start-NextModule`, `$btnDelFiche.Add_Click`, `$btnSaveProfile.Add_Click`, modale `Show-BackupPause`, données d'aperçu)

**Interfaces:**
- Consumes: tout ce qui précède ; `Get-EndChecklistItems` (lib/Report.ps1) ; `Show-KitInputDialog`, `New-KitCard`, `New-KitEyebrow`, `New-KitButton` (Task 2).
- Produces: `$script:TabClose` (TabPage Clôture, ajoutée en fin de run), `$script:LblPwdValue` (Label passphrase masquée), `$script:BtnPwdShow` (Afficher/Masquer), `Show-KitClosePhase -RebootRequired <bool>` ; `-PreviewPhase Running|Done` peuple l'interface avec `Get-KitPreviewData`.

- [ ] **Step 1: Construire la page Clôture (cachée au départ)**

Après la construction des onglets (Task 6), ajouter :

```powershell
# --- Page Cloture : construite tout de suite, ajoutee au TabControl en fin de
# run seulement (spec 4.3). Journal et Aide restent consultables a cote. ---
$script:TabClose = New-Object System.Windows.Forms.TabPage
$script:TabClose.Text = 'Clôture'
$script:TabClose.BackColor = ConvertTo-KitColor $script:Palette.Ground
$script:TabClose.Padding = New-Object System.Windows.Forms.Padding(10)

$cardPwd = New-KitCard
$cardPwd.Location = New-Object System.Drawing.Point(10, 10)
$cardPwd.Size = New-Object System.Drawing.Size(810, 76)
$cardPwd.Anchor = 'Top,Left,Right'
$cardPwd.BackColor = ConvertTo-KitColor $script:Palette.AccentBgSoft

$eyePwd = New-KitEyebrow -Text 'Compte Admin-Local créé'
$eyePwd.Location = New-Object System.Drawing.Point(12, 8)
$cardPwd.Controls.Add($eyePwd)

$script:LblPwdValue = New-Object System.Windows.Forms.Label
$script:LblPwdValue.Text = '(généré après le module Accounts)'
$script:LblPwdValue.AutoSize = $true
$script:LblPwdValue.Location = New-Object System.Drawing.Point(12, 34)
$script:LblPwdValue.Font = New-Object System.Drawing.Font('Consolas', 11)
$script:LblPwdValue.ForeColor = ConvertTo-KitColor '#134e4a'
$cardPwd.Controls.Add($script:LblPwdValue)

$script:BtnPwdShow = New-KitButton -Text 'Afficher' -Kind Mini
$script:BtnPwdShow.Size = New-Object System.Drawing.Size(80, 26)
$script:BtnPwdShow.Location = New-Object System.Drawing.Point(620, 30)
$script:BtnPwdShow.Anchor = 'Top,Right'
$script:BtnPwdShow.Enabled = $false
$cardPwd.Controls.Add($script:BtnPwdShow)

$btnCopy.Text = 'Copier'
$btnCopy.Size = New-Object System.Drawing.Size(80, 26)
$btnCopy.Location = New-Object System.Drawing.Point(710, 30)
$btnCopy.Anchor = 'Top,Right'
Set-KitButtonStyle -Button $btnCopy -Kind Mini
$cardPwd.Controls.Add($btnCopy)          # re-parentage : handler Copier conserve

$script:TabClose.Controls.Add($cardPwd)

$script:GbChecklist.Text = ''
$script:GbChecklist.Location = New-Object System.Drawing.Point(10, 96)
$script:GbChecklist.Size = New-Object System.Drawing.Size(810, 214)
$script:GbChecklist.Anchor = 'Top,Bottom,Left,Right'
$script:GbChecklist.Visible = $true      # la page entiere est cachee, pas le groupe
$script:TabClose.Controls.Add($script:GbChecklist)

$eyeCheck = New-KitEyebrow -Text 'Avant de rendre le PC'
$eyeCheck.Location = New-Object System.Drawing.Point(10, 320)
$script:TabClose.Controls.Add($eyeCheck)   # NOTE : place au-dessus du groupe via BringToFront a l'affichage

$btnReportClose = New-KitButton -Text 'Ouvrir le rapport' -Kind Ghost
$btnReportClose.Size = New-Object System.Drawing.Size(150, 30)
$btnReportClose.Location = New-Object System.Drawing.Point(10, 322)
$btnReportClose.Anchor = 'Bottom,Left'
$btnReportClose.Add_Click({ $btnReport.PerformClick() })
$script:TabClose.Controls.Add($btnReportClose)

$btnDelFiche.Text = 'Supprimer la fiche PC de la clé'
$btnDelFiche.Size = New-Object System.Drawing.Size(230, 30)
$btnDelFiche.Location = New-Object System.Drawing.Point(170, 322)
$btnDelFiche.Anchor = 'Bottom,Left'
Set-KitButtonStyle -Button $btnDelFiche -Kind Danger
$script:TabClose.Controls.Add($btnDelFiche)   # re-parentage : handler conserve
```

Supprimer l'eyebrow mal placé `$eyeCheck` du bloc ci-dessus s'il chevauche à l'écran (vérification visuelle au Step 5) : le GroupBox sans texte + l'eyebrow au-dessus de lui, à `(10, 96)` décalé, `$script:GbChecklist.Location` passant à `(10, 116)` et hauteur 194. Poser les positions définitives d'après la capture.

Supprimer `$lblPwd` (l'ancien label plein écran) : `Select-String -Path .\Run-GUI.ps1 -Pattern 'lblPwd\b'` et remplacer chaque écriture `$lblPwd.Text = "Mot de passe Admin-Local : ..."` comme suit :
- dans le bloc terminal (fiche trouvée) : remplacé par le Step 3 ci-dessous ;
- dans `$btnDelFiche.Add_Click` : `$lblPwd.Text = "..."` devient `$script:LblPwdValue.Text = '(fiche supprimée de la clé)' ; $script:BtnPwdShow.Enabled = $false`.

- [ ] **Step 2: Afficher/Masquer**

Ajouter avec les autres variables d'état (`$script:AdminPwd = $null`) : `$script:PwdVisible = $false`. Ajouter le handler après `$btnCopy.Add_Click` :

```powershell
$script:BtnPwdShow.Add_Click({
    if (-not $script:AdminPwd) { return }
    $script:PwdVisible = -not $script:PwdVisible
    if ($script:PwdVisible) {
        $script:LblPwdValue.Text = $script:AdminPwd
        $script:BtnPwdShow.Text = 'Masquer'
    } else {
        $script:LblPwdValue.Text = ('•' * 12)
        $script:BtnPwdShow.Text = 'Afficher'
    }
})
```

- [ ] **Step 3: Bloc terminal de Start-NextModule : basculer en clôture**

Dans le bloc terminal, remplacer la section mot de passe :

```powershell
        $fiche = Join-Path $script:Root "runtime\FICHE-PC-$env:COMPUTERNAME.txt"
        if (Test-Path $fiche) {
            $line = (Get-Content $fiche -Encoding UTF8 | Where-Object { $_ -match 'Mot de passe' } | Select-Object -First 1)
            if ($line -match ':\s*(.+)$') {
                $script:AdminPwd = $Matches[1].Trim()
                $lblPwd.Text = "Mot de passe Admin-Local : $($script:AdminPwd)"
                $btnCopy.Enabled = $true
            }
        }
```

par :

```powershell
        $fiche = Join-Path $script:Root "runtime\FICHE-PC-$env:COMPUTERNAME.txt"
        if (Test-Path $fiche) {
            $line = (Get-Content $fiche -Encoding UTF8 | Where-Object { $_ -match 'Mot de passe' } | Select-Object -First 1)
            if ($line -match ':\s*(.+)$') {
                $script:AdminPwd = $Matches[1].Trim()
                $script:PwdVisible = $false
                $script:LblPwdValue.Text = ('•' * 12)     # masquee par defaut (spec, decision grill)
                $script:BtnPwdShow.Text = 'Afficher'
                $script:BtnPwdShow.Enabled = $true
                $btnCopy.Enabled = $true
            }
        }
```

Et remplacer le bloc checklist existant (`$rebootReq = ... ; $script:GbChecklist.Controls.Clear() ; ... ; $script:GbChecklist.Visible = $true`) par un appel unique :

```powershell
        try {
            $rebootReq = Test-Path (Join-Path $script:Root 'runtime\reboot-required.flag')
            Show-KitClosePhase -RebootRequired $rebootReq
        } catch { }   # la cloture est informative : toute erreur ne bloque pas la fin de run
```

avec la fonction (ajoutée après `Set-KitActionPhase`) :

```powershell
function Show-KitClosePhase {
    # Peuple la checklist, ajoute l'onglet Cloture et le selectionne.
    [CmdletBinding()]
    param([bool]$RebootRequired)
    $script:GbChecklist.Controls.Clear()
    $yItem = 8
    foreach ($item in @(Get-EndChecklistItems -RebootRequired $RebootRequired)) {
        $cb = New-Object System.Windows.Forms.CheckBox
        $cb.Text = [string]$item
        $cb.AutoSize = $true
        $cb.Checked = $false
        $cb.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)
        $cb.Location = New-Object System.Drawing.Point(8, $yItem)
        if ($RebootRequired -and ([string]$item -match 'REBOOT REQUIS|REDÉMARRER')) {
            $cb.ForeColor = ConvertTo-KitColor $script:Palette.Err
            $cb.Font = New-Object System.Drawing.Font('Segoe UI', 9.75, [System.Drawing.FontStyle]::Bold)
        } else {
            $cb.ForeColor = ConvertTo-KitColor $script:Palette.Ink
        }
        $script:GbChecklist.Controls.Add($cb)
        $yItem += 24
    }
    if (-not $script:Tabs.TabPages.Contains($script:TabClose)) {
        $script:Tabs.TabPages.Add($script:TabClose)
    }
    $script:Tabs.SelectedTab = $script:TabClose
}
```

Dans `$script:BtnNewRun.Add_Click` (Task 7), ajouter au début : `if ($script:Tabs.TabPages.Contains($script:TabClose)) { $script:Tabs.TabPages.Remove($script:TabClose) }` puis `$script:Tabs.SelectedTab = $script:TabHelp`.

- [ ] **Step 4: InputBox VB remplacée, modale backup rhabillée, AutoScroll retiré**

1. Dans `$btnSaveProfile.Add_Click`, remplacer :

```powershell
    $nom = [Microsoft.VisualBasic.Interaction]::InputBox(
        "Entrez le nom du profil à enregistrer :", "Enregistrer comme profil", "")
```

par :

```powershell
    $nom = Show-KitInputDialog -Owner $form -Title 'Enregistrer comme profil' -Prompt 'Nom du profil à enregistrer :'
    if ($null -eq $nom) { return }
```

et supprimer la ligne `Add-Type -AssemblyName Microsoft.VisualBasic` en tête de fichier (plus aucun usage : vérifier `Select-String -Path .\Run-GUI.ps1 -Pattern 'VisualBasic'` -> aucune sortie).

2. Dans `Show-BackupPause`, ajouter après `$dlg.ControlBox = $false` : `$dlg.BackColor = ConvertTo-KitColor $script:Palette.Card`, donner au label `$lbl.Font = New-Object System.Drawing.Font('Segoe UI', 9.75)` et remplacer la création du bouton `$ok` par `$ok = New-KitButton -Text 'OK, continuer' -Kind Primary` (mêmes Size/Location/handler qu'actuellement). NE RIEN CHANGER d'autre : le timer stoppé avant ShowDialog et relancé en finally (fix réentrance v1.9) reste à l'identique.
3. Retirer le transitoire de la Task 4 : `$script:HostRight.AutoScroll` passe à `$false`, ET dans le même geste (déviation actée en Task 6, commentaire dans Run-GUI.ps1 près du TabControl) : retirer le plafond de hauteur 360 et le pilotage manuel Width/Height des onglets dans le handler `HostRight.Add_Resize` (le handler ne conserve que le dimensionnement des deux cartes), puis poser `$script:Tabs.Anchor = 'Top,Bottom,Left,Right'` avec une Size initiale cohérente à 1200x720 (largeur = zone cliente moins padding, hauteur = jusqu'au bas du host). Le plafond n'existait que pour cohabiter avec AutoScroll : le garder après son retrait figerait les onglets à 360 px sur toute fenêtre agrandie. NE JAMAIS reconstruire le TabControl (le handler Add_Resize capture une copie instantanée des variables script : un contrôle reconstruit deviendrait orphelin du handler) : modifier l'existant.

- [ ] **Step 5: -PreviewPhase : données factices des trois phases**

Ajouter juste avant `[void]$form.ShowDialog()` :

```powershell
# --- Apercu des phases (-UiPreview -PreviewPhase Running|Done) : donnees
# factices pour les captures du README et la verification visuelle. Aucune
# ecriture disque, aucun process lance. ---
function Get-KitPreviewData {
    [CmdletBinding()]
    param()
    return @{
        Running = @(
            @{ Id='00'; State='Ok';      Detail='00:45' }
            @{ Id='01'; State='Ok';      Detail='08:05' }
            @{ Id='02'; State='Running'; Detail='en cours - 03:12' }
        )
        DoneWarn = '05'
        DoneSkip = '15'
        DoneDurations = @{
            '00'='00:45'; '01'='08:05'; '02'='11:30'; '03'='06:12'; '04'='01:08'
            '05'='avertissement(s)'; '06'='04:55'; '07'='07:40'; '08'='00:38'
            '09'='00:22'; '11'='00:51'; '12'='00:14'; '13'='00:29'; '10'='00:18'
        }
    }
}

if ($UiPreview -and $PreviewPhase -ne 'Prepare') {
    $pv = Get-KitPreviewData
    $lblModulesEyebrow.Text = 'DÉROULÉ'
    Set-KitPrepareEnabled -Enabled $false
    if ($PreviewPhase -eq 'Running') {
        Set-KitBadgeMode -Band $script:Band -Mode Real
        foreach ($m in $script:Modules) {
            $preset = @($pv.Running | Where-Object { $_.Id -eq $m.Id })
            if ($preset.Count -gt 0) {
                Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State $preset[0].State -Detail $preset[0].Detail -Mdl2Available $script:Mdl2
            } elseif ($m.Id -eq '15') {
                Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Skipped -Detail 'ignoré' -Mdl2Available $script:Mdl2
            } else {
                Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Pending -Detail 'en attente' -Mdl2Available $script:Mdl2
            }
        }
        Set-KitActionPhase -Phase Running
        $script:ActionSummary.Text = 'Module 3/14 - 02 Antivirus - écoulé 12:40'
        Set-KitActionProgress -Current 2 -Total 14
        $script:Tabs.SelectedTab = $script:TabLog
        foreach ($demo in @(
            '[14:11:58] [INFO] Module 02 Antivirus : démarrage',
            '[14:12:03] [OK] Avast désinstallé proprement',
            '[14:12:19] [OK] Windows Defender activé (protection temps réel)',
            '[14:13:05] [WARN] Exclusion Defender héritée détectée : C:\Games (conservée)',
            '[14:13:22] [INFO] Scan complet Defender démarré...')) {
            try {
                $txtLog.SelectionStart = $txtLog.TextLength ; $txtLog.SelectionLength = 0
                $txtLog.SelectionColor = ConvertTo-KitColor (Get-KitLogLevelColorHex -Line $demo)
            } catch { }
            $txtLog.AppendText("$demo`r`n")
        }
    }
    if ($PreviewPhase -eq 'Done') {
        Set-KitBadgeMode -Band $script:Band -Mode Real
        foreach ($m in $script:Modules) {
            $detail = [string]$pv.DoneDurations[$m.Id]
            if ($m.Id -eq $pv.DoneSkip)      { Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Skipped -Detail 'ignoré' -Mdl2Available $script:Mdl2 }
            elseif ($m.Id -eq $pv.DoneWarn)  { Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Warn -Detail $detail -Mdl2Available $script:Mdl2 }
            else                             { Set-KitModuleRowState -Row $script:ModuleRows[$m.Id] -State Ok -Detail $detail -Mdl2Available $script:Mdl2 }
        }
        $script:AdminPwd = 'tortue-camion-soleil-42'   # passphrase FACTICE d'apercu
        $script:LblPwdValue.Text = ('•' * 12)
        $script:BtnPwdShow.Enabled = $true
        $btnCopy.Enabled = $true
        Show-KitClosePhase -RebootRequired $true
        Set-KitActionPhase -Phase Done -Data @{ Bilan = 'Terminé : 12 OK - 2 avertissement(s) - 0 erreur(s) - durée 43:12' }
    }
}
```

- [ ] **Step 6: Boucle visuelle des trois phases et itération de polissage**

```powershell
foreach ($phase in 'Prepare','Running','Done') {
    $p = Start-Process powershell -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File','.\Run-GUI.ps1','-UiPreview','-PreviewPhase',$phase -PassThru
    Start-Sleep -Seconds 5
    .\tools\Capture-Fenetre.ps1 -TitrePattern '*PC-Refresh-Kit*'
    $p | Stop-Process -Force
}
```

LIRE les trois PNG. Vérifier contre la spec section 4 : bandeau, timeline, cartes, onglets (Clôture présent en Done avec Journal et Aide à côté), barre d'action conforme à chaque phase, passphrase masquée, checklist avec l'item REDÉMARRER en rouge gras. CORRIGER les défauts visuels constatés (chevauchements, tailles, alignements) et reboucler jusqu'à un rendu propre. Vérifier aussi une capture à la taille minimale : ajouter temporairement `$form.Size = $form.MinimumSize` en tête de la boucle si besoin, puis retirer.

- [ ] **Step 7: Contrôles, commit**

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
pwsh -c "Invoke-Pester .\tests -Output Detailed"
```

Expected: tout vert.

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: phase cloture, passphrase masquee, apercu des trois phases"
```

---

### Task 10: Aide intégrée - fonctions et catalogue (reprise du plan du 14/08)

**Files:** ceux des Tasks 1 à 4 du plan `docs/superpowers/plans/2026-08-14-documentation-integree.md`.

**Interfaces:** celles des Tasks 1 à 4 de ce plan (Get-HelpCatalog, Get-HelpEntry, Format-HelpPanel, Format-HelpTooltip ; catalogue de 40 entrées ; tests de couverture).

- [ ] **Step 1: Exécuter les Tasks 1, 2, 3 et 4 du plan 2026-08-14-documentation-integree.md**

Ouvrir ce plan et suivre ses Tasks 1 à 4 étape par étape : le code complet y figure (lib/Help.ps1, tests/Help.Tests.ps1, config/help.fr.json avec les 16 entrées modules puis les 24 autres, tests de couverture). DEUX adaptations obligatoires :
1. Rester sur la branche `feat/v2.2-cockpit-trimko` : IGNORER toute mention de `public-main` et tout `git push` de ce plan.
2. Ajouter aux messages de commit les deux trailers listés dans les Global Constraints du présent plan.

- [ ] **Step 2: Validation groupée**

Run: `pwsh -c "Invoke-Pester .\tests\Help.Tests.ps1 -Output Detailed"`
Expected: PASS intégral (40 entrées, couverture, format, typographie).

---

### Task 11: Câblage de l'aide sur le nouveau cockpit

**Files:**
- Modify: `Run-GUI.ps1` (dot-sourcing, tooltips, handlers de survol)

**Interfaces:**
- Consumes: les fonctions et le catalogue de la Task 10 ; `$script:Tabs`, `$txtHelp` (Task 6) ; `$script:ModuleRows` (Task 5).
- Produces: `Show-KitHelp -Key <string>` ; toutes les infobulles générées du catalogue ; plus aucun texte d'aide en dur.

- [ ] **Step 1: Appliquer la Task 6 du plan 2026-08-14-documentation-integree.md avec quatre adaptations**

Suivre ses Steps 1 à 5 (dot-sourcing de lib/Help.ps1 + chargement du catalogue ; suppression de `$moduleDescriptions` ; remplacement du bloc ToolTip par `Show-KitHelp` + `$helpBindings` + les câblages debloat/profil), avec ces adaptations :

1. **Liste des modules** : le bloc `$clbModules.Add_MouseMove` / `Add_SelectedIndexChanged` de son Step 3 est REMPLACÉ par le survol des lignes de timeline :

```powershell
# Timeline : aide du module survole (panel entier + nom, l'un ou l'autre recoit l'evenement).
foreach ($m in $script:Modules) {
    $row = $script:ModuleRows[$m.Id]
    $key = "module.$($m.Id)"
    foreach ($ctrl in @($row.Panel, $row.NameLabel, $row.CheckBox)) {
        $ctrl.Add_MouseEnter({ Show-KitHelp -Key $key }.GetNewClosure())
    }
    $entryRow = Get-HelpEntry -Catalog $script:HelpCatalog -Key $key
    $toolTip.SetToolTip($row.NameLabel, (Format-HelpTooltip -Entry $entryRow -Width 90))
}
```

2. **Bascule Journal au LANCER** (son Step 4) : la ligne `$script:Tabs.SelectedTab = $script:TabLog` s'ajoute dans `$btnRun.Add_Click` juste après `Set-KitActionPhase -Phase Running` (Task 7). Ne pas la poser deux fois.
3. **`$helpBindings`** : ajouter à sa table l'entrée `@{ Ctrl = $script:BtnNewRun; Key = 'action.run' }` est INUTILE (pas d'entrée dédiée) : ne rien ajouter ; en revanche vérifier que `$btnRun`, `$btnCancel`, `$btnReport`, `$btnCopy`, `$btnDelFiche`, `$btnApplyProfile`, `$btnSaveProfile` de sa table existent toujours sous ces noms (oui : Tasks 5-9 les conservent).
4. **Trailers** de commit des Global Constraints.

- [ ] **Step 2: Vérification visuelle du panneau d'aide**

Boucle visuelle standard (phase Prepare), lire le PNG : l'onglet Aide affiche l'invite. La vérification interactive du survol (aide qui suit la souris) reste pour le run -WhatIf manuel du mainteneur : la structure (bindings présents, tooltips générés) est couverte par le parse et par `Select-String -Path .\Run-GUI.ps1 -Pattern 'SetToolTip'` qui ne doit plus montrer AUCUN texte en dur (uniquement des `Format-HelpTooltip`).

- [ ] **Step 3: Contrôles, commit**

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add Run-GUI.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: aide integree cablee sur le cockpit Trimko"
```

---

### Task 12: Profils qui tiennent leur promesse (reprise du plan du 14/08)

**Files:** ceux de la Task 7 du plan `2026-08-14-documentation-integree.md`.

**Interfaces:** les siennes (`Remove-KeptEntries`, `Read-KitProfile` étendu, `Build-ModuleArgList` cas '12', `12-Startup.ps1 -KeepPatterns`, les 3 profils réécrits, transmission GUI).

- [ ] **Step 1: Exécuter la Task 7 du plan 2026-08-14-documentation-integree.md avec trois adaptations**

1. Son Step 8 contient une coquille : dans `Set-GuiFromProfile`, le paramètre s'appelle `$Prof` (pas `$Profile`). Écrire `$script:ProfileStartupKeep = @($Prof.StartupKeep)` et `$script:ProfileDescription = [string]$Prof.Description`.
2. Rester sur `feat/v2.2-cockpit-trimko`, aucun push.
3. Trailers de commit.

- [ ] **Step 2: Validation**

Run: `pwsh -c "Invoke-Pester .\tests\Common.Tests.ps1 -Output Detailed"`
Expected: PASS avec les nouveaux Describe (Remove-KeptEntries, Build-ModuleArgList module 12, Read-KitProfile étendus).

```powershell
powershell -ExecutionPolicy Bypass -File .\modules\12-Startup.ps1 -WhatIf -KeepPatterns "Steam,EpicGamesLauncher"
```

Expected: `Profil : 2 entree(s) de la liste noire preservee(s)` dans la sortie, aucune ligne WHATIF sur Steam/Epic. (Ce module en -WhatIf ne modifie rien et ne demande pas d'élévation pour lister ; si une opération exige l'admin, elle se contente de journaliser : sortie à lire, pas de crash attendu.)

---

### Task 13: Retouches du rapport HTML

**Files:**
- Modify: `lib/Report.ps1` (bloc CSS vers les lignes 360-390, en-tête `<header>` et pied `<footer>` du HTML)
- Modify: `tests/Common.Tests.ps1` (si un test de contenu HTML existe, l'étendre ; sinon rien)

**Interfaces:**
- Consumes: rien de nouveau.
- Produces: le rapport HTML porte « Trimko Labs » en en-tête et le lien kit.trimko.com en pied.

- [ ] **Step 1: Localiser et modifier l'en-tête et le pied**

```powershell
Select-String -Path .\lib\Report.ps1 -Pattern '<header>|<footer>|</header>|</footer>' -Context 2
```

Dans le bloc `<header>` (celui qui contient le titre du rapport), ajouter juste après la ligne du titre principal :

```html
<div style="color:#99f6e4;font-size:12px;margin-top:4px">Trimko Labs</div>
```

Dans le bloc `<footer>`, ajouter en dernière ligne :

```html
<div>PC-Refresh-Kit - <a href="https://kit.trimko.com" style="color:#0d9488">kit.trimko.com</a></div>
```

(Adapter la syntaxe exacte à la construction du fichier : ces fragments s'insèrent dans les here-strings existants du HTML, au niveau d'indentation voisin. Ne toucher à AUCUNE autre ligne du template.)

- [ ] **Step 2: Vérifier au rendu**

```powershell
pwsh -c "Invoke-Pester .\tests -Output Detailed"
```

Expected: tout vert (les tests de rapport existants ne testent pas ces fragments ; aucun ne doit casser). Générer un rendu de contrôle si un échantillon de log de test existe dans `tests/` (sinon, la vérification visuelle du rapport se fera au run -WhatIf du mainteneur).

- [ ] **Step 3: Contrôles, commit**

```powershell
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
```

```bash
git add lib/Report.ps1 tests/Common.Tests.ps1
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "feat: marque Trimko Labs et lien du site dans le rapport HTML"
```

---

### Task 14: Documentation, version, captures et distribution

**Files:**
- Delete: `TOOLTIPS.md`
- Modify: `README.md`, `docs/PROCEDURE-OPERATEUR.md`, `lib/Report.ps1` (`Get-KitVersion`), `tests/Common.Tests.ps1` (test de version), `tools/Build-ReleaseZip.ps1` (version + liste `$include`)
- Create: `docs/RELEASE-NOTES-v2.2.0.md`, `docs/img/gui-prepare.png`, `docs/img/gui-run.png`, `docs/img/gui-close.png`

**Interfaces:**
- Consumes: `-UiPreview -PreviewPhase` (Task 9), le zip (`tools/Build-ReleaseZip.ps1`).
- Produces: la distribution v2.2.0 complète et documentée.

- [ ] **Step 1: Appliquer la Task 8 du plan 2026-08-14-documentation-integree.md (Steps 1 à 3)**

Suivre ses Steps 1 (TOOLTIPS.md supprimé et retiré de `$include`), 2 (section « Aide intégrée » du README) et 3 (version v2.2 dans Get-KitVersion + test + Build-ReleaseZip 2.2.0). Adaptations : branche courante, trailers, PAS son Step 7 (push).

- [ ] **Step 2: Captures des trois phases**

```powershell
foreach ($phase in @(@{ p='Prepare'; f='gui-prepare.png' }, @{ p='Running'; f='gui-run.png' }, @{ p='Done'; f='gui-close.png' })) {
    $proc = Start-Process powershell -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File','.\Run-GUI.ps1','-UiPreview','-PreviewPhase',$phase.p -PassThru
    Start-Sleep -Seconds 5
    $png = .\tools\Capture-Fenetre.ps1 -TitrePattern '*PC-Refresh-Kit*'
    $proc | Stop-Process -Force
    Copy-Item -Path $png -Destination (Join-Path 'docs\img' $phase.f) -Force
}
```

LIRE les trois PNG copiés : rendu final propre exigé (dernier contrôle avant publication). Remplacer dans `README.md` la section Captures :

```markdown
## Captures

![Préparation](docs/img/gui-prepare.png)

Préparer : profil d'intervention, modules, réglages et actions sensibles, aide intégrée. La barre
d'action résume ce qui sera fait.

![Exécution](docs/img/gui-run.png)

Exécuter : chaque module affiche son état et sa durée sur la timeline, le journal défile,
la barre porte la progression.

![Clôture](docs/img/gui-close.png)

Clôturer : passphrase masquée à remettre au propriétaire, checklist avant restitution, rapport.
```

Supprimer `docs/img/gui-cockpit.png` (remplacée) : `git rm docs/img/gui-cockpit.png`.

- [ ] **Step 3: Section mode aperçu du README et procédure opérateur**

Ajouter au README, après la section « Aperçu sans rien modifier (WhatIf) » :

```markdown
### Aperçu de l'interface sans droits administrateur

```powershell
powershell -ExecutionPolicy Bypass -File .\Run-GUI.ps1 -UiPreview
powershell -ExecutionPolicy Bypass -File .\Run-GUI.ps1 -UiPreview -PreviewPhase Running
powershell -ExecutionPolicy Bypass -File .\Run-GUI.ps1 -UiPreview -PreviewPhase Done
```

Ouvre le cockpit sans élévation, LANCER désactivé, avec des données de démonstration pour
chaque phase. Sert aux contributeurs et aux captures d'écran ; rien n'est lu ni écrit sur la machine.
```

Dans `docs/PROCEDURE-OPERATEUR.md` : relire le document et mettre à jour toute mention de l'ancienne disposition (bouton LANCER dans la colonne de gauche, mot de passe affiché sous le journal, checklist sous le journal) vers la nouvelle (barre d'action en bas, onglet Clôture, passphrase masquée + bouton Afficher). Les étapes métier (ordre des modules, vérification backup, fiche PC) ne changent pas.

- [ ] **Step 4: Notes de version v2.2.0**

Créer `docs/RELEASE-NOTES-v2.2.0.md` en partant du contenu donné dans la Task 8 Step 4 du plan 2026-08-14 et en le complétant : ajouter en tête de « Résumé » la refonte :

```markdown
# PC-Refresh-Kit v2.2.0

**Date :** 2026-08-21
**Licence :** MIT

## Résumé

Refonte complète du cockpit aux couleurs Trimko Labs et aide intégrée. L'écran suit le geste de
l'opérateur : préparer (profil, modules, réglages, actions sensibles), exécuter (timeline d'états
par module, journal en direct), clôturer (passphrase masquée, checklist de restitution, rapport).
Chaque module et chaque option disposent d'une aide complète, et les profils d'intervention
tiennent la promesse de leur nom.

## Cockpit Trimko

- Bandeau d'identité avec badge de mode : SIMULATION, INTERVENTION RÉELLE ou APERÇU
- Timeline d'intervention : chaque module passe d'une case à cocher à un état visuel
  (en attente, en cours, OK, avertissement, erreur, ignoré) avec sa durée
- Barre d'action en pied de fenêtre : résumé de ce qui sera fait, progression pendant le run,
  bilan chiffré à la fin
- Fenêtre redimensionnable (minimum 1000x576) qui tient sur un écran 1366x768, y compris à 125 %
- Journal sombre aux couleurs du rapport HTML
- Passphrase masquée par défaut en clôture, boutons Afficher et Copier
- Mode aperçu sans élévation : `Run-GUI.ps1 -UiPreview [-PreviewPhase Prepare|Running|Done]`
```

puis reprendre telles quelles ses sections « Aide intégrée », « Profils d'intervention », « Divers » (y ajouter la ligne `- Le rapport HTML porte la marque Trimko Labs et le lien kit.trimko.com`) et « Site du projet ».

- [ ] **Step 5: Zip de distribution et validation complète**

Appliquer la vérification du zip de la Task 8 Step 5 du plan 2026-08-14 (build + présence de `config/help.fr.json` et `lib/Help.ps1`, absence de `TOOLTIPS.md` et des specs), et vérifier EN PLUS :

```powershell
Add-Type -AssemblyName System.IO.Compression.FileSystem
$z = [System.IO.Compression.ZipFile]::OpenRead((Resolve-Path .\dist\PC-Refresh-Kit-v2.2.0.zip))
$n = $z.Entries.FullName ; $z.Dispose()
"Theme.ps1 present : $($n -contains 'lib/Theme.ps1')"     # attendu : True
"captures presentes : $((@('docs/img/gui-prepare.png','docs/img/gui-run.png','docs/img/gui-close.png') | Where-Object { $n -contains $_ }).Count)"   # attendu : 3 si docs/img est inclus, sinon 0 et README pointera vers le depot : verifier la liste $include de Build-ReleaseZip.ps1 et l'etendre a docs/img si absent
```

- [ ] **Step 6: Commit**

```bash
git add -A
git -c user.name=Trimko -c user.email=contact@trimko.com commit -m "docs: version 2.2.0, captures des trois phases, notes de version"
```

---

### Task 15: Validation finale de branche

**Files:** aucun nouveau.

- [ ] **Step 1: Suite complète**

```powershell
pwsh -c "Invoke-Pester .\tests -Output Detailed"
.\tests\Test-KitParse.ps1 -CI ; .\tests\Test-KitEncoding.ps1 -CI ; .\tests\Test-KitAnonymity.ps1 -CI
.\tests\Invoke-SmokeTest.ps1 -CI
```

Expected: Pester tout vert (baseline + Theme + Help + extensions Common), 0 erreur partout. NOTE : le smoke lance les modules en -WhatIf en process enfant ; il n'exige pas l'élévation pour la plupart des contrôles, mais s'il échoue ici pour une raison de droits, le noter et le laisser à la CI (windows-latest est élevé).

- [ ] **Step 2: Captures finales de contrôle**

Boucle visuelle des trois phases (Task 9 Step 6), lecture des PNG : aucun défaut visuel restant.

- [ ] **Step 3: État final**

```bash
git status --short          # attendu : propre
git log --oneline public-main..feat/v2.2-cockpit-trimko
```

Expected: la liste complète des commits de la vague. NE PAS pousser, NE PAS merger : demander la revue finale de branche (superpowers:requesting-code-review) puis l'accord du mainteneur. Après merge dans public-main et push `public public-main:main`, la CI distante valide, et les runs -WhatIf manuels du mainteneur (session élevée, y compris la GUI complète en dry-run) restent la validation terrain avant toute release GitHub.

## Après le plan

1. Revue finale de branche complète (pattern v1.9 : elle avait rattrapé un CRITICAL de réentrance invisible aux revues par tâche). Corriger, re-valider, puis merge + push sur accord du mainteneur.
2. Release GitHub v2.2.0 avec le zip (décision du mainteneur).
3. Runs -WhatIf élevés par le mainteneur : GUI complète en dry-run réel (aide au survol, pause backup, clôture) sur une machine de test ou la sienne en simulation.
4. Porter sur la branche privée `master` (historique disjoint) : décision séparée, hors vague.
